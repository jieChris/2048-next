# 2048 Next 账号色板与主题广场

本上下文定义账号私人色板库、跨设备色板选择以及自定义色板进入公开主题广场后的核心语言，避免把私人色板、界面主题和公开作品混为一谈。

## Language

**界面主题**：
控制页面、控件和棋盘容器整体外观的内置视觉方案，当前不由玩家自由编辑或分享到广场。
_Avoid_: 自定义色板、广场作品

**自定义色板**：
玩家拥有且可编辑的一套方块背景、文字、边框、发光颜色及强度配置；每套色板拥有跨设备稳定身份，内置色板不属于自定义色板。
_Avoid_: 界面主题、内置主题

**账号私人色板库**：
账号在云端长期保存的自定义色板集合，是登录状态下的权威色板库；最多包含十套自定义色板，不包含内置色板，每套色板拥有独立权威记录。
_Avoid_: 广场作品、浏览器本地色板、单套公开主题

**权威色板记录**：
账号私人色板库中一套自定义色板的服务端事实，包含稳定色板 ID、独立版本、内容和删除状态；它不与其他色板共用整库版本。
_Avoid_: 整库 JSON、设备缓存、广场发布版本

**色板版本**：
权威色板记录的一份不可变内容快照；每次服务端接受有效内容变化都会形成新版本，三方合并以客户端声明的共同版本为基线，当前版本和删除标记始终保留。
_Avoid_: 当前色板选择版本、设备时间戳、原地覆盖

**过期共同版本**：
设备声明的三方合并基线已超出服务器历史保留范围的状态；内容不一致时不得覆盖原色板，设备修改应以冲突副本无损保留。
_Avoid_: 普通同步冲突、最后写入覆盖、丢弃离线修改

**规范化色板提交**：
玩家点击保存后，设备针对一套稳定色板 ID 提交的完整最终内容、共同版本和操作身份；服务端从完整内容计算最小字段差异，而不是信任客户端给出的字段补丁。
_Avoid_: 整库提交、客户端合并结果、颜色拖动过程帧、未保存草稿

**设备色板工作副本**：
账号私人色板库在当前设备上的缓存和离线编辑副本；编辑期间的变化先成为本地草稿，玩家明确保存前不写入云端权威记录。
_Avoid_: 云端权威色板库、广场作品、自动保存结果

**色板编辑草稿**：
玩家在编辑器中尚未确认保存的一套色板内容；它只影响设置页预览，不改变游戏或其他页面，不进入离线操作队列、不创建云端版本，离开编辑上下文前必须选择保存、放弃或取消。
_Avoid_: 已保存工作副本、待上传色板、云端 revision

**色板保存操作**：
玩家点击保存后形成的一次规范化色板提交；它冻结当前草稿、生成稳定操作身份，并立即定向写回；网络不可用时先持久保存到当前设备并进入原账号队列。
_Avoid_: 自动保存、每次颜色变化、页面离开

**账号离线操作**：
玩家在已识别账号归属但网络不可用时明确执行的色板创建、修改、删除或选择操作；它绑定该账号并在恢复联网后定向写回，区别于普通旧缓存。
_Avoid_: 游客操作、页面缓存、后台同步

**游客色板**：
未登录状态下只属于当前设备的自定义色板；登录时可以导入账号私人色板库，但游客当前选择不覆盖账号云端的当前色板选择。
_Avoid_: 账号离线色板、已同步色板

**当前色板选择**：
账号最后使用的色板偏好，可指向跟随主题、任一内置色板或账号私人色板库中的一套自定义色板，并随账号跨设备恢复；它拥有独立变化序列，所指自定义色板被删除时回退到跟随主题。
_Avoid_: 仅自定义色板 ID、本页临时预览、色板内容 revision

**待建立色板选择**：
旧账号数据无法确定跟随主题、内置色板或自定义色板时的迁移状态；第一个新客户端以有效设备选择原子建立账号选择，之后普通缓存不能再次覆盖。
_Avoid_: 默认跟随主题、空选择、每台设备重新认领

**色板身份**：
一套自定义色板跨设备保持不变的稳定 ID；内容哈希描述内容是否相同，但不决定两套色板是否是同一逻辑色板，内容相同而 ID 不同的色板允许共存并分别占用名额。
_Avoid_: 色板名称、内容哈希、数组位置

**色板顺序**：
账号私人色板库中已同步自定义色板的跨设备展示顺序；它拥有独立变化序列，与色板内容和当前色板选择分开变化，最后一次成功排序生效，本地待上传色板不进入正式顺序。
_Avoid_: 数组位置即色板身份、设备临时排序、色板内容 revision

**按套同步**：
账号私人色板库中的每套自定义色板拥有独立同步状态；一套色板的新增、修改或删除不使其他色板进入冲突。
_Avoid_: 整库同步、整库 revision 冲突

**色板库延迟加载**：
登录首同步只取得恢复当前显示所需的色板，完整私人色板库仅在玩家进入设置等需要浏览或管理色板的界面时加载。
_Avoid_: 首页加载全部色板、永不加载云端色板

**登录会话首同步**：
每个登录会话只读取一次账号的当前色板选择及恢复显示所必需的自定义色板；同一会话内不轮询或重复拉取色板库。
_Avoid_: 每页同步、账号一生只同步一次、后台轮询

**定向写回**：
玩家成功创建、修改、删除或应用色板后，只把受该操作影响的单套色板或当前色板选择立即写回账号，不重新上传整个色板库。
_Avoid_: 整库覆盖、离开页面时批量保存、后台轮询

**三方合并**：
同一色板发生并发修改时，由服务端以双方共同版本为基线，按名称、皮肤、单个方块等级的背景/文字/边框/发光、整体强度和单个倍率等最小用户可编辑单元比较；互不重叠的修改自动合并，重叠且结果不同的修改不互相覆盖。
_Avoid_: 客户端裁决、最后写入覆盖、整库合并、整组颜色数组冲突

**重复内容色板**：
内容哈希完全相同但稳定 ID 不同的多套自定义色板；它们可以共存并分别占用名额，自动导入或保存时默认使用已有色板，玩家也可明确选择保留新色板。
_Avoid_: 网络重试重复创建、同一色板的多个 revision

**重复确认**：
后台流程发现重复内容色板时留下的待决状态；它不占用新的云端名额，玩家进入色板管理界面后选择使用已有色板或保留新色板。
_Avoid_: 同步冲突、自动弹窗、静默去重

**冲突副本**：
三方合并无法无损完成时，为后到修改创建的新稳定色板身份；原色板和后到修改都被保留，账号库已满时冲突副本转为本地待上传色板。
_Avoid_: 覆盖版本、错误提示、同一色板的新 revision

**本地待上传色板**：
由于账号私人色板库已满而尚未进入云端的一套设备色板工作副本；它绑定原账号并持久保留，上传成功前云端当前色板选择不指向它。
_Avoid_: 已同步色板、被丢弃色板、云端第十一套色板

**待上传重试**：
在不轮询的前提下，于当前设备释放名额、加载设置页或开启新登录会话时重新尝试上传本地待上传色板；登录首同步只优先处理当前正在使用的一套。
_Avoid_: 定时轮询、静默丢弃、每页重试

**离线操作队列**：
绑定账号并按稳定色板 ID 折叠的待写回操作集合；创建、修改、删除、排序和当前选择按依赖顺序执行，短暂失败保留待写回状态而不迁移到其他账号。
_Avoid_: 原始点击日志、整库上传队列、无限轮询

**单套色板同步状态**：
一套自定义色板自身的已同步、写入中、等待联网、本地待上传、重复确认或已删除状态；其他色板的状态不改变该色板能否使用或分享。
_Avoid_: 整库 dirty、全局同步冲突

**可分享色板**：
已存在于账号私人色板库、没有未写回修改且未被删除的当前自定义色板；主题广场只依据当前选中色板自身状态判断分享资格。
_Avoid_: 整个色板库全部同步、内置色板、设备本地待上传色板

**色板删除标记**：
账号对一个稳定色板 ID 的永久删除事实；它不占用十套有效色板名额，旧设备重新上线时必须服从删除标记，离线修改只能以新稳定 ID 保存，不能复活原 ID。
_Avoid_: 本地缺失、物理删除后遗忘、可复用色板 ID

**广场作品**：
一名玩家在主题广场维护的唯一公开分享位，可指向一个已发布版本和至多一个待审核的新版本。
_Avoid_: 私人色板、实时共享主题

**发布版本**：
广场作品中经过审核、内容不可变的一份自定义色板快照；作者更新分享时创建新版本，不原地改写旧版本。
_Avoid_: 草稿、私人源色板

**保存副本**：
其他玩家从发布版本复制到自己色板仓库的一套独立自定义色板，保存后不再跟随作者更新或下架。
_Avoid_: 订阅、实时引用、共享编辑

**引用**：
某个账号首次成功把某个发布版本保存为副本的历史事实；删除副本不撤销引用，同一账号重复保存同一版本不重复计数。
_Avoid_: 点击量、下载请求数、当前仍保留的副本数

**当前保存状态**：
账号色板仓库中目前是否仍保留某个发布版本对应的副本；它与历史引用事实分开计算。
_Avoid_: 引用次数、曾经保存

**评价**：
登录玩家对某个发布版本设置的一种互斥态度：点赞、点踩或未评价；作者不能评价自己的作品。
_Avoid_: 多次投票、累计点击

**首次同步对账**：
一个设备首次登录时，将设备色板工作副本按稳定色板 ID 与账号私人色板库对齐，并恢复账号的当前色板选择；单边新增或缺失不属于冲突。
_Avoid_: 同步冲突、整库覆盖

**色板同步冲突**：
同一稳定色板 ID 的本地与云端内容都从共同基线发生了不兼容修改，系统无法无损合并；仅本地存在、仅云端存在或内容相同都不属于冲突。
_Avoid_: 首次同步、待上传色板、待下载色板、设备私有元数据差异

**审核**：
由确定性规则、DeepSeek 语义判断和必要的管理员复核共同决定发布版本能否公开展示的过程。
_Avoid_: 仅靠 AI 自动裁决、仅校验颜色格式
