# 执行记录

## Route Deviation

- 仓库缺少 `.trellis/scripts/get_context.py`，采用现有任务目录格式手工记录，并直接读取 `.trellis/spec/index.md` 后继续。
- 初版将高阶方块显示为二进制 `K/M` 简写，容易与十进制数量混淆；按用户反馈改为显示完整的 2 的幂整数。
- 后续按用户反馈将原有 `1K～64K` 也统一改为 `1024～65536` 完整整数。
- 预览棋盘原先使用独立尺寸、字体和固定棕色背景；改为复用实际棋盘的 `.game-container`、`.tile`、`.tile-inner` 主题规则。
- 正式棋盘方块字号由 `HTMLActuator` 按格宽和数字位数动态写入，单靠共享 CSS 仍会产生差异；预览改为使用同一计算规则，并在窗口尺寸变化时重新计算。
- 拓展预览首次切换到 6～8 位完整数字后出现文字溢出；在预览与正式棋盘的同一字号算法中增加按位数计算的宽度上限，并保留原有 2～5 位字号。

## 验证记录

- `node --check js/theme_manager.js js/palette_page.js js/core_theme_settings_runtime.js`：通过。
- `npx vitest run tests/unit/bootstrap-theme-settings.spec.ts`：10 项通过。
- `npx playwright test --config=playwright.config.ts --workers=1 tests/smoke/pages-palette-board-switch-preview.smoke.spec.ts`：9 项通过；覆盖四个颜色维度、刷新持久化、26 项导出和最高方块运行时 CSS。
- `npm run build`：通过。
- `git diff --check`：通过。
- 浏览器桌面端 `1280×720`：默认预览 16 格、展开后 26 格，无页面或编辑器横向溢出。
- 浏览器移动端 `390×844`：展开后 26 格，高阶标签显示 `131072～67108864` 完整整数，无页面、标签或编辑器横向溢出；检查后已恢复默认 viewport。
- 浏览器桌面端与移动端：顶部主题预览、颜色编辑格和下方棋盘均显示 `1024～65536` 完整整数，无文字溢出。
- 桌面端预览棋盘为 `500×500`、`15px` 内边距和间距；动态字号为 2=`51px`、128=`43px`、1024/4096=`37px`、16384=`31px`，与正式棋盘算法一致。
- 浏览器移动端 `390×844`：预览棋盘为 `280×280`、`10px` 内边距和间距；动态字号为 2=`28px`、128=`26px`、1024/4096=`20px`、16384=`17px`，雾青灰背景、圆角和阴影均随当前主题生效，页面 `scrollWidth` 与 viewport 同为 `390px`。
- `style/main.css` 的本地 `@import` 已恢复为无查询参数路径，生产构建可正常内联主题样式。
- 本轮目标 Smoke：9 项通过；`npm run build` 与 `git diff --check` 均通过。
- 按页面反馈移除“外观设置”眉题与分类说明，并隐藏顶部小型配色预览；主题选择区域改为单列铺满，隐藏预览挂载点继续供现有主题初始化复用。
- 进一步移除独立主题卡片与棋盘选择，将主题下拉合并到左侧“色板”栏顶部；前 16 级编辑按等级同步到其他规则变体，高阶颜色仅保留给支持它们的棋盘。
- 左侧“色板”标题缩小为与“选择主题”一致的 `15px / 600`；主题下拉箭头重置共享三角形边框，并分别使用 `-45deg / 45deg` 表示收起向右与展开向下。
- 默认日间外观改为“雾青灰 + 冷青·跃阶”；经典和跟随主题仍保留为可选项，夜间预设继续使用夜幕星云与跟随主题。
- 将“跟随主题”固定 ID 与默认选中色板 ID 分离，避免冷青色板成为默认后产生重复 ID。
- 首屏主题预加载默认值同步为雾青灰，并统一更新 20 个页面的预加载缓存版本。
- `core-night-mode-preload` 与 `core-night-mode-runtime` 单测共 14 项通过；色板 Smoke 9 项、共享设置 Smoke 6 项通过；`npm run build` 与 `git diff --check` 通过。
- 拓展视图改为仅显示 `131072～67108864`，下方预览同步显示这 10 个高阶方块；桌面与移动 Smoke 均验证文字不溢出，首个色板项顶部间距为 `4px`。
- 窄屏色板列表项禁用 Flex 收缩，内容保持完整高度，超过 `280px` 的部分继续使用列表滚动；色板 Smoke 9 项通过。
- 设置页顶部分类改为互斥切换：无锚点默认仅显示计时器，`#appearance-settings` 显示外观与配色；未选模块使用原生 `hidden` 隐藏，按钮同步选中态。
- 删除编辑区重复的“当前色板名称 + 只读/维度”标题行及其运行时必需节点绑定，保留下方名称输入框和操作区。
- 删除外观模块独立的“主题与色板”标题及废弃多语言文案/样式，直接由顶部选中的“外观与配色”承担模块标题，避免新增外层卡片嵌套。
- 内置及“跟随主题”色板同步禁用名称输入框；切换或新建自定义副本后恢复可编辑，防止出现可输入但无法保存的假交互。
- 禁用名称框复用按钮的 `opacity: 0.42`、`not-allowed` 与灰度状态，并提供中英文悬停说明；自定义副本不显示该提示。
- 将 `#palette-note` 移入颜色编辑标题行：桌面端位于“背景颜色”右侧，窄屏不足时自然换行；保留原状态更新与 `aria-live`。
- 雾青灰主题下的“跟随主题”改为完整映射“冷青·跃阶”，同步背景、文字、边框和发光四个维度；其他主题的跟随逻辑保持不变。
- 色板 Smoke 增至 10 项并全部通过，新增断言验证雾青灰“跟随主题”与“冷青·跃阶”四个颜色维度完全一致；`npm run build` 与 `git diff --check` 通过。
