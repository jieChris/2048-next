# 排行榜昵称下伸部裁切

## 目标

- 修复游戏内排行榜昵称中 `g`、`y` 等下伸字母被裁切的问题。
- 保留现有固定高度、居中、截断省略和其他计时方块样式。

## 实施

1. 先增加排行榜昵称专属行高的 CSS 回归测试并确认红灯。
2. 只在名称方块规则中补足行高，不修改全局 `.timertile`。
3. 运行目标单测、`git diff --check`，并用内置浏览器验证实际昵称。
