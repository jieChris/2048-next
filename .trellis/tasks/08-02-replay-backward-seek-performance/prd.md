# 回放后退性能修复

## 目标

- 普通回放后退一步或向后拖动时，不重复从开局重放大量动作。
- 正常播放、向前单步、回放导入和包含撤回动作的回放保持原行为。
- 状态缓存有明确上限，长回放不会无限增长内存。

## 验收标准

- 已访问过的近期步骤可直接恢复，不调用 `restartWithBoard()` 或重复执行中间动作。
- 超出近期历史时，从稀疏检查点恢复，单次补算动作数有固定上限。
- 向前单步仍执行真实移动，动画和出生方块逻辑不变。
- 含撤回动作的回放继续使用原重建路径，避免破坏撤回栈语义。
