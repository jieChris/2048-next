# 执行记录

## Route Deviation

- 2026-07-22：`trellis-brainstorm` 预期使用 `./.trellis/scripts/task.py create` 创建任务，但本仓库没有 `.trellis/scripts/task.py`。采用最保守回退：仅手工新增本任务目录与文档，不修改或迁移任何既有 Trellis 文件。
- 2026-07-22：实施前按 `trellis-before-dev` 调用 `./.trellis/scripts/get_context.py --mode packages`，但仓库没有该脚本。采用最保守回退：手工只读枚举现有 `.trellis/spec`、包清单与仓库说明，逐项读取适用规范后再编码。
- 2026-07-22：当前环境没有可用的 PostgreSQL 客户端、服务或容器运行时，无法在真实数据库 fixture 上执行 `0019_record_era.sql`。采用最保守回退：只完成静态 SQL 防破坏测试与全量代码验证，不运行迁移；把真实数据库迁移预演、备份和前后不变量查询保留为正式发布门槛。
- 2026-08-01：`trellis-before-dev` 要求读取 `.trellis/spec/guides/index.md`，但仓库没有该目录或文件。采用最保守回退：只读取现有 `.trellis/spec/index.md` 及其列出的架构、API 边界和 Smoke 规范；本次只修改已有 Rating 任务文档，不触碰业务代码或无关工作区改动。

## 进度

- 已记录用户目标与初步验收标准。
- 已完成前端、API、排行榜三个仓库的只读数据审计。
- 已只读检查生产公开 API：确认标准无撤回 PB 榜当前 15 名玩家，并确认单个活跃账号存在 375 条跨模式记录。
- 已从公开 2048Verse 终盘数据逐人精确复原 4x4 单局与 B10 rating 公式；未将网络数据写入仓库。
- 用户已确认 rating 的核心语义为“长期稳定实力”；旧 B10 rating 仅保留为独立的巅峰指标。
- 用户已确认首版只使用 2048next 自有、回放验证通过的 ranked 记录，不融合人工导入历史榜。
- 已只读查询 27 个 ranked 模式的生产公开榜单覆盖：标准 4x4 无撤回以 15 人居首，3x3 无撤回 9 人、4x2 无撤回 7 人，其余更少。
- 用户已确认首版限定 `standard_4x4_pow2_no_undo`，其他模式以后单独标定。
- 用户已确认明确开始后的主动退出、重开和删局继续影响 rating；未开始 session 与确认的基础设施故障不处罚。
- 已记录现有历史无法完整回补的限制：删局 3 天后物理清理，session 过期状态懒更新。
- 用户已确认 rating 采用近期窗口，旧成绩降低权重，生涯表现另行展示。
- 已只读统计标准模式公开玩家样本量：15 人，中位 2 局，至少 5 局 3 人，至少 10 局 1 人。
- 用户已确认样本不足时不显示暂定 rating；只有估计稳定后才公开数值。
- 用户已确认数据不足时只显示简洁提示语，不展示资格进度或有效局数。
- 用户曾确认正式 rating 采用 1500 中心制、约 200 分为一个标准差层级；此决定已于 2026-08-01 被固定基准方案替代。
- 2026-07-22：用户进一步澄清此前的“清理旧记录”并非物理删除。正确需求是把全部现有成绩隔离为“内测成绩”，继续保留在玩家记录中，仅作历史档案，并从所有正式成绩统计中排除。
- 已撤销研究文档中的旧记录删除方案；未执行过任何删除、迁移或业务代码修改。
- 正式统计将从独立的新纪元开始，旧成绩不回算、不迁入正式 Rating。
- 用户已确认内测档案可查看详情、回放和导出，但为只读，并从 PB、排行榜、Rating、个人汇总、里程碑和成绩型成就中全部排除。
- 用户已确认切换时已由玩家主动删除、仍处于恢复期限内的成绩不恢复，继续原删除流程。
- 用户已确认本次只隔离 2048next 服务器玩家游戏记录；本地历史、石头专题、接力数据和人工维护/导入旧榜保持不变。
- 用户已确认切换前已经开始但尚未提交的排位局可继续完成，但结果仍为内测成绩；只有切换后新开始的排位局进入正式统计。
- 用户已确认内测档案继续在公开玩家主页可见，并保留公开详情、回放和导出能力，同时显示“内测成绩”标签。
- 用户已确认以支持新统计字段的正式版本上线作为唯一纪元切换点，不按历史日期追溯。
- 此前仅完成需求与实施规划；用户随后授权在工作区先完成代码与本地验证。数据库与线上数据仍未修改，迁移和纪元切换继续等待正式发布指令。
- 2026-07-22 实施就绪度复核：纪元隔离、内测档案、正式统计过滤和新数据采集已具备实施条件；正式数值 Rating 因新纪元尚无样本、模型参数尚待回测，不具备启用条件。
- 2026-07-22：用户授权立即在工作区实施并完成本地验证；正式版本发布前仍禁止运行线上迁移、清理线上派生数据或启用正式纪元。
- 2026-07-22：实施复核发现“内测档案只读”与“删除中的记录继续原流程”需明确优先级。按用户后确认的删除规则处理：生效中的内测档案禁止新删除；切换前已软删除的记录仍可在原期限内手动恢复，恢复后转为只读内测档案。
- 2026-07-22：实施审计确认现有 session 在签发时即标为 started，无法区分预取但未游玩的会话。采用最小修正：不新建表，复用 `ranked_sessions` 的 `created/started/consumed/abandoned` 状态与 `server_context`，通过服务端验证的 attempt 事件固化正式 Rating 原始事实。
- 2026-07-22：实施复核发现正式发布后仍可能收到浏览器旧 pending payload。为防止旧协议成绩按提交时间误升正式，增加客户端 schema capability handshake：缺少当前版本的 session/普通记录继续归入 beta；纪元与统计摘要仍完全由服务端决定。
- 2026-07-22：已完成 A/B 记录纪元隔离。新增 `0019_record_era.sql`；正式统计、榜单缓存和成绩型成就只读取 `official_v1`，玩家历史与回放继续跨纪元读取；内测档案保持公开、可回放和可导出，但生效中的内测记录不可新删除。
- 2026-07-22：已完成 C 阶段尝试账本。新排位 session 以 `created` 签发；首次有效移动经 replay 验证后进入 `started`；终局进入 `consumed`；确认重开或明确站内离开进入 `abandoned`。完成与放弃摘要固化在 session `server_context`，不会随可见 record 的物理清理消失。
- 2026-07-22：前端 attempt outbox 使用无 TTL、按用户隔离的本地持久化；普通 pagehide 只冲刷已有事件，不自行判定放弃。若 abandon 无法持久化则阻止离开；若 abandon 已记录但下一 session 暂不可用，则冻结旧局移动，避免继续提交已放弃会话。
- 2026-07-22：Rating 公开占位已完成。API 固定返回 `rating.value = null`、`status = insufficient_data`，界面只显示“数据积累中，暂无 Rating”；未实现、未展示任何暂定数值。
- 2026-07-22：后端独立复验通过：Node 21 个文件、159 项；兼容 Worker 4 个文件、59 项；合计 218 项测试、TypeScript 类型检查及 `git diff --check` 均通过。
- 2026-07-22：前端独立复验通过：目标合同与单元测试 112/112、定向 Smoke 32/32、OpenAPI 类型漂移检查、453 文件服务边界审计（0 违规）、生产构建及 `git diff --check` 均通过。构建仅出现既有 `ui-preview.html` 非 module 脚本警告。
- 2026-07-22：未执行 PostgreSQL 迁移、未清理线上派生数据、未部署、未提交、未推送。正式发布仍需在隔离数据库副本完成迁移预演和不变量校验，再按“迁移数据库 → 同版本 API/前端部署 → 校验”一次性切换。
- 2026-07-22：兼容策略暂时保留无版本客户端进入 beta 的能力，以避免旧客户端污染正式数据。这也意味着玩家可选择非正式局；在数值 Rating 启用前必须重新评估是否拒绝无版本 ranked session 或显式区分 rated/unrated 入口。
- 2026-08-01：用户决定将正式 Rating 从“基于整体玩家分布的相对标尺”改为“项目自有固定基准的绝对实力指数”。先前关于总体中心、参考分布和 `1500 + 200 × z` 的方案由此作废。
- 2026-08-01：新标尺定义为 `b = log1p(B)`、`skill = (n × m + k × b) / (n + k)`、`rating = 1500 + 200 × (skill - b) / ln(2)`。1500 对应固定有效表现基准 `B`，约每 200 分代表稳定有效表现翻倍；整体玩家数量、均值和标准差不参与个人 Rating 计算。
- 2026-08-01：正式玩家数据仍用于验证窗口、收缩强度、资格阈值和预测能力，但不得动态移动基准 `B`。具体 `B`、窗口、`k` 和公开阈值仍待冻结，因此 API 与界面继续只显示 `insufficient_data` 占位。
