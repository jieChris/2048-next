# “经典棋盘工坊”真实页面预览台实施计划

## 目标

新增独立预览页，在不修改正式页面样式的前提下，将当前页面与候选视觉系统并排展示。

## 最小实现

- 新增 `visual-preview.html`，只承担预览台结构。
- 新增 `src/entries/visual-preview.ts`，负责页面、设备、主题切换，iframe 初始化、安全拦截和只读示例状态。
- 新增 `style/classic-workshop-preview.css`，同时包含预览台外壳和只在候选 iframe 生效的主题规则。
- 首批页面：`2048.html`、`Practice_board.html`、`account.html`、`account_settings.html`、`medal-wall.html`、`relay_5x5.html`。
- 候选样式通过 `html[data-classic-workshop-preview="1"]` 作用域隔离，正式页面不加载该样式。
- 当前与候选 iframe 使用相同 URL、相同 DOM 和相同只读示例状态。
- 拦截导航、表单提交和业务按钮；保留设置、统计、展开、筛选等安全本地交互。

## 实施顺序

1. 先新增 Playwright 冒烟测试并确认因页面不存在而失败。
2. 实现预览页、模块入口和 Vite 构建入口。
3. 实现 iframe 同步、候选 CSS 注入、设备/主题切换和安全拦截。
4. 增加最少量只读示例状态，使无后端时仍能检查表格和成就布局。
5. 完成候选浅色与夜间样式。
6. 运行专项测试、语言审计、生产构建和 `git diff --check`。
7. 使用桌面和移动视口截图验收。
8. 将账号设置游客登录表单与操作区合并为单面板，并把登录状态标签接入全站语义色 Token。

## 验收标准

- 当前版和候选版真实并排显示。
- 六个代表页面均可切换。
- 桌面、移动与浅色、夜间状态均可切换。
- 候选 iframe 加载候选样式，当前 iframe 和正式页面不加载。
- 无后端时账户榜单与成就页仍有只读示例内容。
- 预览中的链接、表单和危险操作不会离开页面或提交数据。
- 正式页面文件和正式共享样式不因候选设计而改变。

## Route Deviation

- `.trellis/scripts/get_context.py` 在当前仓库不存在，因此无法运行包上下文脚本；已改为直接读取 `.trellis/spec/index.md`、`frontend-api-boundary.md` 和当前任务设计文档，未改变实现边界。

## 雾青石板配色修订

1. 先用现有 Playwright 冒烟测试锁定候选页背景、棋盘、分数/时间框和夜间根背景颜色。
2. 只修改 `style/classic-workshop-preview.css` 中右侧候选作用域的颜色变量与游戏表面，不修改按钮结构、动画或正式页面样式。
3. 运行专项测试、生产构建和桌面截图验收。

## 正式应用

1. 用 Playwright 锁定正式游戏页的暖雾象牙配色，并锁定 `modes.html` 内部按钮样式不变。
2. 通过 `style/main.css` 接入已确认主题，使所有正式页面复用同一套表面、棋盘、表单和夜间规则。
3. 移除预览 iframe 的候选 CSS 动态注入，预览与正式页面改为读取同一份共享样式。
4. 运行专项测试、生产构建、差异检查和真实页面截图验收。

## 2026-07-20 全站经典配色修订

1. 将最终浅色与夜间角色写入 `style/tokens/base.css`，让工坊、釉彩按钮和页面私有变量复用现有 token。
2. 统一普通、悬停、主要、选中、未选中、焦点、禁用、危险、开关和下拉选项状态。
3. 将 `modes.html` 的独立暖米色与深蓝配色映射到全站雾青灰和深青炭体系，保留页面结构。
4. 清理账户表头、触屏卡片、色板工具面板、设置弹窗和历史行中的旧暖色或蓝色残留。
5. 统一游戏页分数、最佳和计时的文字层级，仅保留小面积角色标记。
6. 更新正式 HTML 的 `main.css` 缓存键和现有视觉回归断言。
7. 运行专项单元/Smoke、生产构建、`git diff --check`，并验证代表页面的浅色、夜间、桌面和移动状态。

### 验收补充

- 所有普通辅助文字在实际表面上达到至少 `4.5:1`。
- 青色只承担交互、选择和焦点；金色只承担里程碑和主要操作细节；绿色只承担成功。
- 同一开关、选中、危险和禁用状态在不同页面及明暗模式中使用相同语义。
- 模式页与其余页面共享画布、面板、正文、边框和夜间色温。
- 页面专有颜色只用于内容本身，例如数字块、成就徽章和色板预览。

## 暖釉棋子色板

1. 复用 `theme_manager.js` 现有内置棋子色板注册机制，新增“暖釉·跃阶”，不修改经典默认色板。
2. 2048 与斐波那契共享同一组 16 色和文字色，不新增色板框架或运行时分支。
3. 扩展现有色板 Smoke，锁定完整色值、`2/4` 距离、最低相邻距离、文字对比度及两种棋盘的一致性。
4. 运行专项 Playwright、生产构建、`git diff --check` 和真实色板页视觉核验。

## 翡翠赭金棋子色板

1. 继续复用 `theme_manager.js` 的内置色板注册机制，新增“翡翠·赭金”，不修改其他色板。
2. 2048 与斐波那契共享 16 色和文字色，不增加色板框架或专属运行时逻辑。
3. 扩展现有色板 Smoke，锁定完整色值、`2/4` 距离、最低相邻距离、文字对比度和两种棋盘一致性。
4. 运行专项 Playwright、完整色板页回归、生产构建、`git diff --check` 和桌面/移动视觉核验。

## 排行榜筛选与表面修正

1. 复用用户历史记录页的“撤回类型 → 模式”联动方式，在排行榜中拆分撤回与模式选择器。
2. 模式下拉仅显示当前撤回类型可用的模式，但排行榜请求继续发送原有完整模式键。
3. 排行榜主卡片使用 `--app-surface-light`，筛选区使用 `--app-surface-soft`，移除旧暖米色渐变与边框。
4. 用专项 Smoke 锁定选项收缩、请求参数和计算样式，再完成真实页面交互与视觉核验。

## 经典与雾青灰主题拆分

1. 新增 `mist_cyan` 内置主题，复用经典数字块并配置雾青灰计时器角色。
2. 将基础雾青灰 Token 和四个经典专用 CSS 作用域迁移到 `data-theme="mist_cyan"`。
3. 恢复 `classic` 的原版页面、棋盘、空格、分数和棕色操作控件。
4. 更新主题、回放、工坊和缓存键回归，验证经典、雾青灰、包豪斯与马年大吉互不覆盖。
5. 运行相关 Playwright、生产构建、`git diff --check`，并核验回放页两套主题。

## 经典顶部文字按钮统一

1. 在现有顶部操作响应式样式中增加经典桌面文字模式覆盖，不新增组件或运行时分支。
2. 用主页 Smoke 锁定可见按钮的统一高度、圆角和文字不溢出。
3. 更新正式页面主样式缓存键，并核验桌面与移动端无横向溢出。

## 经典 4x4 竞速榜单

1. 仅在“无撤回 + 4x4”下为榜单分类增加“竞速”，其他模式继续只显示已有分类。
2. 选择竞速时显示数字选项 `2048 / 4096 / 8192 / 16384 / 32768`，请求使用 `metric=speed&target_tile=<数字>`。
3. 权威 API 直接复用已验证记录的 `verification_summary.tile_times_ms`，按玩家取所选目标的最短时间，不新增表或迁移。
4. 更新 OpenAPI 契约，并用前端 Smoke、后端 Node 测试、类型检查、生产构建和真实页面完成验收。

## 本地历史筛选收敛

1. 移除“导入合并”“导入并替换”及文件选择入口，只保留导出全部与清空全部。
2. 复用用户历史记录页的“撤回类型 → 模式”两级筛选，模式仍以完整 `mode_key` 查询本地存储。
3. 保留归属、关键词、排序、分页与单条记录操作，不扩展存储或 API 边界。
4. 用现有历史 Smoke 锁定控件移除、模式联动和关键词搜索，再执行生产构建与真实页面核验。

## 二级页面页头收敛

1. 在共享样式入口新增单一返回控件样式，使用 CSS 绘制箭头，不新增脚本或图标依赖。
2. 将八个主要二级页的可见标题替换为返回链接，复用现有本地化文本作为无障碍名称。
3. 保留视觉隐藏的原 `h1`，移除同一页头中重复的返回文字入口。
4. 模式页释放原品牌标题列宽；触屏页将返回箭头和保留的首页入口排在同一行。
5. 用专项 Smoke 锁定返回路径、透明无边框外观、`44×44px` 点击区域和隐藏标题，再执行语言审计、相关页面回归、生产构建与视觉核验。

## 模式页标准分类合并

1. 用模式页 Smoke 锁定独立八方向、封顶标签与面板移除，标准面板包含两类全部原入口。
2. 将八方向、封顶模式和禁止目标移动到标准面板，保持原模式键和链接不变。
3. 标签栏调整为桌面四列、移动端两列，并同步中英文分组顺序。
4. 运行模式页专项 Smoke、双语审计、相关单元测试、生产构建与桌面/移动实页核验。
