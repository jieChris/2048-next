# 实施计划：成就系列融合

1. 在前端新增纯聚合函数，输入目录、已获得记录与展示位，输出系列卡片和单项卡片；为三级、五级、隐藏、空系列写单测。
2. 重构勋章墙渲染为系列网格、类别/状态筛选和详情抽屉；保留现有图标生成器并按当前等级取图。
3. 更新统计逻辑，排除未获得隐藏节点，统计可见类型与可见等级。
4. 修改展示位选择与服务端保存校验，使同一 `series_id` 最多出现一次；读取旧数据时只在 UI 层去重。
5. 在解锁提示队列入口按系列保留最高新等级，确保一次游戏不会弹出同系列的 3 到 5 个重复提示。
6. 部署 `0020_lost_page_achievement.sql`，验证“你也曾迷路”在未获得前不出现、获得后进入隐藏分类。
7. 运行前端单测、成就页 Smoke、后端成就测试、构建输出检查和移动/桌面截图验证。

## 验证重点

- 新账号：所有常规系列灰显，隐藏分类不存在。
- 仅获得 `tile_2048_count_1`：`2048 里程碑` 显示 `1 / 3`，后两级灰显。
- 一次触发多个 `speed-2048` 阈值：数据层获得全部符合条件的节点，界面只弹出最高一档。
- 展示位选择同一系列的两个等级：UI 和 API 都拒绝重复。
- 英文界面：系列名称、状态、等级条件、日期标签均无中文残留。
