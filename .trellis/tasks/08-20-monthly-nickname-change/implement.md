# 实施计划

1. API 增加 `users.nickname_changed_at`，不回填历史值。
2. 自助昵称修改在数据库事务内锁定用户行，按 `Asia/Shanghai` 自然月检查并更新。
3. 返回稳定错误码、`Retry-After` 和下次可修改时间。
4. 账户设置页提交前确认，并映射月度限额错误。
5. 仅运行昵称路由、前端静态合同与必要类型检查；网页交互留给内置浏览器或 CI。

