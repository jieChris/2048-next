# 排行榜对局回放时间完整性：执行记录

## 当前状态

- 2026-08-12：已在实际生产前端基线 `b73cab95` 的隔离分支实施恢复时间轴修复；API 基于生产 `ab53282` 实施目标级竞速排除与历史可见性恢复。
- 尚未提交、推送、部署或正式执行 `0028_target_speed_metric_exclusions.sql`。
- 主设计位于原工作树 `.trellis/tasks/08-12-ranked-replay-timing-integrity/design.md`；本分支仅携带实施记录，避免复制超长调查文档。

## 已实施

- V1 检查点恢复保留原始 root、记录顺序、出生块、扩展记录、`start_unix_ms` 和逐步 `deltaMs`。
- 恢复重演只允许内部动作通过输入门禁，并阻断渲染、最佳分、本地持久化、检查点写入和终局上传副作用。
- 下一次真实动作从主计时器累计值减去已记录累计值生成 `deltaMs`；旧存档缺少累计字段时从 move/undo 记录求和。
- 页面关闭期间仍按既有主计时器语义继续计时。
- 重演失败先用现有完整存档能力回滚 manager；失败或无法建立回滚快照时保留恢复锁和原镜像，`pagehide` 不得覆盖镜像。
- API 删除整条记录级 `<20ms/步` 判定和新写入的 `speed_eligibility` 字段；公共竞速榜、个人最快、速度成就及 canonical 工具统一读取目标级 `speed_metric_exclusions_v1[target]`。
- `0028` 仅为已确认事故记录的 2048 指标写排除，并移除旧整局策略字段；不删除或隐藏记录、普通分数、回放或同局其他目标成绩。
- 新申请的排行榜对局使用出生序列 V2：起手仍消费序号 `0、1`，后续回放动作记录从 `2` 继续；旧令牌、旧回放、旧存档缺少版本时保持 V1。
- 出生序列版本贯穿会话响应、活动会话、检查点、完整/精简存档、回放扩展和管理员救援；API 同时绑定令牌、数据库会话上下文和回放版本，任何不一致均拒绝。
- 精简存档或检查点清空 `move_history` / `spawn_value_counts` 时，从 V1 回放恢复 `action_count`、从 `init_tiles` 恢复起手出生数，避免刷新后序号回退。
- 超长局只在恢复时线性派生一次 `action_count`，正常游玩每步 O(1) 累加；不在每次出生时扫描整份回放。

## 定向验证

- 前端恢复、输入、撤回、保存和提交相关单元测试已分批通过；最终修改后将只重跑直接受影响集合、类型/构建和 `git diff --check`。
- API 竞速查询、成就、导入、canonical 门禁、迁移边界相关测试和 typecheck 已通过；提交前将做一次最终定向复核。
- 数据库已在生产数据上用事务演练 `0028` 并完整回滚。演练期间有一条正常并发新记录；旧字段清零、目标排除恰好一条、事故证据 guard 和审计写入均符合预期。
- 出生序列前端定向集合 141 项通过，另有精简存档专项 86 项通过；覆盖 V1 `0、1、0、1…`、V2 `0、1、2、3…`、撤回计数、精简存档刷新和 RPL1 版本保留。
- 出生序列 API 定向集合 81 项通过；覆盖旧令牌字节不变、V2 令牌/回放/数据库绑定、V2 错用旧序号拒绝及无版本旧回放继续通过。
- 最终收尾：OpenAPI 类型同步、服务边界审计、前端生产构建、API typecheck 与两个工作树 `git diff --check` 均通过；O(1) 动作计数和提交版本绑定收紧后的直接前端回归 104 项通过。

## 发布顺序

1. 推送并部署停止写旧字段的新 API。
2. 创建迁移前数据库专用备份并验证可读。
3. 执行 `0028`，核对记录/回放计数不下降且仅事故 2048 被排除。
4. 推送生产基线前端修复分支并对该分支触发 `deploy-self-hosted.yml`。
5. 仅使用内置浏览器验收生产竞速榜与刷新续局。

## Route Deviation

- 前端 `origin/main` 与实际生产 `b73cab95` 严重分叉。采用最保守路线：从生产 SHA 建隔离修复分支，只对该分支手动部署，不合并、不推送前端 main。
- 数据库演练期间生产有正常并发写入，记录和回放计数各增加一条。演练事务已完整回滚；正式迁移按“计数只能增加、不得下降”验收，不把并发增长误判为迁移副作用。
- 只读审阅发现真实移动使用全局 `actuate(manager)`，替换 `manager.actuate` 无法覆盖副作用边界。已在共享 `actuate` 和在线提交入口增加最小 guard，并补直接回归。
- 只读审阅发现重演失败会留下部分改写的 manager 并解除输入。已增加现有完整存档快照回滚；若快照或回滚不可用则保持恢复/持久化锁，绝不放行半恢复状态。
- 首次前端发布在既有 `game-manager-audit` 的 1500 行门禁被阻断，生产未发生切换。原因是旧壳层重复实现了核心保存状态运行时已有的 V1 会话规范化；采用保守修复删除重复实现并继续复用核心 fallback，不提高门禁、不改变恢复语义。
- 第二次前端发布通过全部审计与单元测试后，被 3 个新局冒烟用例阻断，生产仍未切换。根因是远端检查点读取已停用、恢复候选只剩本地镜像，但设置逻辑仍因“存在登录令牌”把无镜像的新局标记为 `rankCheckpointRestorePending`，继而按恢复保护合同阻断渲染和首份存档。采用保守修复：仅在确有本地镜像候选时进入 pending，保留真实恢复期间的完整副作用屏障。
- 最终审阅发现精简存档会清空 `move_history` 和 `spawn_value_counts`，仅靠这两个字段会令 V2 出生序号在刷新后回退。采用保守兼容路线：从既有 RPL1 `records` / `init_tiles` 恢复计数，不改变回放字节格式。
- 初版恢复兜底若每步扫描全部 RPL1 records，会令数百万步 5×5 对局退化为 O(n²)。收紧为恢复时一次性派生 `action_count`、后续每步 O(1) 递增；该运行时字段不进入 RPL1 字节格式。
- 出生序列 V2 的首次前端发布在 `game-manager-audit` 的 19 行函数热点门禁被阻断，生产未切换。新增扩展记录令既有 `createReplayV1ExtRecords` 超限 3 行；采用保守修复仅压缩该函数排版，不改变序列化顺序或业务语义。
- 第二次出生序列 V2 发布通过全部结构审计后，被 5 个旧单测文件中的 6 个精确对象断言阻断，生产仍未切换。运行时新增的 `spawn_sequence_version=1` 兼容字段及 V1 `action_count=0` 均符合新合同；采用保守修复只同步这些契约预期，不修改运行代码。
