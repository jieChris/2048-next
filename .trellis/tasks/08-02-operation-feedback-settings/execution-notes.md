# Execution Notes

## Route Deviation

- 2026-08-03：HTTPS 推送在 HTTP/2、HTTP/1.1 及固定 Content-Length 上传下均于 sideband 返回阶段断连，远端分支未创建；对象审计确认实际 pack 约 22.2 MiB、最大 blob 约 332 KiB，不属于 GitHub 容量限制。保守改用已验证为同一 `jieChris` 账号的一次性 SSH URL 推送，并在完成后核对远端分支 SHA；不修改仓库 remote、不拆分提交、不删除视觉基线。
- 2026-08-03：多个诊断 Agent 曾同时运行 Playwright Runner，并共享默认 `test-results` 目录，导致一个业务断言已通过的结构化回放用例在关闭页面时因 trace 文件竞争报 `ENOENT`。保守停止其他 Runner，改用单 worker、隔离端口独占重跑完整 Pages Smoke；未放宽断言、增加重试或修改产品行为。
- 2026-08-03：本地页面最初只有 Vite，排位 4×4 因 API 不可用而保持空棋盘。短暂启动仓库现有线上 API 代理并仅执行健康检查后，发现其不符合“本地测试不向线上提交”的项目约束，立即停止且未用页面请求线上会话；保守切换为 `/tmp` 隔离 PostgreSQL、本地迁移和仅进程内随机密钥的 `dev:local`。内置浏览器确认本地页面已生成 2 枚起始棋子，未修改线上数据。
- 2026-08-03：发布时当前工作分支与远端 `main` 已明显分叉，直接推送会覆盖或回退已经上线的改动。用户明确选择发布工作区全部待提交内容后，保守改为先生成完整快照提交，再仅将该快照提交移植到最新 `origin/main`，通过发布门禁后由 PR 合入 `main` 触发既有原子部署；不以旧分支直接覆盖生产。
- 2026-08-02：标准 Trellis 前置脚本 `.trellis/scripts/get_context.py` 与共享指南索引不存在。保守回退为读取根规范、冒烟规范和实际设置模块；仅实施操作反馈设置调试页，不改游戏逻辑、视频或 CapCut 内容。
- 2026-08-02：用户取消独立 iframe 布局调试方案，要求直接在真实游戏页编辑。已保守删除 iframe、参考棋盘注入与独立弹层路径，复用当前页面和现有设置弹窗；未扩展到真实输入采集或统计逻辑。
- 2026-08-03：视觉基线工具依赖独立 Playwright Chromium，与“所有页面测试只使用 Codex 内置浏览器、不得启动本机 Chrome”的项目约束冲突。保守回退为真实 `/2048.html` 的内置浏览器截图、DOM 语义验收和 CSS／JSDOM 回归测试；未伪造或更新 Playwright 基线。
- 2026-08-03：内置浏览器当前不提供浮层 `getBoundingClientRect()`／连续 hover 的直接读取接口，且自动点击“新游戏”没有触发页面重开流程。锁定几何继续由固定 `96×520` CSS 契约、锁定前后坐标单测和既有透明命中桥探测覆盖；跨局清空由真实 setup 生命周期单测覆盖，没有改用 Chrome。

## Validation

- 设置弹窗规范化改为保留现有胜利、BGM、夜间模式核心节点，只增量补入操作反馈行和完整设置入口，避免 `innerHTML` 重建销毁既有事件监听；节点 identity 回归与 BGM／夜间模式目标 Smoke 均通过。
- 排位相关 Smoke 统一复用 `waitForRankedMoveReady()`，在直接移动前同时等待 `rankedSetupBlockedUntilSessionReady`、`rankCheckpointRestorePending`、`rankCheckpointApplying` 与 `needsRankedCheckpointRestore` 清零；覆盖存档恢复、回放和无撤回防篡改场景，不使用固定延时。
- 独占运行 `PW_WEB_PORT=4195 npm run test:smoke:pages`，最终 232/232 通过；此前 trace 产物竞争未再出现。
- `npm run test:visual -- --reporter=dot` 首次为 407/408：唯一差异是 320×568 夜间设置弹窗的旧基线仍显示夜间模式关闭，而 fixture、DOM 和夜间运行时均明确为开启；连续 3 次定向复现后仅更新该 PNG 并同步 manifest 理由，普通比较最终 408/408 通过。
- 发布工作树将操作反馈设置与草稿页的直接 `localStorage` 访问统一迁移到既有 `src/storage/browser-storage.ts` 边界；`npm run audit:service-boundary` 报告 459 个文件、0 个违规，相关 53 项定向单测通过。
- 发布门禁暴露排位盘面恢复期间过早调用 `move()` 的 Smoke 竞态；测试改为等待真实恢复标记清零，不增加固定延时或重试。最终 `npm run verify:release` 通过：1933/1933 单测、41/41 关键 Smoke、全部架构审计、构建与发布就绪检查均通过。
- `npx tsc --noEmit`、目标 Vitest（14 项）和 `git diff --check` 通过。
- `npm run build` 通过。
- 使用内置浏览器确认桌面设置入口、完整静态 4×4 参考页与窄屏隐藏；未启动或占用 Chrome。

## 已废弃：设置页入口接入（2026-08-02）

- 在 `palette.html` 左侧书签目录与右侧折叠区新增“操作反馈”，复用现有偏好、开关和 4×4 静态布局调试弹层；未复制功能实现或增加依赖。
- 新锚点 `#operation-feedback-settings` 复用现有展开、滚动与活动书签同步逻辑；入口和模块均在 `980px` 以下隐藏。
- 设置页内的操作反馈卡片覆盖弹窗通用半宽规则，宽度限制为 `min(640px, 100%)`，不影响其他设置弹窗。
- 定向 Vitest 共 40 项、`npx tsc --noEmit`、`npm run build` 与 `git diff --check` 均通过；内置浏览器在 `1045×837` 下确认入口与模块可见、锚点自动展开、绑定完成，卡片实际宽度为 `640px`。构建仅保留既有 `ui-preview.html` 非模块脚本警告，未调用 Chrome。

## 已废弃：iframe 真实 4×4 无撤回布局（2026-08-02）

- 删除手绘棋盘、分数和计时器参考，改为在无脚本 iframe 中直接加载 `2048.html` 与项目现有样式；固定示例方块写入真实 `.tile-container`。
- iframe 使用 `sandbox="allow-same-origin"` 且不包含 `allow-scripts`，内置浏览器确认模式为 `standard_4x4_pow2_no_undo`、`game_manager` 未启动，不读取或写入真实对局。
- 内置浏览器确认 iframe 内原生尺寸：页面容器 `750px`、棋盘 `500×500px`、计时器 `250px`；外层 1000×720 坐标画布与操作反馈层统一等比缩放。
- 为避免共享回放弹窗的 `500px` 最大宽度覆盖调试弹层，使用组合选择器限定为 `1100px`；同时为组件样式和 palette 主样式增加缓存版本。
- 定向 Vitest 41 项、`npx tsc --noEmit`、`npm run build` 与 `git diff --check` 全部通过；构建仅保留既有 `ui-preview.html` 非模块脚本警告。

## 初版页面内操作反馈与设置重构（2026-08-02，后续交互已替代）

- 完整设置页第三项改为“界面语言”；游戏设置弹窗移除语言行，保留操作反馈开关，并在底部新增全宽“完整设置”入口卡。
- 开启操作反馈后设置弹窗自动关闭，当前真实游戏页显示可整体拖动的反馈层；支持贴近计时器、屏幕右侧、恢复默认与顶部锁定，自定义位置和锁定状态持久化。
- 删除运行时 iframe、参考棋盘注入和独立布局调试弹层；`980px` 及以下继续隐藏操作反馈，完整设置入口与语言设置保留。
- 定向 Vitest 56 项、`npx tsc --noEmit`、`git diff --check` 与 `npm run build` 通过；构建只有既有 `ui-preview.html` 非模块脚本警告。
- 内置浏览器验证：1280×720 浅色下检查游戏设置卡片、完整设置入口、页面内编辑、拖动转自定义和锁定收起；390×844 浅色下检查操作反馈隐藏、完整设置入口可见、界面语言模块可见。未调用 Chrome。未更新视觉基线；当前基线工具依赖隔离 Playwright Chromium，与用户“只用内置浏览器”约束冲突。

## 屏幕右侧锚点修正（2026-08-03）

- 根因是编辑工具栏参与浮层宽度计算，导致“屏幕右侧”锚定工具栏而非键帽栈。
- 工具栏改为绝对定位附件，不再占据主体宽度；选择“屏幕右侧”时工具栏自动翻到键帽左侧，因此位置计算和拖动边界始终以键帽栈为准。

## 已否决：初版真实按键反馈（2026-08-03）

- 用户确认写死的五键示例不能作为锁定后的实际反馈。编辑态仍显示摆位示例；锁定后改为记录真实方向、字母与撤回键，最近输入从底部向上排列，5 秒无操作渐隐。
- 继续保守限定为展示逻辑，不提前接入有效／无效输入判定与统计汇总。

## 单一锁入口（2026-08-03）

- 删除设置弹窗中重复的“调整位置”按钮。锁定浮层在鼠标悬停时显示顶部锁，点击后直接解锁并展开位置工具，再次点击锁定并保存。

## 批准的完整重设计（2026-08-03）

- 用户否决当前五键、瞬间替换、细箭头和未区分有效性的实现。批准最近八键连续上推、稳定节点、单次无效红色、长按无效不记录及引擎结果驱动的数据流。
- 用户批准简约、圆润、饱满的双主题键帽：经典使用暖黑灰，雾青灰使用冷蓝灰；箭头和退格使用与粗体字母等重的自绘 SVG。
- 用户确认锁定／解锁主体坐标必须完全稳定，有效输入数和无效输入数仅加入当前局统计汇总，键帽附近不显示数字。

## 锁按钮悬停命中修复（2026-08-03）

- 根因是锁按钮位于锁定主体上方，按钮底边与主体顶边之间存在实测 `4px` 空隙；锁又只在主体 `:hover`／`:focus-within` 时显示，鼠标跨越空隙会使按钮先消失。
- 仅为锁定态锁按钮增加向下 `8px` 的透明命中桥，不移动按钮、不改变键帽主体尺寸或视觉像素；鼠标经过原空隙时仍命中同一个锁按钮。
- TDD 回归先确认结构测试失败，再加入命中桥后通过。定向 Vitest 共 35 项、`npx tsc --noEmit` 与 `git diff --check` 通过。
- 内置浏览器实测原空隙为 `4px`；在锁底部下方 `2px` 的探测点命中元素为 `.operation-feedback-lock`。透明命中修改不产生视觉像素差异，因此未更新视觉基线；未调用或占用用户 Chrome。

## 完整重设计最终实施与验收（2026-08-03）

- 输入确认链路已统一：最近输入携带唯一 ID、物理键令牌和 repeat；普通移动、撤回、重做按引擎真实结果发布确认事件，被节流覆盖或长按无效的输入不显示、不计数。八方向模式中 `Z` 为斜向移动，只有退格撤回。
- 浮层保留最近 8 个稳定节点，新键从底部进入、旧键连续上推、最旧键顶部淡出；单次无效为红色，5 秒闲置渐隐，下一次输入立即唤醒。经典为暖黑灰，雾青灰为冷蓝灰；普通键 `58×58`，退格 `96×58`，主体固定 `96×520`。
- 统计汇总新增“有效输入数／无效输入数”，仅存在于当前局 runtime 与统计面板；检索确认未进入存档、历史、回放、排行榜、主页、在线提交或 API。
- 最终边界修正：键帽令牌改由 `event.code` 识别，兼容非拉丁键盘；旧数字 `move` 载荷继续原样透传；每局 setup 发布重置事件清空上一局键帽；已开启偏好会在游戏启动脚本前安装监听；首页启动包和 nomodule loader 使用 `20260803-operation-feedback` 缓存版本。
- 定向 Vitest：15 个文件、118/118 通过。`npx tsc --noEmit`、`npm run audit:game-manager`、`npm run build`、`git diff --check` 均通过；构建只保留既有 `ui-preview.html` 非模块脚本警告。
- 全量 `tests/unit` 仅剩 2 个与本任务无关的既有失败：一个仍断言旧 `main.css` 缓存键，另一个来自既有 `achievement_unlock_showcase.css?v=...` 本地 `@import`；未为通过检查而修改用户的无关资产。
- 内置浏览器真实 `/2048.html`：连续 12 次输入后稳定为 8 键，其中 2 个无效红键；统计面板实测有效输入 9、无效输入 5；5 秒闲置后隐藏，下一次输入立即显示。经典与雾青灰均完成截图检查，`980×720` 下浮层不可见；全过程未启动或控制 Chrome。
- 两套主题的浏览器验收均使用实际 `1280×720` 视口：经典主题与雾青灰主题分别检查了键帽配色、粗箭头、宽退格键、八键上推和无效红色状态。
- 内置浏览器不能生成原生 `repeat=true` 的连续长按事件，因此长按语义由自动测试替代验证：`repeat=true, valid=true` 的每次有效位移都会计入有效输入并发布；`repeat=true, valid=false` 的无效位移不会计数，也不会发布键帽。
- 内置浏览器不提供浮层 `getBoundingClientRect()` 的直接读取接口。锁定几何使用替代契约验证：在 `1024×720` 的“屏幕右侧”场景中，编辑态 overlay/stack 为 `left: 910px; top: 100px; width: 96px; height: 520px`，锁定态完全一致；`left/top` 来自锁定前后单测，`width/height` 来自共享 CSS 契约，不作为浏览器直接测量结果。

## 终局后输入统计生命周期修正（2026-08-03）

- 根因是共享移动执行出口在 `manager.move()` 因终局返回 `false` 后仍无条件发布确认结果，导致终局后的非重复按键被继续记录为无效输入并显示红色键帽。
- 输入执行前快照终局状态；导致终局的最后一次有效输入仍正常发布，已经终局后开始的方向、撤回和重做不再发布、显示或计数。终局撤回继续执行救阵，但该次撤回不计数；恢复后的新输入重新正常统计。
- TDD 回归先复现终局后普通输入仍发布，再确认修复后普通输入和救阵撤回均不发布，同时保护最后一次致终局输入仍发布。
- 操作反馈定向 Vitest 共 13 个文件、104/104 通过；`npm run audit:game-manager`、`npx tsc --noEmit` 和 `npm run build` 通过。构建仅保留既有 `ui-preview.html` 非模块脚本警告；本次没有视觉改动，未调用 Chrome。
