# Execution Notes

## Route Deviation

- 2026-08-20：当前仓库没有 `./.trellis/scripts/task.py`。为避免改动工具链或污染其他脏工作树，按既有 `.trellis/tasks/MM-DD-slug/` 结构手工创建本任务规划目录；仅写规划与视觉对比资产，不改产品代码。
- 2026-08-20：本轮接入横幅时发现当前工作树同样缺少 `.trellis/scripts/get_context.py` 与 `.trellis/spec/guides/index.md`。为避免为视觉任务修补 Trellis 工具链，改为读取现有 `.trellis/spec/index.md`、前端边界、Smoke 与视觉规范；任务范围仍只限个人主页前端与静态素材。
- 2026-08-20：首次提取 4+8 前景层时，图像工具错误输出 1774×887 的不透明棋盘格图，而非 2172×724 Alpha PNG。该产物未复制进项目；沿用确认稿重新提取，并以尺寸与 Alpha 通道检查作为接入门槛。
- 2026-08-20：第二次 4+8 提取虽恢复 2172×724，但仍为不透明 RGB 棋盘格。为避免把伪透明素材带入产品，停止独立前景层路线，保守降为“背景板 + 四角色透明层 + CSS 光影层”；仍满足分层视差，且不增加运行时抠图逻辑。
- 2026-08-20：四角色合并提取同样未产生真实 Alpha。最终不再继续试错，采用可验证的“城市与 4+8 底景 + 0+2 透明中景 + CSS 光影”三视觉层；四个角色保留，0/2 与前景产生相对位移，且无需运行时抠图。
- 2026-08-20：首次页面接入把封面实际高度从原先约 140px 放大到 376px，并且城市、设备、4、8 仍在同一底图，只有 0+2 位移明显；用户验收判定效果差。撤回该尺寸路线，改为约 180px 的原比例封面，并新增独立远景天空，使天空、城市/4+8、0+2、光影四层分别移动。
- 2026-08-20：规划阶段建议独立腾讯 COS 媒体桶，但当前生产权威 API 是单机自托管 Node 服务，现有部署没有用户媒体 COS 配置。为避免引入无法直接部署的外部依赖，首版改为独立私有持久卷 `/srv/user-media`，不与 `/srv/replays` 混用，并通过独立存储模块隔离；未来进入多实例部署时再替换为 COS，数据库和 API 合同无需改变。
- 2026-08-20：第二套夜景横幅初稿错误沿用了素材原稿的 3:1 比例假设；实际桌面封面为流体宽度 × 180px，在当前 1489px 视口约 8.27:1，移动端 390×160 又约 2.44:1。该 3:1 稿不接入产品。路线改为可安全裁切的远景/中景/前景背景层，加四张独立透明全身角色层，由 CSS 按容器高度响应式定位，避免任何固定合成图在桌面或移动端裁人。
- 2026-08-20：夜景背景已从人物合成稿中独立重建，并裁出 2172×272（约 8:1）的桌面安全背景 `assets/profile-banner-night-background-8x1.png`；角色精灵稿仍为带棋盘格的 RGB 图、没有真实 Alpha，因此未接入页面，避免再次把伪透明素材带入生产。
- 2026-08-20：对角色精灵稿执行一次可逆棋盘格边界抠除后，得到 4 张真实 Alpha 全身层 `assets/profile-characters-v2/`；以 2172×272 背景合成了仅用于视觉确认的 `assets/profile-banner-night-composite-8x1.png`。该合成稿不替代运行时分层，四个角色文件仍可分别定位和移动。
- 2026-08-20：内置图像生成器两次无法产出合格透明层：第一次保留半透明黑场，第二次生成不透明棋盘格；两份失败产物均未复制进项目。为立即撤下割裂的伪分层效果，保守回退为桌面与移动端各一张完整合成场景，仅保留整图轻微跟随；真正独立视差层延后到取得原生分层源文件或可验证的透明输出后再做。
- 2026-08-20：再次使用内置 Image 2 后确认无参考图的独立生成可以输出真实 Alpha，而带参考图的编辑式生成仍会把棋盘格烘成不透明底色。两张失败编辑稿未复制进项目；最终只接入逐张验证过的天空、城市和近景独立素材，移除旧的同图遮罩切层方案。
- 2026-08-20：用户明确不启用单独计费的 Image 2 API，只使用内置生成。由于内置输出画布仍为 2172×724，改为在 3:1 画布内生成约 8:1 安全场景后裁成 2172×272；城市真 Alpha 层等比缩小后靠右放置，近景真 Alpha 层等比缩小并交替铺满底部，最终三层均为 2172×272，不再让页面适配 3:1 原图。
- 2026-08-21：Image 2 的整图昼间编辑会重画部分建筑与花盆，且城市透明稿再次输出不透明棋盘格。为满足“元素完全相同”，昼间天空采用生成后的日光候选，城市与前景则以原夜景 Alpha 为几何权威进行确定性色阶映射；浅色页面使用 `day-*`，夜间背景状态继续使用 `night-*`。
- 2026-08-21：首次白天版在实页中过曝且去饱和过度，绿植失去绿色。修订版降低白色占比、恢复浅蓝天空和灰蓝建筑层次，并从自然日光候选提取绿色植被与中性栏杆颜色后重新覆盖原 Alpha；昼夜几何哈希保持一致。
- 2026-08-21：自然日光候选与原城市细节并非像素级对齐，混合后出现楼体内部深浅割裂。城市层最终完全撤下候选 RGB，只保留从原夜景城市统一映射得到的灰蓝青日间材质，并重新覆盖原 Alpha；自然候选仅用于天空与前景色彩参考。
- 2026-08-21：用户明确要求白天版必须直接使用其确认的完整场景，拒绝另行生成或换色的建筑。白天模式因此撤下独立城市、前景和光效层，直接使用附件裁出的 `day-scene-8x1.png`；夜间模式仍使用原三层视差。
- 2026-08-21：按用户要求恢复白天三层视差。Image 2 仅生成天空修复层与语义粗遮罩，随后用 Pillow/NumPy 将遮罩贴回确认原图并提取原始 RGB；取消遮罩扩张后白边消失，零位移重组与原图 SSIM 达到约 0.997，再接入页面。
- 2026-08-21：白天紧贴原图的遮罩无法承受夜景同等幅度，最大位移会拉开白色裂口。为保留原图一致性，白天视差幅度降为夜景的 25%，夜景幅度保持不变；白天仍保留三层相对运动。
- 2026-08-21：城市外扩 4px、前景外扩 6px，并让天空修复区使用相同外扩范围后，白天左右极限位移不再出现白边；零位移重组 PSNR 约 47.96dB、SSIM 约 0.9985。
- 2026-08-21：用户否决从完整白天场景继续抠图，路线改为分别原生生成天空、城市、近景三张模板。城市和近景只接受生成器直接输出的真实 Alpha；后处理仅允许整体画布裁切、缩放与定位，不再制作或修补语义遮罩。
- 2026-08-21：为制作与白天像素级同场景的夜版，尝试以白天城市透明层做 Image 2 光照编辑；输出为 2140×735 的不透明棋盘格 PNG，未保留 Alpha，因此拒绝接入。保守回退为昼夜共用同一套几何与透明素材，夜间仅由 CSS 调整分层亮度、色温和光效；这能严格保证建筑、栏杆和植物的位置与轮廓一致。

## Current State

- 规划基于干净工作树 `codex/durable-auth-record-delivery-20260819@21f0467a`。
- 页面、接口、数据库和导航改动已按本任务范围实施。
- 已制作第三版交互原型，用于验证“固定概览首屏 + 用户选择代表模式”；展示成就不可在主页重复选择，严格沿用勋章墙槽位。
- 2026-08-20：核对真实成就数据流后，删除原型中的虚构成就名称，改用现有 `achievement_definitions` 中的真实 ID/名称；规划明确个人主页不得拥有第二套成就数据。

- 2026-08-20：已完成 API、前端主页和管理员头像审核界面；个人主页成就直接读取 `GET /api/user/:id/achievements`，展示槽位直接使用返回的 `showcase_slot`。
- 2026-08-20：已完成本地验证：API typecheck、3 个相关 Node 测试（28/28）、前端 build、2 个相关单元测试（8/8）和两个工作树 `git diff --check`。
- 2026-08-20：个人主页外层改为连续全视口画布；封面增加轻量分层鼠标跟随，触摸设备与减少动态效果设置下自动停用。
- 2026-08-20：用户确认 0 女星术师、2 男探索者、4 男工程师、8 女运动员为项目固定角色；定稿参考图和横幅构图约束已保存到本任务，横幅确认前不接入页面。
- 2026-08-20：已按固定角色参考生成 2172×724 横幅首稿 `assets/profile-banner-concept-v1.png`；四人身份与徽章保持正确，左侧保留资料区，尚未接入产品页面。
- 2026-08-20：根据反馈生成 `assets/profile-banner-concept-v2.png`，只调整 8 的构图意图：人物缩小、向内收并改为朝队伍方向的自然三分之四侧向跑姿；仍未接入产品页面。
- 2026-08-20：否决首版高封面后，正式页改为桌面 180px、移动端 160px 的横幅；远景天空、城市/装置/4/8、透明 0/2 人物和 CSS 光影使用不同位移幅度。0/2 人物层单独缩放，避免为容纳原始 3:1 插画而抬高页面。

## Local Verification Notes

- 本地 API 使用 `127.0.0.1:55432/2048_next_test` 测试数据库；为验证新增字段，在该本地库执行了 `0031_public_player_profiles`，未触及生产数据库。
- 内置浏览器通过 `http://127.0.0.1:4186` 验证真实 `user.html`：概览、模式成绩、游戏记录、成就四个标签和模式筛选均可用。
- 内置浏览器在 1489×1116 下验证：页面无横向溢出，导航、主画布与底部背景连续；封面移动约 4.96px 时资料层保持无 transform。新增 TypeScript 通过 `npx tsc --noEmit`。
- 内置浏览器在 1489×1116 下复验重做横幅：实际高度 180px、横向溢出 0、三张分层素材均载入，昵称保持白色；天空、城市、0/2 人物与光影沿用独立位移变量。针对性单测 2/2、`npx tsc --noEmit` 与 `git diff --check` 通过。
- 2026-08-20 最终保守修正：内置浏览器在 1489×1116 下确认桌面完整合成场景载入、横幅高 180px、横向溢出 0；在 390×844 下确认移动端完整场景载入、横幅高 160px、四角色仍在同一构图且横向溢出 0。桌面仅整图轻微跟随（实测 transform 发生变化），不再使用背景切片或独立人物贴层。针对性单测 2/2、`npx tsc --noEmit` 与 `git diff --check` 通过。
- 2026-08-20：按用户最终选择，本版封面只保留无人物的 2172×272 夜景背景；桌面和移动端均不再加载任何包含 0／2／4／8 角色的合成图或独立角色层。
- 2026-08-20：用户澄清“角色与背景不在一个图层”指画风与光影违和，而非遮罩空洞。本版继续保持无人物背景，并将同一张夜景按天空、城市、近景三段做大幅视觉摆动；三层全部来自同一素材，避免再次混入不同画风角色。
- 2026-08-20：内置浏览器在 1489×1116 下验证大幅摆动：鼠标从封面左侧移至右侧时，天空位移差约 16px、城市约 25px、近景约 47px；角色层数量为 0，横向溢出为 0。针对性单测 2/2、`npx tsc --noEmit` 与 `git diff --check` 通过。
- 2026-08-21：内置浏览器在 1489×1116 的浅色页面验证白天三层均加载、横幅高 180px、横向溢出 0；昼夜城市 Alpha MD5 均为 `57533040b92dfc5d8362b8a022d8d067`，昼夜前景 Alpha MD5 均为 `7b81a52ae50760b2ac207b86ebb99bc4`。针对性单测 2/2、`npx tsc --noEmit` 与 `git diff --check` 通过。
- 2026-08-21：改用分别原生生成的白天天空、城市、前景模板后，三张正式素材均为 2172×272，城市与前景具备真实 Alpha。内置浏览器确认桌面横幅高 180px、横向溢出 0；左右极限移动时天空、城市、前景横向位移差分别约 15.95px、25.06px、47.84px，未再出现建筑竖条、楼体切断或透明层漏底。
- 2026-08-21：昼夜同场景方案已接入：浅色与夜间状态读取完全相同的天空、城市、前景 URL，三层渲染矩形也完全一致；夜间仅增加分层 `filter` 与现有光效。内置浏览器在 1489×1116 夜间页面确认横向溢出 0，建筑、栏杆和植物没有位移、替换或重新生成；预览标签保留为夜间状态供验收。
