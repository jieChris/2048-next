# 实施顺序

> 权威需求为 `prd-v3-corrected.md` v3.1；本文件只记录可执行顺序，不改变验收合同。

## P0（当前收口）

1. 完成 P0a 公开主页、双 ID 安全边界、代表模式和展示成就。
2. 完成 P0b 简介 150 code points、DeepSeek 文本审核、outbox、限频/封禁、密钥托管、本人历史和管理员操作。
3. 完成 OpenAPI/生成类型、全量测试、迁移幂等、真实本地 API 内置浏览器矩阵和两路只读对抗审查。
4. 未解决 P0/P1 清零后才能进入 P1；未经用户要求不提交、推送或部署。

## P1（下一阶段）

1. `2048-game-api`
   - 增加 `0037` 背景变体/场景/默认指针迁移与配置项。
   - 增加最小独立背景存储/真实 PNG 校验模块；复用现有事务、管理员权限和审计。
   - 实现变体上传/列表、场景配对/列表、发布/归档/默认切换、公开目录/分层资源端点。
   - 先写校验、路径、状态机、事务回退和归档回退测试，再接路由。
2. `2048-next`
   - 扩展本任务 OpenAPI 并重新生成类型。
   - 管理台新增“主页背景”操作页；一次上传一套三层变体，可保存多套并指定完整昼夜场景。
   - 个人主页编辑器只显示白天预览；渲染根据统一显示模式自动请求 day/night 三层。
3. 验证
   - 运行目标测试、两仓 typecheck、前端 build、服务边界和 `git diff --check`。
   - 用内置浏览器连接真实本地 API 验证上传、配对、发布、默认、归档回退、用户选择、昼夜切换和四档视口。
   - 独立代理做安全/事务审查和 PRD 合同审查，清零 P0/P1。

## P2（P1 独立收口后）

1. 按已冻结合同实现：批准头像不回填 `users.avatar_url`，新头像只通过 `profile_current.avatar_revision_id → avatar_objects` 发布。
2. API 增加头像对象/提交迁移、私有存储与 `sharp` 处理、视觉模型适配、限频和崩溃对账。
3. 前端接头像上传、本人状态和管理员图片复核；沿用 P0 审核历史与重试接口。
4. 跑真实格式/尺寸/动画/私有对象/不能自审/7 天限频/旧头像保留/崩溃恢复测试与内置浏览器端到端。

## 交付纪律

- 两仓只在当前本地 `main` 工作树继续，不新建分支/worktree，不 pull 覆盖本地代码。
- 保留所有既有未提交修改；每期验收完成后先向用户报告，再按明确指令 commit/push/deploy。
