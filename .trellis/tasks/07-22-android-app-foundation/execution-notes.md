# 执行记录

## Route Deviation

- 2026-07-26 13:20 +0800，non-debuggable release 真机性能采样最初把 Perfetto 启动与 ADB 输入拆在两个工具回合，工具调度使输入相对 trace 晚约 22 秒；另两轮由主机反复建立 ADB shell 导致输入流出现 1.5–3.5 秒空档，均不足以证明不少于 30 秒连续棋盘动画。采用最保守回退：不修改 App、动画时长或验收阈值，把 Perfetto、延迟 2 秒后的设备端单一 shell 手势循环和刷新率检查放入同一个并发命令；最终 trace 完整覆盖 33.541 秒输入窗口。无效样本继续保留并明确标记，不用终局后的静止时间、自动重开或过滤 60Hz 平台伪造结果。
- 2026-07-26 12:32 +0800，ColorOS 12 在 App 未声明通知权限时仍于首次启动 non-debuggable release 测试包弹出厂商级“允许 2048 NEXT 发送通知吗？”对话框。`dumpsys package` 与合并清单继续确认平台权限只有 `android.permission.INTERNET`，另有 AndroidX 应用私有 signature permission；没有 `POST_NOTIFICATIONS`、`VIBRATE`、存储或后台权限。测试中选择“拒绝”，不为消除 OEM 对话框新增权限、通知通道或厂商 SDK；该提示记录为 ROM 行为，不能表述为 App 主动申请通知权限。
- 2026-07-26 11:48 +0800，真机采样结束后按计划执行 `svc power stayon false`，但一加 Android 12 ROM 静默忽略该 shell 调用，`stay_on_while_plugged_in` 仍为 `7`；直接写 global setting 又被 `WRITE_SECURE_SETTINGS` 拒绝。采用最保守回退：不 Root、不授予 shell 特权、不绕过系统安全，打开系统“开发者选项”，通过可见的“充电时屏幕不休眠”开关恢复关闭，并核对 switch `checked=false`、global setting `0` 后返回 App。未改变其他开发者选项或系统刷新率设置。
- 2026-07-26 03:26 +0800，为补齐本机 API 36 模拟器矩阵，首次通过旧 `sdkmanager` 下载 `system-images;android-36;google_apis;arm64-v8a` 时，Google 仓库在连续多轮等待后仍未返回 package manifest。采用最保守回退：中止旧进程，不使用第三方镜像、未知来源 system image 或手工拼装 AVD；改用 Android Studio 当前推荐的官方 `android sdk` CLI 后成功从 `dl.google.com` 安装同一官方镜像，并建立 `2048_NEXT_API_36` AVD。路线偏离只限于官方 SDK 工具的迁移，未改变代码、镜像来源或验收标准。
- 已批准设计原计划使用 Capacitor 官方 Haptics 插件，但核对 8.0.2 Android 源码确认其通过 `Vibrator/VibrationEffect` 直接振动，无法可靠遵守用户已确认的系统“触觉反馈”开关。采用最保守回退：移除未使用的官方 Haptics 依赖及其 `VIBRATE` 权限，改为单方法 `Next2048SystemHaptics` 原生桥，仅调用 Activity decor view 的 `performHapticFeedback`，不设置 `FLAG_IGNORE_GLOBAL_SETTING`，由 Android 系统决定是否执行；App 仍只在合并、里程碑和结束反馈时调用，不对每次滑动触发。影响是已批准的 Capacitor 官方插件集合从五个缩减为四个，并增加一个仓库内可审计的无权限桥；未新增第三方依赖、权限或后台能力，若未来官方插件提供可验证的系统设置遵循能力，可删除该桥并恢复官方实现。
- 为闭合阶段 2.2B 的真实 PostgreSQL 门禁，本机通过 Homebrew 安装 `postgresql@17` 17.10；公式下载与安装完成，但 post-install `initdb` 被 macOS 系统策略拒绝加载 `icu4c@78` 的动态库，报告代码签名无效。采用最保守回退：不关闭系统安全策略、不对第三方动态库强制改签、不把 mock 当真实数据库；最终使用隔离的受信 PostgreSQL 17.10 运行时 `/tmp/2048-next-pg17-runtime` 建立仓库外测试集群并完成迁移、回填、索引和账号删除真实验证。未修改系统安全策略、未启动生产服务、未接触生产数据。
- 阶段 5 的 API 29 基准镜像使用 Android WebView 91；Playwright `connectOverCDP` 建立浏览器级连接时会自动调用该旧 WebView 不支持的 `Browser.setDownloadBehavior`，并以“Browser context management is not supported”中止，无法用浏览器级封装采集当前原生包证据。采用最保守回退：继续连接同一 debug WebView 暴露的页面级 DevTools socket，直接使用原始 CDP 的 `Runtime`、`Network` 与 `Input` 域完成 DOM、IndexedDB、真实触摸和零业务网络断言；不修改 App 源码、WebView 配置、原生资产或验收条件，也不以桌面 Chromium 结果替代 API 29。
- 仓库存在 `.trellis/spec` 与 `.trellis/tasks`，但缺少技能约定的 `.trellis/scripts/task.py`，无法执行标准任务创建命令。采用最保守回退：按现有 `MM-DD-slug` 目录格式手工创建本规划任务；未启动实现，也未修改产品代码。
- `trellis-before-dev` 约定的两仓库 `.trellis/scripts/get_context.py` 与 `.trellis/spec/guides/index.md` 均不存在。采用最保守回退：手工枚举并完整阅读前端现有三份 spec、后端现有两份 spec 与两仓库 `AGENTS.md`；未跳过任何已存在规范。
- 已批准设计原写为“撤回恢复 RNG 步号、计时和回放游标”，但冻结既有 Web 行为与服务端 verifier 时确认 RPL1 将撤回视为动作：有效局面回退，已经经过的时间与动作流不回拨，并消耗一个 RNG action step。为避免破坏既有回放、排位验证和跨端确定性，采用最保守兼容规则并以共享黄金 fixture 固化；影响仅是撤回后的下一次出块不会复用被撤销分支的 RNG 位置，产品可见棋盘/分数/步数等状态仍正确恢复。
- 已批准 Android 权限基线预期合并清单只有 `INTERNET` 与 `VIBRATE`，但 Capacitor 8.4.2 使用的 AndroidX Core 1.17.0 会为 `ContextCompat` 的非导出动态广播自动合并 `${applicationId}.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION`，并以 `protectionLevel=signature` 声明/自用。真实 debug APK 的 `apkanalyzer` 已确认该来源；强行通过 manifest removal 删除会破坏 API 29 兼容并削弱同签名隔离。采用最保守回退：允许且仅允许 `INTERNET`、`VIBRATE` 以及与当前 debug/release application ID 完全匹配、保护级别严格为 signature 的这一项自有权限；审计继续拒绝通知、存储及任何其他权限。影响是原“只有两项”的字面计数变为两项平台权限加一项应用私有签名权限，不扩大对系统或其他应用的数据访问能力。
- API 29 基准镜像自带 Android WebView 91；Capacitor 8.4.2 内置 `SystemBars` 会在该旧 WebView 的 `document.documentElement` 创建前抢先注入安全区 CSS，断网冷启动稳定产生 `Error injecting safe area CSS`，但随后页面仍正常加载。全局关闭会破坏 Android 15/16 强制 edge-to-edge 的安全区，直接修改 `node_modules` 又会削弱可重复安装。采用最保守分层回退：生成配置继续固定 `SystemBars.insetsHandling=css`，Android 15+ 沿用官方 CSS insets；`MainActivity` 仅在 API 29–34 的运行时配置中关闭该注入，并由已批准的官方 `StatusBar` 以 `overlaysWebView=false` 将内容放在状态栏下方。移动 CSS 同时消费 Capacitor `--safe-area-inset-*` 与标准 `env()` fallback。影响仅是旧 Android 使用 fitted content、新 Android 使用 edge-to-edge；不修改游戏页面结构、不增加权限或依赖。
- 阶段 4 开发前的 IndexedDB v1 尚未进入任何 APK、公开版本或用户数据，但其 `cache` 单字段主键不能从数据库层保证多账号同名逻辑键隔离。采用发布前基线修正：物理版本升至 v2，在单个原生 `versionchange` 事务内把测试 v1 cache store 改为 `[ownerKey, cacheKey]` 复合主键并保留原行；任一复制失败会由 IndexedDB 原子回滚到完整 v1，未来 row schema 不改写。该预发布迁移因此包含一次 store 重建，而不是发布后的纯加法升级；首个公开版本直接创建 v2，后续正式 schema 仍严格执行 expand/contract，不以此先例重建已发布数据。
- 阶段 3.3 计划文字写为 Kotlin 测试，但受控 `app` module 与 `MainActivity` 当前均使用 Java；只为约 200 行安全桥引入第二种 App 源码语言会增加插件、编译与审计面。采用最保守回退：桥和 Android 仪器测试均使用 Java，继续直接调用相同 Android Keystore API，并限定所有原生门禁为 `:app:*`。功能合同、API 29 下界与加密方案不变，未增加依赖。

## 调查记录

- 当前请求是复杂的全新移动前端规划，需要在 PRD 确认后补齐 `design.md` 和 `implement.md`，并由用户审阅后才能进入实现。
- 用户已确认接受 Capacitor，但最终产品必须使用 App 专属页面、入口和交互，不是线上网页壳或现有 Web 的简单移动适配。
- 当前 Web 的 legacy 脚本并非全部无效：`play` 清单仍主动加载 91 个脚本；同时审计明确阻止 56 个已退役 runtime 返回现代清单。App 基底不得把这套加载图整体复制过去。
- 现有后端与数据边界适合增加移动客户端；主要规划风险位于无 DOM 游戏核心、API 契约冻结、移动生命周期、安全存储、持久化 outbox 和 Android 发布链路。
- 用户确认首个公开版本只覆盖玩家核心产品；管理后台、接力工具、2K 监控、完整色板编辑和实验模式延后为独立模块。
- Capacitor App 的页面与静态资源随 APK/AAB 本地发布，不需要新增移动前端托管服务；在线产品能力继续复用现有 `2048-game-api`。
- 用户明确长期目标为 Web、Android App、微信小程序三端并存，共享同一后端、账号和云端业务数据；当前 Android 基底规划必须同时定义可供小程序复用的核心与契约边界。
- 代码扫描未发现 `openid`、`unionid`、微信登录或通用第三方身份映射模型；三端统一账号需要在后端新增平台身份到权威用户 ID 的映射，而不是为小程序复制用户表。
- 用户确定先做 Android App，小程序延后；游客只可游玩一个 4×4 经典玩法，其他模式在开始前要求登录。
- 当前模式目录把无撤回玩法命名为“标准版 4×4”，把可撤回玩法命名为“经典版 4×4”；用户已确定游客开放无撤回的 `standard_4x4_pow2_no_undo`。
- App 首页信息架构开始收敛。现有 Web 首页同时承载对局、用户、公告、统计、新游戏、回放导出、练习盘、模式、设置、计分和计时信息，移动端不直接继承这一入口密度；当前最高优先级产品决策是首页采用“快速开局页”还是“综合大厅”。
- 用户确认 App 本地页面和游客对局不应像线上 Web 页面切换一样等待服务器；在线数据采用页面先呈现、后台异步刷新的边界，强在线操作除外。
- 用户确认 App 顶层采用“首页 / 模式 / 记录 / 我的”四项底部导航；对局、结算、回放和登录注册流程隐藏，排行榜从记录页或对局等明确入口打开。
- 用户确认游客标准局历史仅保留本机并标记为游客记录；登录不自动上传或并入账号云历史，游客记录不参与排行榜和成就，登录后的新记录才按账号规则同步。
- 代码证据显示 `2048-next` 已使用 Vite + TypeScript 且未安装 UI 框架，共享核心也在同一仓库。按最小依赖与原子迁移原则，首版采用同仓库隔离的移动端构建和原生 DOM/CSS，不新增 React，也不另建移动仓库。
- 用户确认首版只交付标准 4×4 不可撤回、经典 4×4 可撤回和标准 3×3 不可撤回三个模式；游客只可进入第一项，其他既有模式不显示占位入口并延后增量迁移。
- 用户确认 App 使用全新的本地数据空间，首版不迁移 Web 浏览器 localStorage/IndexedDB 历史；登录只读取已有云记录，现有文件导入能力延后。
- 用户确认首版最低支持 Android 10（API 29）；Android 9 及以下不进入兼容范围。开发机已检测到 Android Studio 2026.1、SDK Platform 36.1、Build Tools 36.0.0 和 platform-tools。
- 用户确认首版锁定竖屏，不支持手机横屏页面；方向变化不得触发对局状态丢失。
- 用户确认首版正式支持安卓手机；平板与折叠屏允许安装并采用居中的手机宽度兼容布局，但不提供专属大屏设计，也不进入首版正式体验验收范围。
- 用户明确要求支持 90+ FPS，而不是把 60 FPS 作为上限。代码检查表明现有部分运行时使用 `requestAnimationFrame`，但 App 不复用 legacy 动画清单；移动动画需从基底采用基于时间的 RAF，避免固定 60Hz 假设。
- 用户确认自动跟随系统 60/90/120Hz，并将 90+ FPS 纳入首版验收；省电、温控和系统设置导致的降频允许优雅回退。性能矩阵增加至少一台 90Hz 或 120Hz 真机。
- 用户确认首页采用快速开局定位：未完成对局优先继续，否则开始标准 4×4；仅保留首版模式和最近记录，不建设依赖网络的综合大厅。
- 用户确认已登录设备断网仍可玩三个首版模式；在线默认排位，排位会话不可用时自动降为有明确标识的离线普通对局，联网后只同步普通历史。后端核对确认 `/api/records` 无排位令牌时写入 `source: normal`，排行榜与成就过滤排位来源，无需新增离线历史接口。
- 用户确认同一进程从后台恢复时直接回到活动对局；被系统结束进程后的冷启动先进入首页，通过“继续游戏”恢复持久化状态，不自动跳入棋盘。
- 用户确认 Android 显示名称为 `2048 NEXT`，application ID 为 `cn.next2048.app`；该包名与 `2048next.cn` 是品牌命名关系，API 与 App Links 仍分别配置。
- 用户明确 App 不采用封闭内测模式，而按正常公开产品运行。后端核对确认 beta gate 默认关闭且公开邮箱注册/登录链路已具备；App 不加载 Web beta 准入页面，发布环境需保持玩家 API gate 关闭。
- 公开发行合规核对未发现正式隐私政策、账号删除 API 或账号删除网页。已将 App 内删除入口、公开网页、身份复验和 Token 撤销列为发布阻塞项；具体云记录与排行榜处置需要产品确认。
- 用户确认账号删除不立即执行，而采用 72 小时冷静期：申请后立即退出、撤销旧 Token并隐藏公开数据；期限内只有邮箱密码真实登录可原子取消，后台请求不能取消；到期未登录则彻底删除资料、记录、回放、排行榜和成就，不保留匿名成绩。
- 用户确认首版只跨端同步账号资料、已结束记录/回放、排行榜和成就；未结束对局仅保存在产生它的设备，服务端排位 checkpoint 不作为跨设备续玩产品能力。
- 用户否决“每台设备只保留一局”的方案，要求不同模式可以并行存档；首页继续最近关闭的未结束模式。每个模式一个存档，点击模式卡自动加载存档或创建新局，模式页不提供“新游戏”次按钮；重新开始只在对局内部触发。
- 用户确认对局内重新开始的防误触规则：已有有效移动且未结算时二次确认；尚未移动或已结算直接新开，只覆盖当前模式存档。
- 用户确认正常退出或切换账号时不保留该账号的本地多模式存档、账号缓存或 outbox；已同步云数据与独立游客数据不受影响。后续已进一步确认：先有界尝试上传，失败时必须由用户选择取消退出或明确清除。
- 用户确认退出时先尽力上传；若仍存在未上传记录或未结束模式，显示数量并允许取消，只有明确选择“仍然退出并清除”才原子删除本机账号数据，不静默丢失也不无限阻塞。
- 用户确认“记录”底部页使用“历史 / 排行”两个内部页签；回放从历史详情进入，不设独立回放页签，登录后可区分账号云记录与本机游客记录。
- 排行榜语义复核发现此前建议有误：现有产品可见指标为“分数 / 竞速”，最少步数筛选已被 smoke test 明确移除；`steps ASC` 仅存在于后端分数榜内部 tie-break。App 排行榜范围需按这一事实重新确认。
- 继续追踪确认“盘面和”并未消失：它是本地历史和用户个人记录的排序字段，Node `/user/:id/records` 支持 `sort_by=board_sum`；但公开 `/leaderboard` 仅接受 score/speed。需由用户决定 App 沿用此分层，还是新增全站盘面和榜。
- 用户确认沿用现有分层：全站排行保留分数/竞速；历史和个人记录保留时间/分数/盘面和排序；不新增全站盘面和榜。
- 边界纠正：上一轮把独立 `2048-ranked` 全国榜误归为 2048 NEXT Web 产品组成部分，这是错误。架构规范和运行调用均表明 `2048-next` 与 `2048-ranked` 是两个独立前端产品，只共享 `2048-game-api` 的账号与后端权威；2048 NEXT Android App 只使用自身 `/leaderboard` 分数/竞速榜，不接入全国榜。相关错误表述和开放问题已从 PRD 删除。
- 继续核对 2048 NEXT 自身榜单发现，API 不返回名次，Web 前端直接用分页数组序号显示排名；后端用未完整展示的用时、步数和日期拆开相同公开指标。是否改为公开并列属于产品规则，已作为单一开放问题保留。
- 用户否决并列名次，确定分数榜保持唯一连续排名；同分按首次取得该分数的对局结束时间升序，更早取得者在前。规划采用 `ended_at` 表达实际取得时间，并以稳定用户 ID 处理时间戳也完全相同的极端情况；整局用时、步数和 `updated_at` 不再作为同分裁决。竞速榜同毫秒规则仍待单独确认。
- 用户确认竞速榜也保持唯一连续排名；目标方块毫秒时间相同按首次取得时间升序，更早取得者在前，稳定用户 ID 仅处理时间戳也完全相同的极端情况。随后核对发现游戏页 TOP 10 已有总/日/周/月四周期，而完整 Web 排行页仅显示总榜；App 周期范围成为下一项产品决策。
- 用户确认 App 排行榜首版保留总榜、日榜、周榜和月榜四周期，并提出排行榜应作为独立弹出窗口，而不是沿用 Web 计时区域中的展开模式。该决定替代此前“记录页历史/排行内联切换”的描述；弹窗采用全屏模态还是较小浮窗仍待确认。
- 用户确认采用覆盖主内容与底部导航的 App 内全屏模态排行榜。Android 返回键和顶部关闭只关闭模态，来源页面状态保持不变；下一项需确定活动对局中是否提供该入口以及计时规则。
- 用户确认活动对局中保留排行榜入口；弹窗期间屏蔽棋盘输入，但计时和排位会话继续，不能暂停竞速成绩。排行榜分支的入口、窗口、周期和同成绩顺序已经收敛，下一项转向会影响所有页面文案与布局的首版语言范围。
- 用户确认 App 首版支持简体中文与英文，首次跟随 Android 系统语言，其他语言回退中文，并允许在设置中手动切换。实现采用移动端集中式字典，不复用分散的 Web legacy 翻译运行时。下一项回到首页，确定唯一在线摘要卡片的内容。
- 用户明确首页不需要排行榜或成就摘要卡，也不保留在线占位；首页因此不需要为这两类数据发起网络请求。下一项进入全局浅色/深色外观范围。
- 用户确认首版支持跟随系统、浅色和深色三种外观，默认跟随系统，手动选择持久化。移动端使用一套设计 Token 切换外观，不复制页面；下一项确定 App 专属视觉与现有品牌元素的保留边界。
- 用户确认保留 `2048 NEXT` 品牌、青金角色和数字块等级识别，其余界面全部重新设计；首版不带 Web 主题选择器或色板编辑器。现有 Web 仅有默认关闭的约 3 MB 背景音乐且没有正式触觉/合并音效链路，下一项确定 App 的声音与触觉范围。
- 用户确认首版采用默认开启的轻量短音效和克制触觉，两者可独立关闭；背景音乐保留但默认关闭。音频与触觉通过核心 effects 的平台适配层实现，不进入游戏规则；Android 只打包一种 BGM 编码。下一项补齐账号彻底删除后的邮箱重注册规则。
- 用户确认账号彻底清理完成后允许原邮箱重新注册，但必须产生新的用户 ID，旧数据不可恢复或关联；冷静期内邮箱仍被原账号占用。同步清理 PRD 中已经被后续决策覆盖的 beta 与语言待确认表述。下一项确定首次启动的隐私同意与纯离线游客路径。
- 用户确认首次启动采用单页隐私选择：同意后启用联网能力，或仅离线体验游客标准 4×4；未同意前不发起业务网络请求或初始化第三方统计，首次进入联网功能时再次请求同意。政策正文随包可读并按实质版本管理。下一项确定首发渠道与更新链路。
- 用户确认首发由官网提供签名 APK，同时持续产出商店兼容 AAB；首版不接商店 SDK 或应用内更新框架。正式签名、校验值和同签名覆盖升级进入发布基底要求。下一项确定云历史与成就的离线快照策略。
- 用户确认有界缓存最近加载的云历史页面和成就摘要，离线只读并显示最后更新时间；退出/切换账号清除快照，游客本地历史保持独立。下一项确定账号云记录与游客本地记录各自的删除语义。
- 用户确认账号云记录沿用 3 天软删除/恢复并提供最近删除筛选，游客本地记录二次确认后永久删除；离线云快照不允许执行删除。成就审计发现目录包含 Web 彩蛋、404、手工授予和未移植模式内容，下一项确定 App 对已获得与未获得成就的展示边界。
- 用户确认全部已获得成就跨端显示，未获得列表只展示首版 App 可完成项，隐藏成就获得前保持隐藏；后端需要提供明确客户端可完成元数据。下一项确定首版是否引入 Android 通知权限与推送链路。
- 用户确认首版不做推送或系统本地通知，不申请通知权限；同步、成就、网络和账号状态仅用 App 内提示，账号删除配合邮箱确认。下一项确定首次进入棋盘时的一次性操作提示。
- 用户否决首次操作提示，App 不加入首次启动轮播、首次对局遮罩、手势动画或强制教学，进入棋盘后直接操作。代码库没有现成 App 崩溃上报链路，下一项确定稳定性诊断的联网与隐私边界。
- 用户确认完成隐私同意后自动上报脱敏的崩溃与严重错误，但不采集页面点击、游戏行为或广告分析数据，也不引入 Sentry、Crashlytics 等第三方统计 SDK。首版采用直接提交到 `2048-game-api` 的最小诊断接口，禁止上传邮箱、昵称、Token、棋盘、回放和移动序列；仅离线体验绝不上传，设置中允许关闭自动诊断并手动导出本地诊断。下一项转入核心对局与结算流程。
- 代码核对确认 Web 默认在首次合成 2048 时显示“你赢了 / 继续游戏”，关闭胜利提示设置后则自动继续。用户确认 App 不继承该分支设置：首次合成 2048 自动继续，只显示非阻塞里程碑提示；计时、排位会话、存档和棋盘输入保持连续，真正无合法移动时才结算。下一项确定主动结束未完成对局是否形成历史。
- 代码核对确认现有后端跳过非 `game_over` 记录并验证真实终局。用户确认 App 未结束对局不形成历史：返回键或离开只保存存档并回首页，重新开始视为放弃当前局并仅覆盖当前模式存档；两者均不写本地/云记录、不进入 outbox，首版不新增“主动结束并保存成绩”。下一项确定经典可撤回模式在无路可走时先允许撤回还是立即固化结算。
- 代码核对确认 Web 可撤回模式的终局层仍提供撤回，但现有持久化会在终局 actuate 时立即提交，直接复用会造成“先提交、后撤回”冲突。用户确认 App 改为待结算终局：有可撤回状态时显示“撤回继续 / 结束并结算”，选择结算或已无法撤回时才固化记录；无撤回模式直接结算。待确认状态需可持久化恢复，不能自动结算。下一项确定结算页的必要信息与动作。
- 用户确认 App 最小结算页展示模式、分数、最大方块、用时、步数、个人最佳/成就和异步上传状态；主操作“再来一局”，次操作“查看回放 / 返回首页”。不重复放置模式选择、排行榜等大厅入口，上传不得阻塞页面展示或离开。下一项确定首版回放分享与文件导出边界。
- 代码核对确认 Web 当前可复制回放代码、下载 `.txt`、打开回放页，历史另有 `.json + .txt` 导出，但仓库没有 Android 系统分享实现。用户确认 App 首版只分享/保存已结算记录的一种版本化回放文件，三个入口复用同一动作；不导出半局、不展示原始代码、不做公开链接或文件导入。产品访谈问题至此收敛，下一步清理 PRD 并编写技术设计与实施计划。
- 已清理 PRD 中被后续决定覆盖的待确认措辞，产品范围无剩余开放问题。
- 技术设计并行审计确认：现有 `EngineSession.move()` 不执行真实棋盘移动，App 页面之前必须补齐无 DOM 移动、确定性出块、撤回和回放闭环；经典模式还存在表面 90/10 配置之外的高位特殊出块规则，已纳入黄金 parity 门槛。
- App 构建采用 `mobile/ + vite.app.config.ts + dist-app`，不调用 Web Vite legacy 复制插件。目标模块采用小 interface 的 Game Session、App Database、Online Sync 与 Android Platform seam，不增加 React、路由库、手势库或通用插件系统。
- 本地关键数据选择新的 IndexedDB 而非 SQLite：现有仓库已有升级/事务模式，首版数据规模无需原生数据库插件。Token 与排位 session Token 使用最小 Android Keystore AES-GCM bridge；回放只写 Cache 后交给官方 Filesystem/Share 插件，不申请外部存储权限。
- 后端联调阻塞项已落入设计：OpenAPI 漂移、最近删除越权、排行榜权威 rank/tie-break、canonical `ended_at`、成就客户端元数据、auth version + 72 小时删号、最小诊断接口和 Capacitor CORS。
- Android 审计基线：Android Studio 2026.1 与 SDK 36.1/Build Tools 36 已安装；终端当前是 JDK 17、Node 24，实施时需按 Capacitor 8/现有 CI 统一为 JDK 21、Node 22，并补 API 29 ARM64 AVD。系统 Gradle 不需要安装，使用 Wrapper。
- 已创建 `design.md` 与 `implement.md`。实施计划分为核心闭环、后端合同、移动构建、存储生命周期、游客离线切片、账号在线、剩余功能和发布候选八个阶段；仍等待用户审阅批准，未初始化 Android 工程或修改业务代码。
- Trellis 规划一致性与门禁复核最初发现：服务端回放耐久性、公开删号网页、隐私延迟同意、云删除后的榜单重算、跨 Keystore/IndexedDB 清理语义、生产 `Backend Ready`、双仓库隔离、auth version 前向回滚、schema 回发兼容、Play 签名和 release 高刷证据尚不够可执行。现已逐项补入 PRD、技术设计和实施计划。
- 退出账号明确采用“立即不可见 + 可恢复续清”的逻辑原子协议，不虚构 Keystore 与 IndexedDB 之间不存在的物理事务；计划为清理每一步增加强杀故障注入。
- 现有 ranked 历史中缺少可信服务端开始锚点的少量记录无法还原真实本地完成时刻。规划采用可审计保守 fallback：只对这些旧记录使用最早可证明的服务端接收时间并记录来源；新记录不得使用 fallback。
- 为同时保证新排位时间锚点与本地输入性能，规划改为在棋盘展示前用稳定 operation ID 幂等创建 session 并冻结 `started_at/seed/token`；安全保存后才展示 ranked 棋盘，失败直接进入 normal。棋盘可操作后首步与后续动作均不等待网络，仍受 ≤50ms 输入预算约束。
- 最终两路独立只读复核均通过：未发现剩余审批阻塞、产品矛盾、开放问题或新增发布门禁缺口。
- 2026-07-23 03:29 CST，用户明确批准 PRD、技术设计、实施计划及两项保守规则，并要求开启 goal 模式持续推进。目标已创建；生产部署、Play 上传与公开发布仍保留独立授权门禁。
- 阶段 0 记录基线：前端 `e1e7946f369124fdb5f67a391f41e82709d5b210`，后端 `177c64cff2d04173a8556bf904974f1a2a92fbbe`。两个原目录均有其他任务的未提交修改，未被带入本任务。
- 已建立干净 worktree：前端 `/Users/a19/Documents/2048-Next/.worktrees/2048-next-android-app`（`agent/android-app-foundation`），后端 `/Users/a19/Documents/2048-Next/.worktrees/2048-game-api-android-app`（`agent/android-app-backend`）。四份任务文档逐文件 SHA-256 与原目录完全一致。
- 两仓库新增 `.nvmrc` 固定 Node `22.22.2`；阶段 0 命令显式使用该版本。Android Studio `2026.1`（build `AI-261.25134.95.2612.15822958`）与内置 JBR `21.0.10` 已验证；`ANDROID_HOME`/`ANDROID_SDK_ROOT` 固定为 `/Users/a19/Library/Android/sdk`。已安装 Command-line Tools `22.0`、Platform `android-36` rev 2、Build Tools `36.0.0`、Emulator `36.6.11`、Platform Tools `37.0.0` 与 API 29 Google APIs ARM64 system image rev 13。
- 已建立 `2048_NEXT_API_29`（Pixel 3a、Android 10/API 29、`google_apis/arm64-v8a`）竖屏 AVD，并完成冷启动到 Android 系统桌面的验证。此时尚无 Android App 工程，因此该证据只证明阶段 0 SDK/模拟器环境，不宣称 App 已冷启动。未修改 shell rc；后续命令继续显式导出 Node/JAVA/Android 环境。
- 已按 ADR Gate 新建单一合并 ADR：`docs/adr/0001-android-app-foundation.md`；未增加模板、ADR 索引或第二份后端 ADR。
- 阶段 0 当前低保真审阅候选为 `android-app-wireframe.html`（SHA-256 `3c302a08d880cd342c11accc45830ec7de9bcd484f089b584e588415de0e7274`），页面清单为 `docs/ui-page-inventory.csv`（SHA-256 `c6862e1cb39b915dfb624c0e1b31a5998dee2a6a3371cfffe51c9970515d5ae5`），专项 Smoke 为 `tests/smoke/pages-android-app-wireframe.smoke.spec.ts`（SHA-256 `284b61a64318e489adda5075f4bad05408cef90adb129688cbd9602031f208aa`）。线框为 source-only 单文件，没有创建 `mobile/`、增加依赖或修改 Vite/package 配置。
- 线框审批状态：`approved`。2026-07-23 09:35 CST，用户明确回复“批准当前低保真线框”，批准对象为 SHA-256 `3c302a08d880cd342c11accc45830ec7de9bcd484f089b584e588415de0e7274`。阶段 0 门禁完成，允许进入阶段 1。配色、字体、图标和动效仍按计划留到正式移动视觉实现，不改变已批准的信息架构、入口和返回关系。
- 2026-07-24，用户再次明确回复“批准当前低保真线框”，确认继续冻结同一 SHA-256 `3c302a08d880cd342c11accc45830ec7de9bcd484f089b584e588415de0e7274`；本次确认不扩大为生产部署、生产迁移、Play 上传或公开发布授权。
- 2026-07-25，用户再次明确回复“批准当前低保真线框”，继续冻结同一 SHA-256 `3c302a08d880cd342c11accc45830ec7de9bcd484f089b584e588415de0e7274`；信息架构、入口与返回关系保持不变，正式视觉打磨继续按既定阶段推进，本次确认仍不包含任何生产或公开发布授权。
- 阶段 0 已固化为前端隔离分支提交 `66dd4003`。阶段 1 建立 `GameState`、`GameTransition`、`GameSnapshot`、`UndoFrame` 和 `v4c | rpl1` 回放合同；`src/core/engine.ts` 已能独立完成三模式移动、合并、计分、确定性出块、终局、撤回、计时、快照和 RPL1，且审计确认没有 DOM、storage 或隐式 `Date.now()` 依赖。
- 前后端共享黄金 fixture SHA-256 为 `392ec48bab2071801d87d15607560a6641eaa9211775ada3af2c2a5da99c07ab`。普通局与排位局统一 seed/hash 通道；经典模式覆盖 131072/262144 高位概率表及精确 15 格强制 8/16。
- RPL1 为 8/16/32/64 特殊出块增加 extType 8，格式固定为 `[ext8, move]`，payload 是字面单字节值且 move bit 为 0；既有 2/4 编码字节不变。`startUnixMs` 统一保持毫秒合同。
- Web 的标准 4×4、经典 4×4、标准 3×3 实时方向输入已通过窄 adapter 消费共享 Game Session transition；其他模式与 replayMode 继续既有路径。高位 Smoke 最初暴露 `restartWithBoard` 在线 hook 安装竞态，测试改用直接构造 Grid/Tile 的确定性盘面注入并显式断言兼容错误为零，没有改变产品运行时。独立审查又发现多 motion 中途异常可造成半写棋盘；兼容层现于应用前捕获 manager/回放/Grid/Tile 事务快照，完整成功后才发布 transition/count，失败则原位恢复、记录 marker 并重新抛出，绝不在半写状态回落 legacy。
- 后端 verifier 阶段 1 已固化为隔离分支提交 `4799f1a`，`verificationVersion` 升至 3；支持 ext8 严格配对、经典高位逻辑值、ranked 精确 spawn 校验和 normal 合法值校验，未修改路由、数据库或 OpenAPI。
- 阶段 2.1 已完成并拆分固化：前端 `98dd6f9b` 提供保留 Web 兼容入口的结构化 HTTP 结果，`c8e15021` 对齐 refresh、排行榜周期与历史字段；后端 `6f2d380` 保护 `deleted/all` 历史筛选并补齐分页/记录字段，`058bd24` 将 Capacitor release/debug origin 收紧为精确白名单。后端 `6ec8635`/`a996031` 另完成成就客户端/模式元数据与隐藏成就目录可见性规则。
- 阶段 3.1 已固化为前端提交 `bb05eef8`：`mobile/` 单入口、独立 Vite/TypeScript 配置与 `dist-app` 不加载 legacy；最小壳含集中式中英文、浅/深色 token、安全区和未批准隐私草案预览。`audit:mobile-boundary --release-candidate` 会因 `unapproved-draft` 主动失败，防止草案进入候选包。
- 2026-07-23 12:00 CST，阶段 2.2A 固化为后端提交 `d60f67d`。迁移 `0020` 只增加 nullable canonical 时间字段与成对约束，不回填、不切换榜单读取、不创建普通索引；`schema_migrations.applied_at` 冻结 legacy fallback 截止点。只读审计在同一已 checkout 连接的 `REPEATABLE READ READ ONLY` 快照中校验可信 session 链、分别计数/抽样 trusted begin、server receive fallback、anomaly，输出仓库外 0600、SHA-256 且拒绝覆盖的证据文件。未连接真实数据库；score/speed 四周期基线、回填、并发索引、榜单重建和绝对 rank 明确保留到 2.2B 及后续门禁。
- 阶段 3.2 已固化为前端提交 `79236d00`：精确锁定 Capacitor 8.4.2 核心/Android/CLI 及五个官方插件，提交 Gradle 8.14.3 Wrapper、AGP 8.13.0、JDK 21、min 29/compile-target 36 的单 module 原生工程；debug 使用 `cn.next2048.app.debug` / `2048 NEXT Dev`，release 使用永久身份 `cn.next2048.app`。主清单锁竖屏、关闭备份和明文、启用硬件加速，debug overlay 才放开本地 HTTP；WebView 调试由 `BuildConfig.DEBUG` 控制。文件分享只暴露 `cache/replay-share/`，无 Google Services/Firebase 接线，release 缺四项签名属性会在构建前明确失败。
- 阶段 4 AppDatabase 子阶段建立物理 IndexedDB v2 / 逻辑 row schema v1 的五仓实现。每个 owner/mode 用 save-head 记录 `client_record_id + generation + active/closed`，终局、丢弃和新局代际由同一事务校验，旧局迟到的 put/delete/finalize 均不能覆盖或删除新局；经典模式只有持久化为 `pending_terminal` 后才能结算。outbox 只接受四种判别式引用 DTO，游客不能入队；缓存只接受历史、分数/竞速榜、成就和精确 ReplayRecord 四种脱敏 DTO，认证 Token 无可写字段。owner 清理标记会立即阻断全部读取/写入，并在事务故障后保留供启动续清。
- 阶段 3.3 Keystore bridge 使用包名隔离且不可导出的 AES-256-GCM master key、12 字节随机 IV、128-bit tag 与逻辑 key AAD；密文只进应用私有 SharedPreferences。残留密文但 alias 丢失时禁止部分换钥，Provider/损坏/写失败归一为有限错误码。Capacitor `loggingBehavior=none` 阻止 debug bridge 把 Token 参数写入 logcat；生产非 Android 环境拒绝创建存储，不静默降级为内存或 Web storage。`write_failed` 的持久化结果按未知处理，阶段 4 退出协调必须 fail-closed 并保留 owner 清理标记直至可确认完成。
- 阶段 4 编排模块完成 owner 清理、session clock、per-session persistence 与 Capacitor lifecycle seam。owner 清理只有在 `beginOwnerClear` 失败后再次读取并明确证明目标 marker 不存在时才恢复同步；marker 存在或状态未知均保持 stopped。单一 `account.session.v1` opaque envelope 冻结承载账号 Token、持久身份和 challenge Token，删除结果未知时 marker 保留。真实 AppDatabase 集成测试填满账号 owner 的 saves/records/outbox/cache/diagnostics，并验证 Keystore 已删除后 IDB 中途故障可在下次启动续清，其他账号与 guest 数据保持隔离。
- session persistence 将 `stale` generation/revision、`save_revision_conflict`、`save_game_conflict`、`save_game_closed` 与 `owner_clearing` 统一视为永久 ownership lost，已排队写在再次触达数据库前熔断；完全相同的幂等 checkpoint 由 AppDatabase 单独返回 `unchanged`，不会误停当前实例。同 revision 的墙钟与逻辑 checkpoint 使用二维单调规则，设备墙钟回拨时仍可推进逻辑 checkpoint。
- session clock 明确把 `StoredGameSave.lastClosedAt` 作为设备墙钟 checkpoint，把 `GameSnapshot.savedAtMs/startedAtMs/lastEventAtMs` 作为逻辑时钟。ranked 缺少服务端 `startedAtMs` 直接拒绝；恢复只增加 checkpoint 之后的非负墙钟差，同进程用单调时钟并在 resume 时取单调/墙钟增量的较大值，绝不把设备绝对墙钟恒定偏差直接加到 ranked duration。阶段 6 ranked start 合同仍须增加 `server_now` 或等价同步 checkpoint，正式在线入口前冻结。
- lifecycle seam 只注册 pause/resume/backButton，按事件到达总序串行处理；handler 与异步 `onError` 拒绝均被收敛，部分注册失败或 listener removal 失败时仍先尝试全部解绑并 drain。`main.ts`、正式对局 runtime、输入多原因 fence、真实 Online Sync stop/drain 和 Android 进程强杀接线尚未完成，本批不得宣称阶段 4 运行时交付或勾选对应实施项。

## 验证记录

- 规划阶段沿用此前只读验证：`2048-next` service boundary audit 通过；核心/API 定向测试 33 项通过；生产构建通过。
- `2048-game-api` Node 测试 21 个文件、143 项通过；`2048-ranked` 边界定向测试 27 项通过。
- 当前 `npm run audit:entry-manifest` 通过，确认现有 Web 页面入口分类和 legacy 加载顺序受审计保护。
- 2026-07-23 规划收口时重新执行 `git diff --check`，通过。
- 2026-07-23 重新执行 `npm run audit:service-boundary`，扫描 456 个文件、0 项违规，通过。
- 2026-07-23 重新执行 `npm run audit:entry-manifest`，页面入口分类、manifest 驱动入口及 play/replay 顺序检查通过。
- 2026-07-23 重新执行 `npm run test:unit:core`，97 个测试文件、658 项测试全部通过。
- 干净前端 worktree（Node 22.22.2）：`audit:service-boundary` 扫描 453 文件、0 违规；`audit:entry-manifest` 通过；`test:unit:core` 97 文件、658 项通过；`verify:api` 4 文件、17 项通过；生产 `npm run build` 通过。
- 干净后端 worktree（Node 22.22.2）：`npm run typecheck` 通过；`npm run test:node` 21 文件、143 项通过。
- `npm ci` 在前端报告 12 项、后端报告 11 项现有依赖漏洞；未运行会改写锁文件的 `npm audit fix`，后续在独立安全审计中逐项判断，不把自动大版本升级混入 App 基底。
- 2026-07-23 04:06 CST，线框专项命令 `npx playwright test --config=playwright.config.ts --workers=1 tests/smoke/pages-android-app-wireframe.smoke.spec.ts` 通过：`1 passed (3.9s)`。测试覆盖延迟隐私同意及取消原路返回、游客/账号记录隔离、登录回目标模式、三模式入口、有/无有效移动时重新开始的不同确认语义、排行榜输入锁且计时继续、竞速模式/目标约束、待结算撤回不重置计时、结算/来源感知回放、记录详情、成就、设置及 320/390/480px 响应式；捕获的 `/api/*` 请求为 0。
- 真实浏览器人工路径通过：首次选择仅离线 → 游客首页 → 模式；游客点击经典 4×4 先进入隐私门，拒绝后回模式且不遗留目标，同意后进入认证，登录后继续经典模式；对局排行榜显示时 `data-input-locked=true` 且计时增长，关闭后恢复同一棋盘并解锁；待结算两分支、结算 → 回放 → 结算/首页、首页/记录来源详情、经典云记录详情、成就与设置均按来源返回。
- 320×720、390×844、480×960 视口下活动页面横向溢出均为 0；排行榜分别精确覆盖 `320×720`、`390×844`、`480×960` 视口。320×568 短屏下产品手机壳高度为 568、底栏位于壳体底部，页面只在内部滚动；审阅工具本身不属于产品界面。最终新加载会话控制台为 0 error、0 warning。
- 当前审阅截图：`output/playwright/wireframe-320-settings.png`（视口 320×720，元素截图 320×721）、`output/playwright/wireframe-390-game.png`（390×844）、`output/playwright/wireframe-390-leaderboard.png`（390×844）、`output/playwright/wireframe-480-leaderboard.png`（480×960）。截图仅用于信息架构/任务流批准，颜色、字体、图标和动效仍属于阶段 3/5 的正式视觉实现。
- 2026-07-23 阶段 1 前端最终门禁（Node 22.22.2）：三模式及经典高位 parity Smoke `4/4`；核心单测 `98` 文件、`687` 项；play/replay Smoke `43/43`；`audit:engine` 通过；`verify:refactor:ci` `10/10` 步骤通过，其中完整单测 `299` 文件、`1901` 项、Critical Smoke `41/41`、生产构建通过；`npx tsc --noEmit` 与 `git diff --check` 通过。所有 parity 用例显式断言 `compatFailures=[]`，日志和产物中的 `[game-session-compat] shared move failed` 数量为 0；故障注入另确认第二个 motion 失败时棋盘、Tile、分数、步数、move history、嵌套回放和发布字段全部回滚。
- 2026-07-23 阶段 1 后端最终门禁（Node 22.22.2）：黄金/ext8 定向测试 `16/16`、Node `22` 文件 `159/159`、`npm run typecheck` 与 `git diff --check` 通过；黄金测试同时断言 `modeKey/ruleset/undoEnabled/bestTile/terminal`。
- 2026-07-23 阶段 3.1 门禁（Node 22.22.2）：移动单测 `35/35`、移动 Smoke `3/3`、320dp/浅深色/中英文检查通过；`dist-app` JS 约 6.95KB、CSS 约 8.08KB、两份本地 Clear Sans 字体合计约 54KB，业务请求为 0。
- 2026-07-23 阶段 2.2A 最终门禁（Node 22.22.2）：canonical audit 定向 `6/6`；后端 Node `24` 文件 `179/179`；`npm run typecheck`、`git diff --check` 与 CLI `--help` 通过。两轮只读复审确认历史 cutoff、session 完整性、PostgreSQL CHECK 三值逻辑、三类抽样、同连接事务和仓库外证据写入无剩余提交阻塞；真实 PostgreSQL 迁移/分类执行尚未宣称完成。
- 2026-07-23 阶段 3.2 最终门禁（Node 22.22.2、JBR 21.0.10、SDK/Build Tools 36）：App unit `7` 文件、`54/54`；`build:app`、`cap sync` 五插件、Gradle `lint testDebugUnitTest assembleDebug :app:processReleaseMainManifest` 共 `396` tasks、原生权限/配置审计和 unsigned release 拒绝门禁全部通过。debug APK 约 10MB，SHA-256 `c53ecb1a8e03a12121bc37150509b201fc1de17f8a73dcf07ff86bf1d15897e8`。API 29 ARM64 AVD 在 Wi-Fi/移动数据关闭时安装成功，首次清数据冷启动 `819ms`，选择仅离线后本地主页正常；再次强停冷启动 `513ms` 并保留离线选择，logcat 无 App/WebView 致命错误。该证据仅为 debug/API29 基底，不替代后续真机、release、高刷或正式签名门禁。
- 2026-07-23 阶段 4 AppDatabase 专项（Node 22.22.2、`fake-indexeddb` 6.2.5）：`mobile-app-database.spec.ts` 22/22；App unit 8 文件 78/78；App TypeScript、`build:app`、`audit:mobile-boundary`、App Smoke 3/3 与 `git diff --check` 通过。覆盖三模式存档、同 revision 关闭时间刷新、跨局 generation、迟到 put/delete/finalize、经典 pending terminal、终局三故障点、owner 清理六故障点、guest/outbox、凭据拒绝、缓存防旧写/LRU、未来 schema 保留、诊断环形和 v1→v2 预发布迁移。生命周期绑定、单调计时以及 Keystore 删除参与的完整五步退出协调仍未宣称完成。
- 2026-07-23 14:48 CST，阶段 3.3 Keystore bridge 门禁（Node 22.22.2、JBR 21.0.10、API 29 AVD）：App TypeScript 通过；App unit `9` 文件 `87/87`；`verify:app` 与移动 Smoke `3/3`；`android:sync`、`:app:compileDebugJavaWithJavac`、`:app:lintDebug`、`:app:assembleDebugAndroidTest` 通过；API 29 `:app:connectedDebugAndroidTest` `7/7`；完整 `android:check`、原生项目/权限审计与三入口 unsigned release 拒绝门禁通过。两路独立只读复审最终均无 P0/P1；该证据只完成安全存储 seam，不宣称生命周期、认证、排位或完整退出协调已经完成。
- 2026-07-23 15:51 CST，阶段 4 编排模块最终门禁（Node 22.22.2、JBR 21.0.10）：owner/clock/persistence/lifecycle 定向 `50/50`，连同 AppDatabase 为 `73/73`，App unit `13` 文件 `138/138`；App TypeScript、`verify:app`、独立 `build:app`、移动边界审计与 Smoke `3/3` 通过。完整 `android:check` 通过：受控 `cap sync` 五插件、Gradle `:app:lintDebug :app:testDebugUnitTest :app:assembleDebug :app:assembleDebugAndroidTest :app:processReleaseMainManifest` 共 `379` tasks 成功，原生权限/项目审计及三入口 unsigned release 拒绝门禁通过。四轮独立只读复审发现的 marker 未知恢复、五仓覆盖、ranked 墙钟偏差、同 revision 分叉追高、`owner_clearing` 复活和幂等 `stale` 误退休均已补回归并闭合，最终无 P0/P1。该证据只签收可独立提交的编排模块；构建仍只转换现有 7 个壳入口模块，`main.ts`/正式对局/Online Sync 尚未接线，真实 Android owner 强杀续清、生命周期输入 fence、退出取消/确认和 pending terminal 产品流仍未宣称完成。
- 2026-07-23 18:44 CST，阶段 5 浏览器最终门禁（Node 22.22.2）：`verify:app` 的 App unit 为 `18` 文件、`197/197`，移动 Smoke 为 `11/11`，覆盖首次离线隐私、四顶层页、模式门、游客终局/结算/回放/详情/永久删除、结算“再来一局”、首次 2048 非阻塞、重开规则、连续八次真实手势后 WAAPI 归零、320×568 与英文 200% 几何；所有场景业务 fetch/XHR/WebSocket 与外域请求为 `0`。`dist-app` 保持单入口、`39` modules，JS `146.20KB`（gzip `41.98KB`）、CSS `29.56KB`（gzip `7.05KB`），移动边界审计、App TypeScript 与 `git diff --check` 通过。
- 同一源码的共享回归：核心单测 `98` 文件、`689/689`，Web 生产 build 与 service boundary audit 通过；完整 `verify:release` 十项 refactor/release-ready 门禁全部通过，其中既有 Web smoke `30.35s`，未因 App 的 RPL1 Base64 解码扩展或移动 CSS 发生回归。
- 阶段 5 独立复审发现并闭合：终局 checkpoint/finalize 结果未知重试、重开 delete/start 结果未知对账、游客删除结果未知对账、摘要刷新与不可逆操作解耦、失去 ownership 的旧会话禁止重开、OwnerCleanupWorkGate 先封门再排空、终局/Android Back 交错不回弹、异步导航 epoch、Dialog/路由焦点、短屏底栏和英文 200% 操作重叠。最终只读复审另发现合并 keyframe 使用 `filter` 违反 transform/opacity 动画边界，以及双 polite live region 可能重复播报；均以最小删除修正，并增加 CSS 属性白名单与单 live region 回归。相关新增回归均进入上述 `197/197` 与 `11/11` 门禁。
- 2026-07-23 17:52 CST，当前阶段 5 Android 构建门禁（JBR 21、SDK 36）：`android:check` 受控同步 `dist-app` 后完成 `379` Gradle tasks，`:app:lintDebug`、`:app:testDebugUnitTest`、`:app:assembleDebug`、`:app:assembleDebugAndroidTest`、`:app:processReleaseMainManifest`、原生项目/权限审计和三入口 unsigned release 拒绝全部通过。当前 debug APK SHA-256 为 `bbadba77d84b70c815348977c5dd4d9a6d08008f80218bff19d5be154fc96981`；`dist-app/index.html`、Android assets 与 APK 时间顺序一致，替代此前早于阶段 5 源码的旧包。
- API 29 完整原生离线门禁在上述 `bbadba77…` 包通过：清数据真断网冷启动约 `650ms`；真实 ADB 滑动写入 revision；HOME/热恢复不重建棋盘；Android Back flush 后回首页；force-stop 冷启动先进入首页，再继续相同 ID/generation/revision/棋盘且计时不回拨；真实终局、结算、回放、再来一局、历史与永久删除均完成，删除后再次强杀不会复活。页面级 CDP 观测 fetch/XHR/WebSocket、外域与 `/api` 请求均为 `0`，logcat 无 AndroidRuntime FATAL、ANR、App/WebView crash。
- 首页存档用时口径修正后的中间 debug APK SHA-256 为 `64671423f79324a34c573c736899f00cb38e858bdde67d6aded9c102d42083b7`，本地文件与设备拉回 `base.apk` 哈希一致。API 29 清数据真断网复验中，真实滑动后 Back flush 的存档为 `durationMs=0`、`savedAtMs-startedAtMs=4169ms`，首页严格按 `max(...)` 显示 `00:04`；force-stop 后冷启动 `442ms`，先显示首页并保留相同 ID/generation/revision/棋盘，继续后计时为 `00:06` 且不回拨。两段 CDP 观测仍为零业务请求，完整复验窗口无 FATAL、ANR 或 App/WebView crash。
- 最终 CSS/可访问性复审修正后的 `android:check` 再次完成 `379` Gradle tasks、原生项目/权限审计与三入口 unsigned release 拒绝门禁；最终 debug APK SHA-256 为 `9ba9502261ac76df9f36dd7add26c1d2dec37e9e84fb1dfd459516dc7fe0de6a`。该包的 API 29 最小原生复验结果记录于下一条。
- 最终 `9ba95022…` APK 的 API 29 真断网最小复验通过：本地与设备拉回 `base.apk` 哈希一致；清数据冷启动 `516ms`，真实 ADB 滑动使 steps/revision 从 `0` 变为 `1` 且棋盘变化，Android Back flush 后首页继续入口保持同一 ID/generation/revision/棋盘；force-stop 后 PID 更换，冷启动 `593ms` 先显示首页，再继续同一棋盘且计时不回拨。两个进程分别由页面级 CDP 观测到业务及外域请求总数均为 `0`，logcat 无 FATAL、ANR、App/Chromium/WebView crash。隐私、首页和恢复对局的 `[aria-live=polite]` 均严格为 `1`；APK 内 tile keyframe 只有 transform/opacity，直接 CSS `filter` 属性声明为 `0`（非动画的 `backdrop-filter` 及其 WebKit 前缀不属于该禁止项）。
- 阶段 5 游客离线切片已固化为前端提交 `0bbb9183`。随后后端 `1638bc4` 让非空无效 ranked Token fail-closed，并由 session `started_at + verifier duration` 写 canonical 时间；`8b4e264` 让 score/speed 四周期榜在分页前产生权威绝对 rank，只使用 canonical 周期与已批准 tie-break。后端全量 Node 分别为 `208/208` 与 `216/216`，均通过 typecheck；历史回填、派生重建与真实 PostgreSQL 仍是部署硬门禁。
- 前端提交 `49c97b53` 冻结 ranked start 的稳定 `operation_id`、`started_at_ms`、`server_now_ms` 与错误合同，`verify:api` 为 `22/22`。提交 `8c70142e` 复用同一 LocalGameSession 支持账号 owner、标准 3×3、经典 pending terminal 与 ranked refs；经典只有仍可撤回时进入待结算，无撤回直接结算，guest 越权模式和缺冻结 seed 的 ranked 局在 session 边界拒绝，ranked 初始逻辑时钟采用 `server_now + 单调增量`，本地 restart 明确交回后续 abandon/start 编排。核心 `689/689`、App `203/203`、移动 Smoke `11/11` 通过；未接 HTTP 或开放 UI。
- 2026-07-23 阶段 2.2B 隔离开发批次固化为后端提交 `e0dee82`。受保护 backfill 默认使用 `REPEATABLE READ READ ONLY`，commit 需要精确数据库名与 dry-run plan checksum，持有事务 advisory lock 并锁定 records/ranked_sessions/leaderboard_best；写前物化 record/best/public plan，只更新 canonical 两列，事务内恢复 updated-at trigger、验证 constraint、全量重建 `leaderboard_best`，再以 EXCEPT、结果 checksum 和 scope 行数拒绝任何漂移。公开榜证据覆盖 score 的 global/bucket/mode_key 与 speed 的 bucket/mode_key、总/日/周/月和五个目标；为避免大库证据按最高约 52 倍展开，只在仓库外 0600 报告保存每 scope 的行数与有序 SHA-256，事务内仍逐行 EXCEPT。运行时、admin import 与 legacy migration writer 已统一 canonical tie-break；不可信 D1 session 不再伪关联 migrated record。云记录重复提交不会复活软删，重复 DELETE 返回原始截止时间，显式 restore 严格受数据库 3 天窗口约束并在过期未 prune 时返回 410。Node 22.22.2 下源码及全部 scripts typecheck、后端 `28` 文件 `235/235`、两条 CLI help 与 `git diff --check` 通过。真实 PostgreSQL 的迁移、SQL 解析、临时表、trigger/advisory lock/回滚、二次 dry-run 幂等仍未执行；canonical 并发索引与隔离库 EXPLAIN 也未完成，因此本提交禁止部署且不能签收 2.2B。
- 前端提交 `7dde734f` 将移动认证 OpenAPI 与 Node 现状对齐：登录只声明必填邮箱/密码，注册验证码阶段只重发邮箱/验证码，密码重置使用 `new_password`，移动认证用户固定 `id/email/nickname/role`；重新生成类型后 `verify:api` 为 `25/25`。
- 前端提交 `ad551347` 建立单一 `account.session.v1` 安全 envelope 和移动认证 HTTP 核心，并把 release 入口改为 fail-closed：production 固定唯一 `https://2048next.cn/api`、禁止 override/loopback/未批准隐私草案联网；Vite 产物、Capacitor 同步和 Gradle release task graph 共同审计构建模式，三个 release 入口均拒绝 debug 资产。Node 22.22.2 下主代理复验 `verify:app` 为 App `230/230`、production `1/1`、Smoke `11/11`；独立完整 `android:check` 完成 `379` Gradle tasks、原生/资产/签名门禁。当前政策版本仍为 `unapproved-draft`，因此 release-candidate 审计按设计失败，不能开启生产在线能力或生成公开候选包。
- 2026-07-25，阶段 2.3 账号删除隔离批次完成 Node/PostgreSQL 验证：迁移 `0022_account_deletion_state` 与 `0023_account_deletion_prune` 已在隔离 PostgreSQL 17.10 应用；旧 Token 缺失 `av` 按 0、删除申请/改密按 `auth_version` 即时撤销，密码哈希快照与行锁阻止并发旧密码“版本洗白”；72 小时内密码登录取消、到期登录 `410 ACCOUNT_DELETED`、root 排除、`FOR UPDATE SKIP LOCKED`、双 `id/game_user_id` namespace 碰撞 fail-closed 和同邮箱新 ID 注册均通过。
- 同一真实数据库以 `users.id=920001/game_user_id=920101` 验证公开隐藏：资料 `404`、记录为空、Stone 与 Relay 不可见；Relay `ownerUserId="0920101"` 仍按安全 bigint 归一化命中。到期清理结果为 `pruned=1/replayFilesQueued=1/blockedOwnerCollisions=0`，账号、shadow、record、Stone 全部归零；Relay 删除匹配用户整段、清空 snapshot、保留其他段且修正 active index；回放对象与 durable queue 保留到文件删除成功。同邮箱重建为新 ID `920002`，未关联旧数据；所有测试数据随后清理。
- 账号真实 API 链路再次通过：登录 `av=0`；改密签发 `av=1` 且旧 Token 返回 `401 TOKEN_REVOKED`；删除申请成功；密码登录取消删除并签发 `av=3`；强制到期后登录返回 `410 ACCOUNT_DELETED`；prune 后用户行为 0。后端全量为 `31` 文件、`271/271`，`npm run typecheck` 与 `git diff --check` 通过。
- 公开隐藏谓词改为兼容 `users.id/game_user_id` 且碰撞时 fail-closed 后，真实 canonical index gate 再次覆盖 `52` 个 score/speed scope，两个索引均为 `existing`；仓库外 0600 证据为 `/tmp/2048-next-pg17.VuZLix/evidence/index-gate-account-visibility-v2.json`，SHA-256 checksum `2b15174ae189bc3c6bd8e6d7b2bfd3346c72ff49303947ee6470e17544958561`。
- 上述均为本地隔离数据库与源码门禁，不是生产 `Backend Ready`：未执行生产迁移、生产部署、CORS/公开政策页验证或任何公开发布。
- 2026-07-25，阶段 6 真实 App runtime 接线完成：`main.ts` 不再把 `enterAuthenticatedMode` 固定返回 `unavailable`。账号 owner 的三个首版模式现在先恢复各自本地存档；无存档且联网、Token 可用时经稳定 operation ID、服务端 `started_at_ms/server_now_ms/seed/token` 和 Keystore challenge ref 创建 ranked 棋盘；请求、刷新或安全保存有界失败且未产生 ranked save 时直接创建明确 normal 局。账号 normal/ranked 会话的 save、终局 outbox、重开和排位编排统一经过真实 `OwnerCleanupWorkGate`，游客数据投影不会混入账号终局。
- 同批修复损坏或未来 `account.session.v1` 使全局启动失败的问题：严格 envelope parser 与未知原文保持不变，账号能力 fail-closed 为未登录；AppDatabase 仅写入一条 `uploadPolicy=never` 的脱敏本地诊断，游客标准 4×4、游客存档和历史仍可启动。模式页/登录回目标模式使用真实 runtime，棋盘标题与“排位对局 / 普通对局 / 离线对局”按实际 owner、模式和 game kind 渲染。
- 为闭合 durable `ranked.abandon` intent，Node/PostgreSQL 新增 `POST /api/ranked-session/abandon`：账号 Token 与 ranked Token 双重校验，operation ID 必须精确绑定 challenge，事务行锁下将未消费 started 会话幂等转为 `abandoned`，到期会话转/保持 `expired`，mismatch/consumed/inactive fail-closed；OpenAPI 与生成类型同步。该接口只完成源码和本地测试合同，尚未部署到任何生产环境。
- 本批最终门禁（Node 22.22.2、JBR 21.0.10、SDK 36）：App unit `20` 文件 `275/275`、production `1/1`、移动 Smoke `12/12`、`verify:app`、`verify:api`、移动边界审计与 TypeScript 全部通过；完整 `android:check` 完成 `379` Gradle tasks，原生权限/项目、release 资产和 unsigned release 拒绝门禁全部通过。后端全量 `31` 文件 `273/273`、`npm run typecheck` 与 `git diff --check` 通过。当前正式政策仍为 `unapproved-draft`，生产 `Backend Ready`、生产在线解锁、签名候选和公开发布仍未获授权且未执行。
- 2026-07-25，经典 4×4 的 `pending_terminal` 产品流接入真实页面：首次无路可走且仍可撤回时显示专属“撤回继续 / 结束并结算”模态；撤回只恢复活动存档和棋盘，不写历史/outbox，确认结算复用一次性终局事务并进入结果页。冷启动从该模式存档恢复时重新显示同一选择；Android Back 或 dialog cancel 只保存 pending 状态并回首页，不会暗中结算。App unit 更新为 `278/278`、移动 Smoke `12/12`，`verify:app` 与完整 `android:check`（`379` tasks、原生/资产/签名门禁）再次通过。
- 2026-07-25，阶段 6 `record.submit` outbox 接入真实运行时：冻结本地记录通过稳定 `client_record_id` 构造 `/records` 请求；normal 永不携带排位令牌，ranked 只读取同 challenge 的 Keystore 引用。网络错误、408/429/5xx 使用 2 秒起、15 秒封顶的指数退避；401 由认证核心最多 refresh 一次，仍失败则保留记录/outbox并要求登录；永久 4xx 标记失败并保留同 ID 手动重试。成功时“记录标记 uploaded + 删除 outbox”在同一 IndexedDB 事务提交，之后才移除 challenge 引用；若安全存储清理失败，后续前台冲刷会从已上传 ranked 记录继续清理，避免先删令牌造成数据不可恢复。终局、启动、联网恢复、前台恢复及登录/refresh 成功均触发 owner-gated 单飞冲刷；结算页显示等待同步/已同步/失败并提供原位手动重试，游客仍只显示本机记录。
- 同批闭合终局自动冲刷与结算页刷新并发窗口：单飞从读取 Keystore 前开始，避免两个 flush 同时消费同一 outbox。认证模块仍保持动态加载；构建日志不再报告静态/动态混用。App unit `21` 文件、`287/287`，production `1/1`，移动 Smoke `12/12`；记录同步专项 `5/5` 已纳入固定 `test:unit:app` 门禁，覆盖 normal 无排位令牌、ranked challenge 引用、临时退避、401 保留与永久失败同 ID 重试。`build:app` 为 `49` modules，认证独立 chunk 约 `5.55KB`（gzip `2.25KB`），主 JS 约 `216.23KB`（gzip `57.92KB`）；移动边界审计与 `git diff --check` 通过。
- 2026-07-25，真实退出/切号清理接入“我的”：点击后先在 3 秒产品等待内尝试 records outbox 冲刷，底层单请求超时固定 8 秒；仍有数据时按未上传记录、未结束模式和其他同步任务分别显示数量，取消只关闭对话框并保留身份与剩余数据，确认才执行已验证的五步 owner 清理。清理先同步封闭旧 owner 工作门并排空进行中保存/上传，再写 `owner_clear` 标记令全部旧 owner 查询立即不可见，清内存身份、删除唯一 Keystore envelope、清 saves/records/outbox/cache/diagnostics，最后移除标记；中途失败保留标记供下次启动优先续清。成功后为后续重新登录创建全新的工作门实例，旧会话仍无法复活，同一用户 ID 在同进程重新登录可安全建立全新本地生命周期。
- 退出批次专项验证覆盖：有界冲刷后准确报告 `1` 条未上传记录与 `1` 个其他模式存档；确认退出清除账号 owner、Token、outbox 与 pending marker，同时保留 guest 存档；同一用户以新的 `persistentIdentity.establishedAtMs` 重新登录可正常创建新局。浏览器 Smoke `mobile-app-auth-outbox.smoke.spec.ts` 通过真实页面验证“取消退出”不调用清理、确认后账号 UI 回到游客、账号 save/outbox 消失且 guest save 保留。`verify:app` 最终为 App unit `21` 文件、`290/290`，production `1/1`，移动 Smoke `13/13`；构建 `49` modules，认证独立 chunk 约 `5.55KB`（gzip `2.25KB`），主 JS 约 `222.30KB`（gzip `59.42KB`），移动边界审计和 `git diff --check` 通过。
- 同一退出候选完成完整 `android:check`：App unit `290/290`、production `1/1`；受控 debug/production `cap sync` 均为 `49` modules 与五个已批准插件；Gradle `379` tasks 完成 lint、debug unit、debug APK、AndroidTest APK 与 release manifest；原生项目/权限审计继续只允许 `INTERNET`、`VIBRATE` 和应用私有 signature 权限；三入口 release 资产模式与 unsigned release 拒绝门禁全部通过。未执行真机账号退出、生产连接、正式签名或任何发布操作。
- 2026-07-25，阶段 7 App 账号删除入口接入已完成的后端 72 小时合同：客户端只用安全会话中的邮箱与用户重新输入的密码调用 `POST /account/deletion/request`，成功后仅在普通本地存储保留版本、申请时间、截止时间与掩码邮箱，不保存 Token、用户 ID 或完整邮箱；随后复用 owner 清理边界立即退出为游客。若清理 marker 或 IndexedDB 失败，运行时 fail-closed 删除 Keystore envelope 并保持账号不可恢复；启动检测到回执时在恢复账号身份前继续清理。冷静期内密码登录返回 `accountDeletionCancelled=true` 时清除回执并在通用登录完成后显示专用取消提示；过期且无账号身份的回执在启动时删除。
- 本批门禁（Node 22.22.2、JBR 21.0.10、SDK 36）：App unit `22` 文件、`302/302`，production `1/1`，移动 Smoke `14/14`，其中真实页面覆盖申请删除、掩码回执、Token/用户 ID/完整邮箱不落回执、重新密码登录取消与专用提示；`verify:api` 为 `26/26`，生成类型与 OpenAPI 一致，`git diff --check` 通过。完整 `android:check` 完成 `379` Gradle tasks，debug APK、AndroidTest APK、release manifest、原生权限、release 资产与 unsigned release 拒绝门禁全部通过。正式隐私政策、用户协议、公开删号网页、生产连接、正式签名与发布仍未执行。
- 2026-07-25，最小客户端诊断后端合同固化为 `POST /api/client-diagnostics`：匿名且不读取认证，严格只接受 event/category/severity/time 与 error type、脱敏 stack、App/构建、Android/WebView 版本；额外邮箱、Token、棋盘、回放或动作字段在数据库访问前拒绝。PostgreSQL `audit_events.client_event_id` partial unique index提供幂等，默认每 IP 每分钟 20 次、解析后 16 KiB/路由 20 KiB 上限，Node 每 6 小时触发一次 30 天有界清理。后端提交为 `4847b54`；全量 Node `32` 文件、`277/277` 与 typecheck 通过，迁移 `0024_client_diagnostics` 已在隔离 PostgreSQL 17.10 应用。真实本地 HTTP 验证首次 `201 duplicate=false`、同 ID `201 duplicate=true`、额外邮箱 `400`，测试行随后删除；未部署生产。
- 同批 App 诊断链路接入 IndexedDB 环形仓：`window.error`、`unhandledrejection` 与原生生命周期严重错误在写入前清理邮箱、Bearer/JWT、凭据形键值、URL query/hash 和本机用户名。隐私仅离线或开关关闭时事件冻结为 `uploadPolicy=never`，以后同意/重开也不追传；联网同意且开启时匿名提交，不附带 Token 或 owner。上传成功只标记本地完成；失败不阻塞启动、对局、存档或退出。我的页提供默认开启的自动诊断开关和本地 JSON 导出，导出省略 `ownerKey`，Android 只使用 `diagnostic-share/` Cache 子目录并在分享/保存返回后删除临时文件。
- 诊断 App 门禁：App unit `24` 文件、`309/309`，production `1/1`，移动 Smoke `16/16`，专项覆盖仅离线零请求、联网脱敏上传、关闭后不上传和浏览器本地导出；`verify:api` 为 `27/27`。完整 `android:check` 完成 `379` Gradle tasks，两个受控构建模式、debug/AndroidTest APK、release manifest、权限、release 资产及 unsigned release 拒绝门禁均通过。最终 `dist-app` 主 JS 约 `238.84KB`（gzip `64.23KB`），Filesystem/Share 仍位于约 `8.72KB`（gzip `2.97KB`）的按需 chunk；未接第三方统计 SDK、设备标识、通知权限或生产诊断服务。
- 2026-07-25，阶段 7 云历史与排行榜真实页面接线完成：记录页登录后默认账号活动记录，可切换本机游客、账号活动与最近删除，排序严格使用时间/分数/盘面和三种个人记录语义。owner/status 切换先清除上一集合，避免“最近删除”短暂显示活动记录；排序仍保留 stale-while-revalidate。云历史按 user/status/sort/page 缓存，先显示快照和时间再后台刷新；离线快照保持只读，不显示删除或恢复动作。在线 DELETE/restore 后失效九种首屏缓存并原位刷新，游客历史仍不会上传或并入账号。
- 同批排行榜改为 App 内全屏原生 `<dialog>`：三个首版模式均有分数榜，竞速和 2048–32768 目标只对标准 4×4 开放，周期为 all/day/week/month。缓存 key 绑定 mode/metric/period/target/page，UI 只显示后端绝对 `rank`，不按数组位置重新编号；对局中打开只增加 dialog 输入 fence，计时、排位会话、持久化和路由保持运行，关闭恢复原棋盘与来源状态。移动源码和请求只使用 2048 NEXT `/leaderboard`，未加入 `/ranked/*` 或全国榜文案。
- 回放播放器扩展为三种首版模式共用的权威 RPL1 重建：从回放元数据读取 mode/ruleset/challenge/seed/start，复用同一 Game Session 执行移动、确定性出块与撤回，限制最多 50,000 个动作；本地记录继续与完整 final snapshot 严格比对，云记录可只凭后端已验证 ReplayRecord 重建，并再核对历史摘要的模式、分数、步数、用时、最大方块和盘面和。云回放按 record 按需缓存，缓存命中立即播放并后台刷新，无缓存离线时明确提示只读不可用。
- 结算页、历史详情和播放器现统一传递同一个版本化 `ReplayRecord` JSON；Android 仅写入 `cache/replay-share/2048-next-replay.json`，通过 Filesystem + Share 打开系统面板，并在返回或取消后 best-effort 删除临时文件。浏览器预览使用同一序列化内容下载。通用 JSON 文件分享 seam 同时复用既有诊断导出，未增加依赖、外部存储权限、公开回放链接或导入能力。
- 本批最终门禁（Node 22.22.2、Android Studio JBR 21.0.10、SDK 36）：App unit `26` 文件 `322/322`、production `1/1`、移动 Smoke `18/18`，新增真实页面覆盖云历史缓存先显示再刷新、active/deleted 切换、删除/恢复、排行榜绝对 rank、周期、竞速目标、对局弹窗计时连续和输入锁。`verify:api` 为 `27/27`；后端 typecheck 与 scripts typecheck、Node `32` 文件 `277/277` 全部通过；`git diff --check` 与移动边界审计通过。`dist-app` 为 `62` modules，主 JS 约 `262.21KB`（gzip `70.14KB`），CSS 约 `32.58KB`（gzip `7.52KB`），Filesystem/Share 仍保持约 `8.72KB`（gzip `2.97KB`）按需 chunk。
- 完整 `android:check` 在显式使用冻结的 JBR 21 后完成 `379` Gradle tasks：lint、debug unit、debug APK、AndroidTest APK、release manifest、原生项目/权限、三入口 release 资产模式及 unsigned release 拒绝门禁全部通过；五个 Capacitor 插件未变化。第一次未设置 `JAVA_HOME` 的 shell 调用在 Gradle 配置阶段因找不到 JDK 21 提前停止，未执行编译或改动工程，随后仅补正确环境变量原样复跑通过。正式政策仍为 `unapproved-draft`，生产 `Backend Ready`、生产连接、正式签名、Play 上传和公开发布均未执行。
- 2026-07-25，成就 OpenAPI 合同以提交 `b5a00fd1` 对齐 Node/PostgreSQL 已返回的 `completable_clients` 与 `required_mode_keys`，并同步 Achievement create/update 请求和生成类型；`verify:api` 更新为 `28/28`。后端仍是元数据与隐藏目录可见性的唯一权威，App 不从名称、规则文案或图标猜测可完成范围。
- 同批 App 成就页按已批准线框成为从“我的”进入的任务页：游客点击后可在隐私门与登录完成后续接原意图；已登录用户先读取绑定 owner 的单份成就快照，再后台并行刷新公开目录与本人已获得列表。已获得列表不按客户端或模式过滤，因而 Web 专属、隐藏或未移植模式成就在真正获得后仍完整显示；未获得列表只保留 `completable_clients` 包含 `android` 且全部 `required_mode_keys` 属于首版三模式的项目。公开目录未返回的隐藏成就没有占位，页面不加载远程成就图标，避免离线快照产生额外网络请求。
- 成就专项与完整门禁通过：App unit `26` 文件 `324/324`、production `1/1`、移动 Smoke `19/19`；专项覆盖游客登录续接、跨端隐藏已获得展示、Web-only/未移植模式未获得过滤、返回关系和断网快照。`build:app` 为 `62` modules，主 JS 约 `271.43KB`（gzip `72.10KB`），CSS 约 `33.60KB`（gzip `7.66KB`）；移动边界审计和 `git diff --check` 通过。完整 `android:check` 再次完成 `379` Gradle tasks，权限与五个插件不变，三入口 release 资产和 unsigned release 拒绝门禁继续通过。正式政策、生产 `Backend Ready`、正式签名与公开发布仍未执行。
- 2026-07-25，“我的”基础设置从静态占位接入真实偏好：外观支持跟随系统/浅色/深色并即时切换；语言支持跟随系统/简体中文/English，切换时重建页面壳但保留当前“我的”路由、账号会话、运行时存档与网络状态；短音效、触觉和 BGM 三个开关独立持久化，默认分别为开/开/关。游客仅离线状态可使用全部本地设置，设置切换不产生业务请求。
- 游戏反馈层只消费 `GameTransition`：无效移动静默，有效无合并移动播放极短操作音，合并、首次 2048 和真实结束使用各自短音；触觉只在合并、里程碑和结束调用，不对每次滑动振动。经典待结算终局只有确认结束时触发结束反馈。短音使用懒创建 Web Audio oscillator，不增加音频文件；关闭音效会停止仍在播放的 oscillator。
- BGM 只打包现有 `windows-bgm.m4a` 一种编码，产物约 `3.57MB`；默认关闭且不请求/解码，用户开启或已开启后的首次手势才创建 `Audio`，关闭立即暂停并归零。App pause/后台会暂停，resume 时按偏好恢复；WebView/系统媒体栈继续负责外部音频焦点中断。触觉按 Route Deviation 改用 `Next2048SystemHaptics` 单方法桥和 `performHapticFeedback`，不带忽略系统设置 flag；移除 `@capacitor/haptics` 与 `VIBRATE` 权限，Capacitor 官方插件缩减为 App/StatusBar/Filesystem/Share 四个。
- 本批门禁（Node 22.22.2、JBR 21.0.10、SDK 36）：App unit `28` 文件 `331/331`、production `1/1`、移动 Smoke `20/20`；专项覆盖五项设置即时回调、语言路由保持、重启持久化、BGM 默认零请求/开启后单一 M4A 请求、反馈优先级与后台暂停恢复。`build:app` 为 `65` modules，主 JS 约 `278.17KB`（gzip `74.10KB`），CSS 约 `33.83KB`（gzip `7.69KB`），移动边界审计通过。完整 `android:check` 为 `336` Gradle tasks，Java bridge 编译、lint、debug/AndroidTest APK、release manifest、四插件集合、仅 `INTERNET` 平台权限、三入口 release 资产及 unsigned release 拒绝门禁全部通过。未执行正式签名、生产连接或发布。
- 2026-07-25，公开合规页面进入未批准草案批次：新增 `/privacy.html`、`/terms.html` 与无需安装 App 的 `/account-deletion.html`。隐私/协议正文由 `src/policies/2048-next-policy.ts` 单一版本化源同时渲染 Web 页面和 App 包内任务页，已按实际代码列出本地存档、账号/邮箱、游戏与回放、IP/安全、匿名诊断、Android 仅 `INTERNET` 权限、Resend、Cloudflare/Turnstile、3 天记录恢复、72 小时删号与 30 天诊断保留规则。当前版本继续为 `unapproved-draft`，并显式阻塞运营主体、联系邮箱、生产托管信息和生效日期；release-candidate 门禁未解除，不能视为正式政策批准。
- 同批公开删号页只向同源 `/api/account/deletion/request` 提交邮箱和密码，不读取或附带已有 Token，不保存密码；严格解析后端 `pending_deletion/requestedAt/dueAt/maskedEmail` 回执，成功后清空密码并显示截止时间，401/410/429/网络错误保持表单可恢复。专项单元 `2/2`、公开页面 Smoke `3/3` 和 App 包内政策/设置 Smoke `1/1` 已通过；生产部署、生产 URL、正式正文和生效日期仍未获授权。
- 2026-07-25，旧 Web 排行榜显示已收口到后端权威 `rank`：`online_leaderboard_runtime.js` 的计时榜、本人行和模式介绍榜，以及 `account_page.js` 完整榜均只读取条目 `rank`；缺失或无效时显示 `--/#--`，不再使用 `i + 1` 或分页 offset 推测。TOP 1–3 样式也按权威 rank 决定。专项静态合同 `1/1`、完整榜与计时榜 Smoke `2/2`、production Web build 和服务边界审计通过；App 全屏榜此前已只渲染后端 rank。
- 2026-07-26，后端提交 `c81b702` 增加真实 PostgreSQL 乱序上传回归：复用生产 `refreshLeaderboardForRecord`，在显式确认的非生产数据库内开启事务，依次写入同一用户、同一模式、同分但 canonical 时间为 1 月 2 日、1 月 1 日、1 月 3 日的三条临时记录；验证较晚上传的 1 月 1 日记录替换已选最佳，随后上传的 1 月 3 日记录不得覆盖，最后无条件回滚。该命令复用 canonical backfill 的目标安全校验，要求 `VERIFY_DATABASE_NAME`，并拒绝 production-like 环境或目标。Node 22.22.2 下 `npm run verify:leaderboard-upload-order` 通过；既有 `leaderboard-period.spec.ts` 覆盖同分与跨页绝对 rank，`leaderboard-canonical-time-backfill.spec.ts` 覆盖重建、checksum 与幂等，因此同分、分页、重建、乱序上传矩阵完成。后端全量仍为 `32` 文件、`277/277`，源码与 scripts typecheck 通过。
- 同批前端提交 `af6c48dc` 增加 `npm run verify:backend-ready -- --api-base=... --web-base=...`。验证器固定要求 API 基址路径精确为 `/api`、Web 基址为站点根目录，读取 production `dist-app/mobile-build-flags.json` 证明 release API 为唯一 `https://2048next.cn/api` 且禁止 override/loopback，逐字比对已服务 OpenAPI 与仓库合同；随后验证 `/api/health` 的环境/版本/数据库、`https://localhost` 与 `http://localhost` 的精确 GET/OPTIONS CORS、未知 Origin 拒绝、三份公开页面、公开验证码注册、refresh、普通记录相同 `client_record_id`/回放重试幂等、排行榜正整数绝对 `rank`、72 小时删号回执以及待删除 Token 对 `/me` 和 refresh 的立即拒绝。验证器在创建测试账号前拒绝 production/live 环境；安全参数专项 `2/2` 已纳入 `verify:api`，该门禁现为 `30/30`。
- 2026-07-26 01:00 CST，最终隔离环境证据使用 Node 22.22.2、前端 `af6c48dc6490`、后端 `c81b702149b0`、PostgreSQL 17.10 数据库 `android_canonical_test`、API `http://127.0.0.1:33101/api` 与 Web `http://127.0.0.1:44173`。命令返回 `PASS env=isolated api=c81b702149b0 openapi=1.0.0 testUser=1003 record=c439b913-6651-4d29-beca-2f799c548488`。测试账号先按产品合同进入待删除态，再在隔离库把同一申请整体平移到 72 小时前，复用正式 `pruneExpiredAccounts` 与 durable replay 删除队列清理；结果 `pruned=1/replayFilesQueued=1/blockedOwnerCollisions=0`、文件 `deleted=1/failed=0/cancelled=0`，最终 backend-ready 测试账号、`user_id>1000` shadow、测试记录和回放删除队列均为 0。
- 隔离 canonical fixture 最初只有 `users_shadow=1000`、账号主表为空，首次试跑的账号序列落入既有 shadow ID 3。该试跑在形成证据前已精确删除唯一测试账号/记录/回放/验证码/审计行并恢复 `Player3`；核对原有 user 3 的 20 条 migration 记录未删除。随后仅将隔离 `game_data.users` 序列抬到 shadow 上界 1000，正式证据账号使用 1003 且完整清理。这是隔离测试夹具的身份序列准备，不是生产迁移或业务数据修复。
- 本批完整前端门禁：`verify:app` 为 App unit `29` 文件 `334/334`、production `1/1`、移动 Smoke `20/20`；production App build 为 `66` modules，主 JS 约 `306.68KB`（gzip `85.51KB`），移动边界、Web production build、服务边界和入口清单均通过。该记录只证明隔离环境 Backend Ready 验证器与当前合同可工作，不是生产 `Backend Ready`：未执行回放备份恢复演练、生产部署/迁移、正式政策批准、生产公开页面部署、正式签名、Play 上传或公开发布，线上解锁门禁继续关闭。
- 2026-07-26，后端提交 `8194e8b526cc` 将回放文件纳入既有每日备份：同一窗口先从 PostgreSQL 生成 replay manifest 并校验 storage key、字节数、SHA-256、引用计数与 migration，再创建回放 `tar.gz`、数据库 custom dump 和两份 checksum；dump 后再次读取同一清单，任何对象/引用/migration 漂移均让整批失败。数据库 dump、回放 archive、checksum 和 manifest 本地保留 14 天、异地保留 30 天；默认硬门禁为 50 GiB/2,000,000 文件。缺失、字节或 hash 不一致直接失败，orphan 不静默删除并在报告中计数抽样。`2048-game-api` 值班维护者负责每日状态与告警、季度演练；生产必须配置可执行 `BACKUP_ALERT_COMMAND` 和异地上传，production/live 明确拒绝 `BACKUP_UPLOAD_ENABLED=false`。
- 同批非生产恢复演练以 canonical 隔离库的全新 clone 为源，将共享 replay object 改为通过正式 `storeReplayEnvelope` 写出的 138 字节对象，并额外放置一条 orphan 验证报告。最终备份位于 `/tmp/2048-replay-restore-rehearsal.2WYfGK/backups/`：PostgreSQL dump checksum `d10d9dbde59df7abd9760d9db951e2399d03b7b95b21fd9a04786b9202cdf31f`，回放 archive checksum `96fb568834855687a04b83b6bf5bc05291b23e2a6c305b9c69c1f98055f8b457`，manifest checksum `55a1c5318607adf68bde2c5d8c223b2a80c21f1dfc114c2a08adfe1a91184fef`；两份 `.sha256` 均验证 `OK`。
- dump 恢复到全新 `android_replay_backup_restore_20260726`、archive 解压到全新目录后，最终审计为 `records=20000`、`replay_objects=1`、`migrations=24`、`referenced_objects=1`、`expected_bytes=138`、`actual_bytes=276`、`files=2`、missing/byte/hash mismatch 全为 0，orphan 为 `1` 且样本精确为 `orphan.json`；完整建库、`pg_restore`、解压和审计耗时 `0.54s`。源/目标演练数据库随后删除，核对剩余数量为 0，未改动 canonical 隔离库。
- 容量/告警证据将 `REPLAY_BACKUP_MAX_BYTES` 降为 `1`，真实备份在首轮审计以 `capacity exceeded bytes=276/1 files=2/2000000` 非零退出；ERR trap 写入 `last-failure.env`（`event=backup_failed`、line、exit code），并输出 `Backup alert command completed`。备份脚本静态合同与文件审计专项 `6/6`、Bash 语法、`git diff --check` 均通过；Node 22.22.2 全量为 `33` 文件、`279/279`，源码与 scripts typecheck 通过。该演练没有连接异地服务器或生产告警渠道，也不是生产备份/恢复授权；生产部署和告警渠道配置仍需单独批准与执行。
- 2026-07-26，阶段 8 性能基底增加 `app-startup`、`app-navigation`、`app-game-entry`、`app-input-core`、`app-input-dom-commit` 与 `app-input-first-frame-latency` User Timing。首次真实页面性能 Smoke 暴露输入首帧约 `121ms`，核心 transition 仅约 `0.8ms`，根因是棋盘每步同步创建多组 Web Animations；改为单次布局读取、一次 RAF 启动全部方块的 CSS transform transition 后，专项结果收敛为 startup `≈101.5ms`、navigation `≈23.6ms`、game entry `≈29.5ms`、input first frame `≈10.6ms`、core `≈0.6ms`、DOM commit `≈1.2ms`，没有放宽 `2000/100/500/50/10/20ms` 预算。
- 同批建立可执行资源硬门禁：首屏 JS `≤360KiB`、gzip `≤100KiB`，首屏 CSS `≤45KiB`、gzip `≤12KiB`，`dist-app ≤5MiB`，单一 M4A BGM `≤4MiB` 且不得出现在首载 HTML，debug APK `≤16MiB`。production 实测为 JS `308013/85942` bytes、CSS `34677/7884` bytes、dist `3982193` bytes、BGM `3566764` bytes；debug APK 为 `13695473` bytes。debug 审计要求 RAF sampler 必须存在，release 同步后再次扫描全部 JS 并要求它完全不存在；`verify:app` 为 App unit `30` 文件 `337/337`、production `1/1`、移动 Smoke `21/21`，完整 `android:check` 的 `336` Gradle tasks、资源、原生项目、release 资产和 unsigned release 拒绝门禁均通过。
- API 29 安装采样时确认 `import.meta.env.DEV` 只覆盖 Vite 开发服务器，最初的 Android debug APK 不含 RAF sampler；将条件收窄为开发服务器或编译期 `MODE=android-debug` 后，production 仍完全裁掉该 chunk，debug APK 单独包含约 `0.75KB` 的采样器。该镜像的 Android WebView `91.0.4472.114` 还不支持 `Array.prototype.at`，采样汇总改用等价数组索引并在删除 `Array.prototype.at` 的单测环境下回归通过；未引入 polyfill、依赖或生产兼容分支。
- 最终 API 29 ARM64 模拟器证据使用 `cn.next2048.app.debug`、debug APK SHA-256 `ae80598302b8bfbc83011dd42e6d76cec6f2b406109b32944d90be4073550890`、1080×2220/60.000004Hz、Android WebView `91.0.4472.114`。覆盖安装后的冷进程 `am start -W` 为 `636ms`；真实 ADB 滑动 30 秒使分数从 `0` 增至 `452`，debug RAF 为 `1792` 帧/`30000.333ms`、`59.70 FPS`、帧间隔中位数 `16.67ms`、P95 `16.68ms`。同窗口原生 ViewRoot `gfxinfo` 为 `1320` 帧、P50 `16ms`、P90 `22ms`、P95 `28ms`、missed vsync `2`，同时记录 `1142` 次 high input latency 与 `44.92%` janky；该数据受到 shell `input swipe`、模拟器主机调度、旧 WebView 及原生 ViewRoot 不等同 WebView compositor 的影响，只保留为诊断，不据此宣称发布级 60Hz 门禁通过。
- 当前性能证据仅覆盖桌面 Chromium 与 API 29/60Hz 模拟器；没有 90/120Hz 真机、Android 10/4GB 真实低端机、Perfetto/FrameMetrics 或生产等价已签名 release 候选。未观察到需要立即增加 `setFrameRate` 的证据，因此按设计不添加；阶段 8 高刷真机、正式 AAB 体积、签名与发布门禁继续未完成，也未执行生产连接、Play 上传或公开发布。
- 2026-07-26，新增独立 `.github/workflows/android.yml`，不引用或修改 Web 的 `deploy-self-hosted` 拓扑。PR/push 构建使用 Node 22、Temurin 21、SDK/Build Tools 36，依次运行 `verify:app`、`android:check`、`git diff --exit-code -- android`，并只上传 debug APK；main、手动和每日夜间再消费同一 APK，以 API 29/API 36 `google_apis` x86_64 矩阵运行离线生命周期 Smoke。PR 不启动模拟器矩阵，避免把高成本设备测试重复到每次提交。
- 模拟器 Smoke 由零新增依赖的 `scripts/android-emulator-smoke.mjs` 执行：关闭 Wi-Fi/移动数据、清应用数据、安装并冷启动 debug APK，经 debug WebView CDP 等待真实控制器就绪，选择仅离线、进入游客标准 4×4；再根据棋盘实际 DOM 边界换算设备像素并用 ADB 真实 swipe，验证后台热恢复、force-stop 冷启动先回首页、继续游戏恢复相同棋盘，并拒绝任何非 `https://localhost/` 资源请求。失败也会写出可上传 JSON，且 DevTools 转发在 `finally` 中清理。
- 同一脚本已在本机 API 29 ARM64 模拟器通过：首次冷启动 `429ms`、HOME 后热恢复 `20ms`、force-stop 后冷启动 `459ms`；真实移动后的 16 格棋盘在后台与进程重启后逐格一致，`externalResources=[]`。静态/纯函数专项 `2/2` 同时冻结 Activity 启动解析、棋盘变化判断、`verify:app`/`android:check`/Capacitor drift/debug artifact 和 API 29/36 矩阵；最终 `verify:app` 为 `31` 个 App unit 文件 `339/339`、production `1/1`、移动 Smoke `21/21`。GitHub 托管 runner 的 API 29/API 36 首次真实运行尚未发生，因此 main/nightly 双 API 清单保持未勾选；该 CI 不生成签名包、不连接生产、不上传 Play。
- 2026-07-26 清单复核补记此前已完成但未签收的 canonical 隔离证据：PostgreSQL 17.10 `android_canonical_test` 已应用 `0020_add_canonical_leaderboard_time` 与 `0021_add_ranked_session_operation_id`；当前 `20000/20000` 条竞争记录均具有成对 canonical 字段，来源全部为可审计的 `server_verified_at_fallback`，异常为 `0`，`leaderboard_best=3000` 且无空 canonical 时间。仓库外 dry-run/commit/post-commit dry-run 报告 SHA-256 分别为 `286909945cf262f0fbdd6ec9b56a15ee3f4893cf971cca3d8d7eddfc2a746e7f`、`8a602748ec8ab598a56ee95efc96c46010baf7e22b65db919d54b13089605016`、`e2d7c5c8f1bd50c05fd8c6968023ef8e9620eecd8ee3c0b3a1a00ae68a17431e`；commit 前更新 `20000` 行并重建 `3000` 个用户/模式最佳，commit 后更新需求为 `0`。该证据只来自隔离库，不是生产迁移授权。
- 同批补强云记录派生回归：删除必须在事务内移除当前用户/模式的 `leaderboard_best` 并按 `score DESC → canonical_ended_at ASC → id ASC` 选回次优；恢复必须重新执行 canonical upsert；模拟第一次榜单重建失败时事务回滚，同一幂等删除第二次可成功。专项 `4/4`，后端源码与 scripts typecheck、Node `33` 文件 `280/280` 全部通过。回放分享另增加系统面板取消/拒绝时仍在 `finally` 删除 Cache 临时文件的回归，专项 `2/2`。
- 据上述代码、隔离数据库和既有真实页面证据，实施清单同步签收 canonical 迁移/盘点/重建、云删除恢复派生、包内/公开政策同源合同、隔离 Backend Ready、challenge 安全令牌与幂等 abandon、阶段 6 错误矩阵、个人资料与本地设置、App 内反馈/无通知权限、阶段 7 综合测试、共享核心 Web/后端门禁及首版明确排除项。正式政策责任人/生效日期与用户批准、生产 Backend Ready、首屏非关键代码拆包、API 29/36 GitHub 实跑、低端/高刷真机、签名/Play/官网发布仍保持未完成。
- 2026-07-26，隐私合同将正文修订号与强制重新同意版本分离：`POLICY_BUNDLE_VERSION=unapproved-draft.1` 只标识当前中英文正文快照，`POLICY_CONSENT_VERSION=unapproved-draft` 继续作为构建清单、认证前置和本地选择的实质版本。普通文案/排版修订只提升 bundle revision，不会清除现有选择；只有数据用途、权限、服务商、保留删除或其他实质变化才提升 consent version。release-candidate 仍因 consent version 为 `unapproved-draft` fail-closed，未借修订号绕过正式批准。
- 新增真实页面隐私矩阵 `3/3`：离线联网意图可取消并精确返回模式页且零业务请求，也可接受后恢复原登录目标；旧实质版本在任何业务请求前回到隐私页；当前 consent 配合更新后的正文修订号直接进入首页且包内政策显示新 revision。最终 `verify:app` 为 App unit `31` 文件 `340/340`、production `1/1`、移动 Smoke `24/24`，production App 资源预算与边界审计通过；Web production build 和公开政策页专项 `1/1` 通过。正式政策正文仍缺责任人、主体/邮箱/托管信息、生效日期和用户批准。
- 2026-07-26，前端提交 `547a6cf3` 将云历史/榜单/成就、回放时间线、回放分享与诊断导出拆为独立动态 chunk；production 首载 JS 收紧到 `297,256` 字节（gzip `82,287`），CSS `34,677` 字节（gzip `7,884`）。建立四类 chunk 存在且不进入首载 HTML 的静态门禁，因此阶段 8 “首屏按需加载”正式签收。
- 前端提交 `e894f0a2` 增加仅 `workflow_dispatch`、绑定 `android-release` Environment 的受保护候选 workflow，但不部署官网、不上传 Play。提交 `8d76793c` 继续增加显式 `NEXT2048_VERSION_CODE` / `NEXT2048_VERSION_NAME` 输入、版本核对与隔离 `upgradeTest` build type。非法版本号和含空格版本名均在构建边界被拒绝。
- API 29 / Android 10 覆盖升级演练通过：同一临时测试证书从 `versionCode 1000001` 覆盖安装到 `1000002` 后，IndexedDB 存档、Android Keystore 加密值和 localStorage 设置保留；使用错误证书的 `1000003` 被 `INSTALL_FAILED_UPDATE_INCOMPATIBLE` 拒绝。证据保存于 `artifacts/android-upgrade-smoke-api-29.json`，证据提交为 `8d76793c1a128702e08365d13f5ef49f722060f7`。
- 同一提交的显式版本候选演练使用 `versionCode 42` / `versionName 1.0-rc-test`：APK `11,790,891` 字节，SHA-256 `12e1f589458d8fd51c52f0f2fe9d7874531ff4fb39d043516ffc91c53572ec62`；AAB `6,775,070` 字节，SHA-256 `bd193d81f9e42a2deebeef766b817ca8927afdc826731c0cf18940c9930bef5b`。这些只是一次性测试签名产物，临时证书已精确清理，不得分发。
- 2026-07-26 03:23 +0800，在 `8d76793c` 重新运行 `npm run verify:app`：App unit `33` 文件 `346/346`、production `1/1`、App Smoke `24/24`，production build、性能预算和移动边界审计全部通过。
- 2026-07-26 03:38 +0800，本机 API 36 / Android 16 ARM64 模拟器 Smoke 通过：使用 debug APK SHA-256 `00a8dced5e3fcc9800097d1a4c58f7cdd4250f902cc84d6531b4a0010da2d8cd`，清数据冷启动 `1212ms`，真实 ADB 滑动后棋盘变化，HOME 热恢复保留棋盘，force-stop 后冷启动 `606ms` 先返回首页，继续游戏恢复同一棋盘，`externalResources=[]`。报告保存于忽略产物 `artifacts/android-emulator-smoke-api-36.json`；该证据不代替 GitHub 托管 runner、真机或正式签名候选。
- 尝试从已推送的 `agent/android-app-foundation` 分支手动运行普通 Android workflow 时，GitHub 返回 `workflow android.yml not found on the default branch (404)`。根因是新 workflow 尚未进入默认分支，不是构建或测试失败；为遵守“不擅自触发生产部署”，未合并默认分支、未触发 release workflow。GitHub 托管 API 29/API 36 与 upgrade Smoke 继续保持未签收。
- 2026-07-26 09:36 +0800，当前分支首次重跑完整 `verify:release` 暴露两项测试门禁回归：通用 `test:unit` 以 `MODE=test` 误收集只能在 production 模式执行的 `mobile-auth-production.spec.ts`；旧 Web 榜单用例在运行时改为只信任后端权威 `rank` 后，fixture 仍假设数组位置就是名次。修复只调整测试契约：通用单测明确排除 production 专项，专项命令仍以 `--mode production` 执行；榜单 fixture 为 TOP 10 和本人行提供 `rank: 1..11`，没有恢复任何前端名次推算。定向榜单 `1/1`、production `1/1`、通用单测 `335` 文件 `2265/2265` 均通过；最终 `npm run verify:release` 的 7 项审计、单测、CI Smoke、Web production build 和 release-ready 门禁全部通过。
- 2026-07-26 09:42 +0800，在前端提交 `9b673932` 上重跑 `npm run verify:app` 通过：App unit `33` 文件 `346/346`、production `1/1`、App Smoke `24/24`、production build、资源预算与移动边界审计全部成功。首次直接运行 `android:check` 时，当前 shell 未设 `JAVA_HOME` 且默认 Java 为 Temurin 17，Gradle 因无法找到已冻结的 Java 21 toolchain 在编译前拒绝；该失败未进入产物或业务测试阶段。按已批准工具基线显式使用 Android Studio JBR `21.0.10` 与本机 SDK 重跑后，`android:check` 通过：App `346/346`、production `1/1`、Gradle `336` tasks、Lint、debug/AndroidTest APK、release manifest、原生权限/插件/资源审计、三入口非 production 资产拒绝和缺少正式签名属性拒绝全部通过。debug APK `13,691,940` 字节，SHA-256 `00a8dced5e3fcc9800097d1a4c58f7cdd4250f902cc84d6531b4a0010da2d8cd`；production 产物不含 debug RAF sampler。
- 2026-07-26 10:24 +0800，用户确认政策与协议内容责任人为王世杰、联系邮箱为 `1203214493@qq.com`，并确认后端服务器备份保存在华为云中国大陆上海区域。共享中英文政策正文修订为 `POLICY_BUNDLE_VERSION=unapproved-draft.2`，强制同意版本仍保持 `POLICY_CONSENT_VERSION=unapproved-draft`，因此现有草案不会被误视为已批准或可公开发行。该信息只补齐内容联系与备份披露；发布门禁仍缺运营主体法定实名、境外生产主服务器服务商及所在国家/地区、生效日期和用户最终正文批准。政策专项单元 `3/3`、公开政策页 Smoke `1/1`、完整 `verify:app` 的 App unit `346/346` 与 App Smoke `24/24` 均通过；随后 `npm run verify:release` 再次通过通用单元 `335` 文件 `2265/2265`、CI Smoke `41/41`、Web production build 和 release-ready 门禁。未执行生产部署、生产迁移、正式签名、Play 上传或公开发布。
- 2026-07-26 10:46 +0800，用户进一步确认运营主体法定实名为自然人王世杰，生产主服务器服务商按用户提供名称记录为 Color Cross、地域为美国芝加哥，生效日期为 2026-08-01，并明确批准完整隐私政策与用户协议。正式合同切换为 `POLICY_BUNDLE_VERSION=2026-08-01.1`、`POLICY_CONSENT_VERSION=2026-08-01`、`POLICY_EFFECTIVE_DATE=2026-08-01`，审批阻塞项清空；中英文正文和首次联网选择明确披露美国芝加哥处理、华为云中国大陆上海备份及个人信息出境单独同意，拒绝后仍可仅离线体验。旧草案联网调试旁路被删除，build-flags schema 提升为 3；正式 `android:release` 现在强制执行 `audit:mobile-boundary -- --release-candidate`，Gradle、构建模式审计和隔离 Backend Ready 验证器同步冻结已批准政策版本。
- 本批 `verify:app` 通过 App unit `346/346`、production `1/1`、App Smoke `24/24`、production build、性能预算和移动边界；正式候选政策审计与公开政策页 Smoke `1/1` 通过；`verify:release` 的通用单元 `335` 文件 `2265/2265`、CI Smoke `41/41`、Web production build 和 release-ready 门禁通过。首次 `android:check` 在最后签名拒绝门发现 Gradle 仍校验旧 build-flags schema/草案字段，导致以资产模式错误提前拒绝；根因合同同步后，定向签名门和完整 `android:check` 均通过 `336` Gradle tasks、原生/权限/资源审计、debug/release 资产模式及缺少正式签名属性拒绝。该批准只解除政策正文门禁；未执行生产部署或迁移、生产 Backend Ready、正式签名、Play 上传、官网发布或公开发布。
- 2026-07-26 11:41 +0800，首轮实体手机联调使用一加 7 Pro（GM1910，Android 12/API 31，ADB `a4a1dc80`），内置屏幕为 1440×3120、density 600，支持 60/90Hz；本轮系统活动模式仍为 60Hz。安装包为 `cn.next2048.app.debug`、`versionCode=1`、`versionName=1.0-debug`，debug APK 为 `13,692,660` 字节，SHA-256 `27e674cc81333151b6e22ad7a32fcac0b7a1041dc2701ac972005af5ad86c04d`。
- 真机首轮暴露三项 UI 问题并按最小根因修复：冷启动状态栏背景为黑色且图标不可读，改为 Capacitor 默认浅色状态栏并在外观切换时同步状态栏背景和图标明暗；首页程序化聚焦标题出现巨大橙色焦点框，只移除 `h1/h2/h3[tabindex=-1]` 的程序化焦点轮廓，按钮与输入框键盘焦点提示继续保留；深色主题下 Android 手势导航栏仍为白色，新增单方法 `Next2048SystemBars` 原生桥，仅同步导航栏颜色和图标明暗，不隐藏系统栏、不进入沉浸模式。浅色/深色状态栏与深色底部手势栏均已在该真机截图确认可读。
- 同机检查首页、模式、记录和我的四个顶层页：未发现横向裁切，底部导航未遮挡“我的”页末尾操作；长屏留白来自当前内容量而非布局越界，暂不凭主观判断扩大本批 UI 改动。系统栏桥加入后的完整 `android:check` 通过 App unit `348/348`、production `1/1`、Gradle `336` tasks、Lint、原生/权限/资源、debug/release 资产模式和缺少正式签名拒绝门禁。最终 `verify:app` 再次通过 App unit `348/348`、production `1/1`、App Smoke `24/24`、production build、性能预算与移动边界审计；首载 JS `298,927/82,923 gzip` 字节、CSS `34,736/7,899 gzip` 字节、`dist-app=3,986,559` 字节。该证据仍是 debug 真机调试，不替代生产等价签名候选或 90/120Hz 发布门禁。
- 同一 debug APK 覆盖安装成功，冷启动 `am start -W` 为 `1,164ms`。一加系统在静止页面会自动回落 60Hz、触摸与连续棋盘输入时自动切换 90Hz，无需修改系统设置。第一轮 30 秒输入脚本提前结束，采样混入后半段自动降频，仅得 `83.63 FPS`、中位 `11.1ms`、P95 `16.7ms`，因此明确判为无效样本。第二轮让真实 ADB 滑动覆盖完整 30 秒，SurfaceFlinger 在第 5 秒与结束时均报告 `Current mode ... fps=90.00`；WebView debug RAF 为 `2,690` 帧/`30,009.8ms`、`89.60 FPS`、帧间隔中位 `11.10ms`、P95 `11.20ms`，达到 debug 90Hz 诊断阈值且未出现稳定 16.7ms 平台，证明当前实现没有应用侧 60 FPS 封顶。该结果只用于本地真机性能调试；阶段 8 的生产等价已签名 release 候选、FrameMetrics/Perfetto 原始 trace 与正式高刷发布门禁继续保持未完成。
- 2026-07-26 12:01 +0800，真机登录提示“网络连接失败”的根因确认为生产 CORS 配置而非 Android 权限、设备网络或账号：已安装包的 `INTERNET` 为 `granted=true`，Wi-Fi 为 validated，App 隐私选择为当前 `online`；但生产 API `fd7de8830505` 对 `Origin: https://localhost` 的 GET 与登录 OPTIONS 均缺少 `Access-Control-Allow-Origin`，真机 WebView `fetch` 稳定返回 `TypeError: Failed to fetch`。用户随后明确批准本次最小生产配置变更。
- 生产主机 `visa:/opt/2048-game-api/current` 在变更前保持 commit `fd7de8830505`、API/PostgreSQL healthy。只将服务端 `deploy/.env.self-hosted` 的 `CORS_ALLOWED_ORIGINS` 在既有四个 Web Origin 后追加精确的 `https://localhost,http://localhost`，完整环境文件备份为 `deploy/.env.self-hosted.before-capacitor-cors.20260726T040143Z`（mode 600）；使用原镜像仅强制重建 `api` 容器，没有拉取代码、执行数据库迁移、重建 PostgreSQL、修改回放文件或发布前端。重建后容器为 healthy，`/api/health` 返回 production/database/replay storage 正常。
- 外部验证确认 `https://localhost` 的 GET 返回 `Access-Control-Allow-Origin: https://localhost`，登录预检为 204 并返回精确 allow-origin/methods/headers；`https://unknown.example` 继续不返回 allow-origin。真机 WebView 随后直接请求生产健康接口得到 200，并以不存在的探针账号调用真实 `/api/login` 得到结构化 401 `INVALID_CREDENTIALS`，不再出现 `Failed to fetch`，证明 HTTPS、CORS 与登录 API 链路恢复。该记录只批准并证明 Capacitor CORS 热修复，不等同于阶段 2.5 的完整生产 `Backend Ready`、数据库迁移或公开发布授权。
- goal 恢复后对生产做只读完成审计：API 主机仍运行 `fd7de8830505`，相对当前 Android 后端分支 `8ef54fd` 缺少 19 个已验证提交；线上 OpenAPI SHA-256 为 `42cae9c3385217968f80871322e650a7b90ff33fc072791e9f0851a986d9cb39`，与当前合同 `cd227264b2abb0f98cbfce0a462350fb113494823f13c04a2b830c5046ed02e5` 不一致。生产 `/leaderboard` 条目仍没有权威 `rank`，`/api/account/deletion/request` 为 404；`/privacy.html`、`/terms.html`、`/account-deletion.html` 也均为 404。因此只读证据明确反驳完整生产 `Backend Ready`，本轮不借已批准的 CORS 热修复扩大为代码部署、迁移或公开 Web 发布。
- 在不使用正式密钥、不覆盖 debug App 的前提下，使用 production `dist-app` 和 `upgradeTest` release fallback 构建一次性真机性能候选：application ID `cn.next2048.app.upgrade`，versionCode `1000090`，versionName `1.0-rc-perf-upgrade`，APK `/tmp/2048-next-1.0-rc-perf.apk` 为 `13,578,715` 字节，SHA-256 `8db8fb2854b4309b7338a95edf19ec081066d663a58149b9839739ee046cf5d0`。一次性 PKCS12 证书在 APK 生成后已精确删除；该包不构成正式签名候选。首次 ADB 流式安装与 `--no-streaming` 推送后的 package install 均因手机已熄屏锁定、ColorOS 停在 `InstallGuideActivity` 而等待系统确认，APK 尚未安装；候选构建本身成功，等待解锁后继续，不清除现有 debug 数据。
- 后续在同一 OnePlus 7 Pro（GM1910、Android 12/API 31、1440×3120、60/90Hz、Android WebView `147.0.7727.137`）安装独立 `upgradeTest` 包并完成 31 秒预发布诊断：原始 trace `artifacts/2048-next-oneplus7pro-upgrade-90hz-hybrid-20260726.perfetto-trace` 为 `46,056,989` 字节，SHA-256 `0fee60c15412e8b2288d82ce39abf5d71876ea0c46c7fc6a4ea7c6bba7795321`；SurfaceFlinger 在中段与结束均为 90Hz，有效回调频率约 `89.65 FPS`、中位约 `11.15ms`，无稳定 16.7ms 平台。该包仍为 debuggable 隔离构建，只作预发布证据。
- 同机另构建并安装 production 资产的 non-debuggable release 测试包：application ID `cn.next2048.app`、versionCode `1000091`、versionName `1.0-rc-perf-release`，APK `/tmp/2048-next-1.0-rc-perf-release.apk` 为 `11,792,607` 字节、SHA-256 `af77c29e87770b9cf3161d04f3a7823aefc23605eb3b56d7e4733c95ec8b082e`，`debuggable=false`，冷启动 `423ms`；一次性测试证书已精确删除。首轮 trace `artifacts/2048-next-oneplus7pro-release-90hz-20260726.perfetto-trace` 的有效频率约 `89.78 FPS`，但动画只覆盖约 `28.455s`，因此没有冒充不少于 30 秒门禁。
- 最终 non-debuggable release 样本将 Perfetto、设备端连续手势与刷新率检查并发启动。SurfaceFlinger 在输入第 5 秒和第 27 秒均报告 `Current mode ... fps=90.00`；`deliverInputEvent` 窗口为 `33.540857747s`，发送 `176` 次真实 ADB 滑动，采样结束后 App 仍停留在同一未终局标准 4×4 对局。原始 trace `artifacts/2048-next-oneplus7pro-release-90hz-final-valid-20260726.perfetto-trace` 为 `127,265,503` 字节，SHA-256 `2c382271c175af56177444ae7475b1cbdad1d4f8e9fb6c2195c151408e99bf4d`。以输入窗口内 `vsyncCallback` 的活动间隔（`≤16.7ms`）计算：`2990` 个有效间隔、`89.7727 FPS`、中位 `11.1339ms`、P95 `11.2451ms`；其中 `2988` 个 `≤12ms`，仅 `2` 个处于 `12–16.7ms`，没有稳定 16.7ms 平台。另有 `13` 个大于 16.7ms 的输入/动画空档，未计入活动回调频率，也未从 33.541 秒输入窗口中删除。
- 上述证据证明当前 production 资产的 non-debuggable Android 运行时没有应用侧 60 FPS 封顶，并达到已批准的 90Hz 数值阈值；但它使用一次性测试签名，不签收正式分发 signer、AAB/Play、120Hz、Android 10/4GB 实体低端机或生产 `Backend Ready`。生产仍缺 19 个后端提交、migrations `0019–0024`、当前 OpenAPI、权威榜单 `rank`、72 小时删号接口与三份公开政策页面；未经单独授权未部署这些内容。
- 性能采样完成后已卸载临时 `cn.next2048.app` 与 `cn.next2048.app.upgrade`，删除设备端本轮 Perfetto 临时文件，只保留工作区忽略目录中的原始 trace；`cn.next2048.app.debug` 未清数据并重新置于前台，原 debug 包私有数据保持不变。设备“充电时屏幕不休眠”继续为关闭，没有为后续调试改动系统常驻设置。
