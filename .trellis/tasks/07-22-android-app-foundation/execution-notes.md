# 执行记录

## Route Deviation

- 仓库存在 `.trellis/spec` 与 `.trellis/tasks`，但缺少技能约定的 `.trellis/scripts/task.py`，无法执行标准任务创建命令。采用最保守回退：按现有 `MM-DD-slug` 目录格式手工创建本规划任务；未启动实现，也未修改产品代码。
- 2026-07-26 03:26 +0800，为补齐本机 API 36 模拟器矩阵，首次通过旧 `sdkmanager` 下载 `system-images;android-36;google_apis;arm64-v8a` 时，Google 仓库在连续多轮等待后仍未返回 package manifest。采用最保守回退：中止旧进程，不使用第三方镜像、未知来源 system image 或手工拼装 AVD；改用 Android Studio 当前推荐的官方 `android sdk` CLI 后成功从 `dl.google.com` 安装同一官方镜像，并建立 `2048_NEXT_API_36` AVD。路线偏离只限于官方 SDK 工具的迁移，未改变代码、镜像来源或验收标准。

## 调查记录

- 2026-07-26 02:31:59 +0800，用户正式批准当前 Android App 低保真线框；冻结版本 SHA-256 为 `3c302a08d880cd342c11accc45830ec7de9bcd484f089b584e588415de0e7274`。批准范围是信息架构、页面入口和任务流；正式视觉、动效与细节打磨仍按后续实施阶段完成，不构成进入页面实现的阻塞项。
- 当前请求是复杂的全新移动前端规划，需要在 PRD 确认后补齐 `design.md` 和 `implement.md`，并由用户审阅后才能进入实现。
- 用户已确认接受 Capacitor，但最终产品必须使用 App 专属页面、入口和交互，不是线上网页壳或现有 Web 的简单移动适配。
- 当前 Web 的 legacy 脚本并非全部无效：`play` 清单仍主动加载 91 个脚本；同时审计明确阻止 56 个已退役 runtime 返回现代清单。App 基底不得把这套加载图整体复制过去。
- 现有后端与数据边界适合增加移动客户端；主要规划风险位于无 DOM 游戏核心、API 契约冻结、移动生命周期、安全存储、持久化 outbox 和 Android 发布链路。
- 用户确认首个公开版本只覆盖玩家核心产品；管理后台、接力工具、2K 监控、完整色板编辑和实验模式延后为独立模块。
- Capacitor App 的页面与静态资源随 APK/AAB 本地发布，不需要新增移动前端托管服务；在线产品能力继续复用现有 `2048-game-api`。
- 用户明确长期目标为 Web、Android App、微信小程序三端并存，共享同一后端、账号和云端业务数据；当前 Android 基底规划必须同时定义可供小程序复用的核心与契约边界。
- 代码扫描未发现 `openid`、`unionid`、微信登录或通用第三方身份映射模型；三端统一账号需要在后端新增平台身份到权威用户 ID 的映射，而不是为小程序复制用户表。
- 用户确定先做 Android App，小程序延后；游客只可游玩一个 4×4 经典玩法，其他模式在开始前要求登录。
- 当前模式目录把无撤回玩法命名为“标准版 4×4”，把可撤回玩法命名为“经典版 4×4”；用户已确定游客开放无撤回的 `standard_4x4_pow2_no_undo`。
- App 首页信息架构开始收敛。现有 Web 首页同时承载对局、用户、公告、统计、新游戏、回放导出、练习盘、模式、设置、计分和计时信息，移动端不直接继承这一入口密度；当前最高优先级产品决策是首页采用“快速开局页”还是“综合大厅”。
- 用户确认 App 本地页面和游客对局不应像线上 Web 页面切换一样等待服务器；在线数据采用页面先呈现、后台异步刷新的边界，强在线操作除外。
- 用户确认 App 顶层采用“首页 / 模式 / 记录 / 我的”四项底部导航；对局、结算、回放和登录注册流程隐藏，排行榜从记录页或对局等明确入口打开。
- 用户确认游客标准局历史仅保留本机并标记为游客记录；登录不自动上传或并入账号云历史，游客记录不参与排行榜和成就，登录后的新记录才按账号规则同步。
- 代码证据显示 `2048-next` 已使用 Vite + TypeScript 且未安装 UI 框架，共享核心也在同一仓库。按最小依赖与原子迁移原则，首版采用同仓库隔离的移动端构建和原生 DOM/CSS，不新增 React，也不另建移动仓库。
- 用户确认首版只交付标准 4×4 不可撤回、经典 4×4 可撤回和标准 3×3 不可撤回三个模式；游客只可进入第一项，其他既有模式不显示占位入口并延后增量迁移。
- 用户确认 App 使用全新的本地数据空间，首版不迁移 Web 浏览器 localStorage/IndexedDB 历史；登录只读取已有云记录，现有文件导入能力延后。
- 用户确认首版最低支持 Android 10（API 29）；Android 9 及以下不进入兼容范围。开发机已检测到 Android Studio 2026.1、SDK Platform 36.1、Build Tools 36.0.0 和 platform-tools。
- 用户确认首版锁定竖屏，不支持手机横屏页面；方向变化不得触发对局状态丢失。
- 用户确认首版正式支持安卓手机；平板与折叠屏允许安装并采用居中的手机宽度兼容布局，但不提供专属大屏设计，也不进入首版正式体验验收范围。
- 用户明确要求支持 90+ FPS，而不是把 60 FPS 作为上限。代码检查表明现有部分运行时使用 `requestAnimationFrame`，但 App 不复用 legacy 动画清单；移动动画需从基底采用基于时间的 RAF，避免固定 60Hz 假设。
- 用户确认自动跟随系统 60/90/120Hz，并将 90+ FPS 纳入首版验收；省电、温控和系统设置导致的降频允许优雅回退。性能矩阵增加至少一台 90Hz 或 120Hz 真机。
- 用户确认首页采用快速开局定位：未完成对局优先继续，否则开始标准 4×4；仅保留首版模式和最近记录，不建设依赖网络的综合大厅。
- 用户确认已登录设备断网仍可玩三个首版模式；在线默认排位，排位会话不可用时自动降为有明确标识的离线普通对局，联网后只同步普通历史。后端核对确认 `/api/records` 无排位令牌时写入 `source: normal`，排行榜与成就过滤排位来源，无需新增离线历史接口。
- 用户确认同一进程从后台恢复时直接回到活动对局；被系统结束进程后的冷启动先进入首页，通过“继续游戏”恢复持久化状态，不自动跳入棋盘。
- 用户确认 Android 显示名称为 `2048 NEXT`，application ID 为 `cn.next2048.app`；该包名与 `2048next.cn` 是品牌命名关系，API 与 App Links 仍分别配置。
- 用户明确 App 不采用封闭内测模式，而按正常公开产品运行。后端核对确认 beta gate 默认关闭且公开邮箱注册/登录链路已具备；App 不加载 Web beta 准入页面，发布环境需保持玩家 API gate 关闭。
- 公开发行合规核对未发现正式隐私政策、账号删除 API 或账号删除网页。已将 App 内删除入口、公开网页、身份复验和 Token 撤销列为发布阻塞项；具体云记录与排行榜处置需要产品确认。
- 用户确认账号删除不立即执行，而采用 72 小时冷静期：申请后立即退出、撤销旧 Token并隐藏公开数据；期限内只有邮箱密码真实登录可原子取消，后台请求不能取消；到期未登录则彻底删除资料、记录、回放、排行榜和成就，不保留匿名成绩。
- 用户确认首版只跨端同步账号资料、已结束记录/回放、排行榜和成就；未结束对局仅保存在产生它的设备，服务端排位 checkpoint 不作为跨设备续玩产品能力。
- 用户否决“每台设备只保留一局”的方案，要求不同模式可以并行存档；首页继续最近关闭的未结束模式。每个模式一个存档，点击模式卡自动加载存档或创建新局，模式页不提供“新游戏”次按钮；重新开始只在对局内部触发。
- 用户确认对局内重新开始的防误触规则：已有有效移动且未结算时二次确认；尚未移动或已结算直接新开，只覆盖当前模式存档。
- 用户确认正常退出或切换账号时不保留该账号的本地多模式存档、账号缓存或 outbox；已同步云数据与独立游客数据不受影响。后续已进一步确认：先有界尝试上传，失败时必须由用户选择取消退出或明确清除。
- 用户确认退出时先尽力上传；若仍存在未上传记录或未结束模式，显示数量并允许取消，只有明确选择“仍然退出并清除”才原子删除本机账号数据，不静默丢失也不无限阻塞。
- 用户确认“记录”底部页使用“历史 / 排行”两个内部页签；回放从历史详情进入，不设独立回放页签，登录后可区分账号云记录与本机游客记录。
- 排行榜语义复核发现此前建议有误：现有产品可见指标为“分数 / 竞速”，最少步数筛选已被 smoke test 明确移除；`steps ASC` 仅存在于后端分数榜内部 tie-break。App 排行榜范围需按这一事实重新确认。
- 继续追踪确认“盘面和”并未消失：它是本地历史和用户个人记录的排序字段，Node `/user/:id/records` 支持 `sort_by=board_sum`；但公开 `/leaderboard` 仅接受 score/speed。需由用户决定 App 沿用此分层，还是新增全站盘面和榜。
- 用户确认沿用现有分层：全站排行保留分数/竞速；历史和个人记录保留时间/分数/盘面和排序；不新增全站盘面和榜。
- 边界纠正：上一轮把独立 `2048-ranked` 全国榜误归为 2048 NEXT Web 产品组成部分，这是错误。架构规范和运行调用均表明 `2048-next` 与 `2048-ranked` 是两个独立前端产品，只共享 `2048-game-api` 的账号与后端权威；2048 NEXT Android App 只使用自身 `/leaderboard` 分数/竞速榜，不接入全国榜。相关错误表述和开放问题已从 PRD 删除。
- 继续核对 2048 NEXT 自身榜单发现，API 不返回名次，Web 前端直接用分页数组序号显示排名；后端用未完整展示的用时、步数和日期拆开相同公开指标。是否改为公开并列属于产品规则，已作为单一开放问题保留。
- 用户否决并列名次，确定分数榜保持唯一连续排名；同分按首次取得该分数的对局结束时间升序，更早取得者在前。规划采用 `ended_at` 表达实际取得时间，并以稳定用户 ID 处理时间戳也完全相同的极端情况；整局用时、步数和 `updated_at` 不再作为同分裁决。竞速榜同毫秒规则仍待单独确认。
- 用户确认竞速榜也保持唯一连续排名；目标方块毫秒时间相同按首次取得时间升序，更早取得者在前，稳定用户 ID 仅处理时间戳也完全相同的极端情况。随后核对发现游戏页 TOP 10 已有总/日/周/月四周期，而完整 Web 排行页仅显示总榜；App 周期范围成为下一项产品决策。
- 用户确认 App 排行榜首版保留总榜、日榜、周榜和月榜四周期，并提出排行榜应作为独立弹出窗口，而不是沿用 Web 计时区域中的展开模式。该决定替代此前“记录页历史/排行内联切换”的描述；弹窗采用全屏模态还是较小浮窗仍待确认。
- 用户确认采用覆盖主内容与底部导航的 App 内全屏模态排行榜。Android 返回键和顶部关闭只关闭模态，来源页面状态保持不变；下一项需确定活动对局中是否提供该入口以及计时规则。
- 用户确认活动对局中保留排行榜入口；弹窗期间屏蔽棋盘输入，但计时和排位会话继续，不能暂停竞速成绩。排行榜分支的入口、窗口、周期和同成绩顺序已经收敛，下一项转向会影响所有页面文案与布局的首版语言范围。
- 用户确认 App 首版支持简体中文与英文，首次跟随 Android 系统语言，其他语言回退中文，并允许在设置中手动切换。实现采用移动端集中式字典，不复用分散的 Web legacy 翻译运行时。下一项回到首页，确定唯一在线摘要卡片的内容。
- 用户明确首页不需要排行榜或成就摘要卡，也不保留在线占位；首页因此不需要为这两类数据发起网络请求。下一项进入全局浅色/深色外观范围。
- 用户确认首版支持跟随系统、浅色和深色三种外观，默认跟随系统，手动选择持久化。移动端使用一套设计 Token 切换外观，不复制页面；下一项确定 App 专属视觉与现有品牌元素的保留边界。
- 用户确认保留 `2048 NEXT` 品牌、青金角色和数字块等级识别，其余界面全部重新设计；首版不带 Web 主题选择器或色板编辑器。现有 Web 仅有默认关闭的约 3 MB 背景音乐且没有正式触觉/合并音效链路，下一项确定 App 的声音与触觉范围。
- 用户确认首版采用默认开启的轻量短音效和克制触觉，两者可独立关闭；背景音乐保留但默认关闭。音频与触觉通过核心 effects 的平台适配层实现，不进入游戏规则；Android 只打包一种 BGM 编码。下一项补齐账号彻底删除后的邮箱重注册规则。
- 用户确认账号彻底清理完成后允许原邮箱重新注册，但必须产生新的用户 ID，旧数据不可恢复或关联；冷静期内邮箱仍被原账号占用。同步清理 PRD 中已经被后续决策覆盖的 beta 与语言待确认表述。下一项确定首次启动的隐私同意与纯离线游客路径。
- 用户确认首次启动采用单页隐私选择：同意后启用联网能力，或仅离线体验游客标准 4×4；未同意前不发起业务网络请求或初始化第三方统计，首次进入联网功能时再次请求同意。政策正文随包可读并按实质版本管理。下一项确定首发渠道与更新链路。
- 用户确认首发由官网提供签名 APK，同时持续产出商店兼容 AAB；首版不接商店 SDK 或应用内更新框架。正式签名、校验值和同签名覆盖升级进入发布基底要求。下一项确定云历史与成就的离线快照策略。
- 用户确认有界缓存最近加载的云历史页面和成就摘要，离线只读并显示最后更新时间；退出/切换账号清除快照，游客本地历史保持独立。下一项确定账号云记录与游客本地记录各自的删除语义。
- 用户确认账号云记录沿用 3 天软删除/恢复并提供最近删除筛选，游客本地记录二次确认后永久删除；离线云快照不允许执行删除。成就审计发现目录包含 Web 彩蛋、404、手工授予和未移植模式内容，下一项确定 App 对已获得与未获得成就的展示边界。
- 用户确认全部已获得成就跨端显示，未获得列表只展示首版 App 可完成项，隐藏成就获得前保持隐藏；后端需要提供明确客户端可完成元数据。下一项确定首版是否引入 Android 通知权限与推送链路。
- 用户确认首版不做推送或系统本地通知，不申请通知权限；同步、成就、网络和账号状态仅用 App 内提示，账号删除配合邮箱确认。下一项确定首次进入棋盘时的一次性操作提示。
- 用户否决首次操作提示，App 不加入首次启动轮播、首次对局遮罩、手势动画或强制教学，进入棋盘后直接操作。代码库没有现成 App 崩溃上报链路，下一项确定稳定性诊断的联网与隐私边界。
- 用户确认完成隐私同意后自动上报脱敏的崩溃与严重错误，但不采集页面点击、游戏行为或广告分析数据，也不引入 Sentry、Crashlytics 等第三方统计 SDK。首版采用直接提交到 `2048-game-api` 的最小诊断接口，禁止上传邮箱、昵称、Token、棋盘、回放和移动序列；仅离线体验绝不上传，设置中允许关闭自动诊断并手动导出本地诊断。下一项转入核心对局与结算流程。
- 代码核对确认 Web 默认在首次合成 2048 时显示“你赢了 / 继续游戏”，关闭胜利提示设置后则自动继续。用户确认 App 不继承该分支设置：首次合成 2048 自动继续，只显示非阻塞里程碑提示；计时、排位会话、存档和棋盘输入保持连续，真正无合法移动时才结算。下一项确定主动结束未完成对局是否形成历史。
- 代码核对确认现有后端跳过非 `game_over` 记录并验证真实终局。用户确认 App 未结束对局不形成历史：返回键或离开只保存存档并回首页，重新开始视为放弃当前局并仅覆盖当前模式存档；两者均不写本地/云记录、不进入 outbox，首版不新增“主动结束并保存成绩”。下一项确定经典可撤回模式在无路可走时先允许撤回还是立即固化结算。
- 代码核对确认 Web 可撤回模式的终局层仍提供撤回，但现有持久化会在终局 actuate 时立即提交，直接复用会造成“先提交、后撤回”冲突。用户确认 App 改为待结算终局：有可撤回状态时显示“撤回继续 / 结束并结算”，选择结算或已无法撤回时才固化记录；无撤回模式直接结算。待确认状态需可持久化恢复，不能自动结算。下一项确定结算页的必要信息与动作。
- 用户确认 App 最小结算页展示模式、分数、最大方块、用时、步数、个人最佳/成就和异步上传状态；主操作“再来一局”，次操作“查看回放 / 返回首页”。不重复放置模式选择、排行榜等大厅入口，上传不得阻塞页面展示或离开。下一项确定首版回放分享与文件导出边界。
- 代码核对确认 Web 当前可复制回放代码、下载 `.txt`、打开回放页，历史另有 `.json + .txt` 导出，但仓库没有 Android 系统分享实现。用户确认 App 首版只分享/保存已结算记录的一种版本化回放文件，三个入口复用同一动作；不导出半局、不展示原始代码、不做公开链接或文件导入。产品访谈问题至此收敛，下一步清理 PRD 并编写技术设计与实施计划。
- 已清理 PRD 中被后续决定覆盖的待确认措辞，产品范围无剩余开放问题。
- 技术设计并行审计确认：现有 `EngineSession.move()` 不执行真实棋盘移动，App 页面之前必须补齐无 DOM 移动、确定性出块、撤回和回放闭环；经典模式还存在表面 90/10 配置之外的高位特殊出块规则，已纳入黄金 parity 门槛。
- App 构建采用 `mobile/ + vite.app.config.ts + dist-app`，不调用 Web Vite legacy 复制插件。目标模块采用小 interface 的 Game Session、App Database、Online Sync 与 Android Platform seam，不增加 React、路由库、手势库或通用插件系统。
- 本地关键数据选择新的 IndexedDB 而非 SQLite：现有仓库已有升级/事务模式，首版数据规模无需原生数据库插件。Token 与排位 session Token 使用最小 Android Keystore AES-GCM bridge；回放只写 Cache 后交给官方 Filesystem/Share 插件，不申请外部存储权限。
- 后端联调阻塞项已落入设计：OpenAPI 漂移、最近删除越权、排行榜权威 rank/tie-break、canonical `ended_at`、成就客户端元数据、auth version + 72 小时删号、最小诊断接口和 Capacitor CORS。
- Android 审计基线：Android Studio 2026.1 与 SDK 36.1/Build Tools 36 已安装；终端当前是 JDK 17、Node 24，实施时需按 Capacitor 8/现有 CI 统一为 JDK 21、Node 22，并补 API 29 ARM64 AVD。系统 Gradle 不需要安装，使用 Wrapper。
- 已创建并获批 `design.md` 与 `implement.md`。实施计划分为核心闭环、后端合同、移动构建、存储生命周期、游客离线切片、账号在线、剩余功能和发布候选八个阶段；当前按阶段门禁继续实施。
- Trellis 规划一致性与门禁复核最初发现：服务端回放耐久性、公开删号网页、隐私延迟同意、云删除后的榜单重算、跨 Keystore/IndexedDB 清理语义、生产 `Backend Ready`、双仓库隔离、auth version 前向回滚、schema 回发兼容、Play 签名和 release 高刷证据尚不够可执行。现已逐项补入 PRD、技术设计和实施计划。
- 退出账号明确采用“立即不可见 + 可恢复续清”的逻辑原子协议，不虚构 Keystore 与 IndexedDB 之间不存在的物理事务；计划为清理每一步增加强杀故障注入。
- 现有 ranked 历史中缺少可信服务端开始锚点的少量记录无法还原真实本地完成时刻。规划采用可审计保守 fallback：只对这些旧记录使用最早可证明的服务端接收时间并记录来源；新记录不得使用 fallback。
- 为同时保证新排位时间锚点与本地输入性能，规划改为在棋盘展示前用稳定 operation ID 幂等创建 session 并冻结 `started_at/seed/token`；安全保存后才展示 ranked 棋盘，失败直接进入 normal。棋盘可操作后首步与后续动作均不等待网络，仍受 ≤50ms 输入预算约束。
- 最终两路独立只读复核均通过：未发现剩余审批阻塞、产品矛盾、开放问题或新增发布门禁缺口。

## 验证记录

- 规划阶段沿用此前只读验证：`2048-next` service boundary audit 通过；核心/API 定向测试 33 项通过；生产构建通过。
- `2048-game-api` Node 测试 21 个文件、143 项通过；`2048-ranked` 边界定向测试 27 项通过。
- 当前 `npm run audit:entry-manifest` 通过，确认现有 Web 页面入口分类和 legacy 加载顺序受审计保护。
- 2026-07-23 规划收口时重新执行 `git diff --check`，通过。
- 2026-07-23 重新执行 `npm run audit:service-boundary`，扫描 456 个文件、0 项违规，通过。
- 2026-07-23 重新执行 `npm run audit:entry-manifest`，页面入口分类、manifest 驱动入口及 play/replay 顺序检查通过。
- 2026-07-23 重新执行 `npm run test:unit:core`，97 个测试文件、658 项测试全部通过。
- 2026-07-26 完成首屏按需拆包：`mobile-cloud-data`（云历史/榜单/成就）、`replay-timeline`、`replay-share` 与 `diagnostic-export` 均成为独立动态 chunk，BGM 继续不进入首载 HTML；本机首页需要的游客最近记录摘要仍保留在首屏，这是 PRD 的首页功能而非云历史整页加载。
- production 首屏 JS 从 `308,037 / gzip 85,942` 字节降至 `297,256 / gzip 82,287` 字节；预算同步收紧到 `327,680 / gzip 92,160` 字节。构建审计要求四类动态 chunk 必须各存在一份且不得出现在首载 HTML，防止静态回归。
- 拆包后的首次完整 Smoke 暴露回放展示会在初始化时清空已加载模块引用；根因在共享 `#presentReplay` 清理顺序，已在该入口修复并增加“打开回放后下一步推进到 1”单元回归，不对各调用点重复打补丁。
- 最终 `npm run verify:app` 通过：App 单元测试 31 文件 `341/341`、production 专项 `1/1`、App Smoke `24/24`、production build、性能预算与移动边界审计全部通过。
- 最终 `npm run android:check` 通过：Android debug/release 构建模式审计、Lint、Gradle 单测、debug APK、Android 工程/资源/签名门禁全部通过；debug APK `13,691,940` 字节，SHA-256 `00a8dced5e3fcc9800097d1a4c58f7cdd4250f902cc84d6531b4a0010da2d8cd`，production 构建无 debug RAF sampler，debug 构建包含 sampler。GitHub 托管 API 29/API 36 矩阵和 90/120Hz 真机证据仍保持未完成，不以本机构建结果替代。
- 前端提交 `547a6cf3` 完成云历史/榜单/成就数据层、回放时间线、回放分享和诊断导出的按需加载及静态回归门禁；未提交 `output/playwright/stage5-visual/`。
- 前端提交 `e894f0a2` 增加仅允许 `workflow_dispatch`、绑定 `android-release` GitHub Environment 的候选发布 workflow，以及 `npm run android:release`。同一 Gradle 调用生成 APK+AAB，随后验证 APK 签名、AAB JAR 签名、两者证书与预期 SHA-256、包名、版本、工作树洁净度及 `GITHUB_SHA == HEAD`，最后输出带 commit、大小和文件 SHA-256 的 `metadata.json`；流程只上传 GitHub artifact，不部署官网、不上传 Play。
- 使用一次性、1 天有效的本地测试证书对提交 `e894f0a2f1a65cb8864e73606419f2b5031e2eef` 完成干净工作树演练：release APK `11,790,879` 字节，AAB `6,775,072` 字节，双产物证书一致且元数据 commit 正确；临时证书已清除。该证据只验证构建链路，不是正式签名、覆盖升级、Play signer 或发布批准，实施计划第 335 项继续保持未完成。
- release workflow 需要仓库侧另行创建并保护 `android-release` Environment，配置 5 个 secrets：keystore base64、store password、key alias、key password、预期证书 SHA-256。正式密钥生成、备份、Play App Signing 决策和第一次真实 workflow 运行均继续等待单独授权。
- 2026-07-26 03:22:04 +0800，用户再次明确批准当前低保真线框；冻结 SHA-256 保持 `3c302a08d880cd342c11accc45830ec7de9bcd484f089b584e588415de0e7274`，不扩张首版范围。
- 前端提交 `8d76793c1a128702e08365d13f5ef49f722060f7` 增加显式 `NEXT2048_VERSION_CODE` / `NEXT2048_VERSION_NAME` 构建输入、候选产物版本核对和隔离的 `upgradeTest` build type。非法版本号与含空格版本名均会在构建边界被拒绝。
- API 29 / Android 10 ARM64 模拟器覆盖升级演练通过：同一测试证书从 `versionCode 1000001` 升级到 `1000002` 后，IndexedDB 未结束存档、Android Keystore 加密值和 localStorage 设置均保留；使用错误证书的 `1000003` 被系统以 `INSTALL_FAILED_UPDATE_INCOMPATIBLE` 拒绝，已安装版本仍为 `1000002`。证据保存于 `artifacts/android-upgrade-smoke-api-29.json`，证据提交工作树的已跟踪文件为干净状态。
- 在同一提交上使用一次性测试证书成功构建 `versionCode 42` / `versionName 1.0-rc-test` 的 APK 与 AAB：APK `11,790,891` 字节，SHA-256 `12e1f589458d8fd51c52f0f2fe9d7874531ff4fb39d043516ffc91c53572ec62`；AAB `6,775,070` 字节，SHA-256 `bd193d81f9e42a2deebeef766b817ca8927afdc826731c0cf18940c9930bef5b`。该次仅验证版本化候选机制，临时证书已精确清理，产物不得分发。
- 正式签名候选、正式包名覆盖升级、Play 签名连续性与跨渠道 signer 证据仍未完成，不以临时测试签名演练代替发布门禁。
- 2026-07-26 03:23 +0800，在前端提交 `8d76793c1a128702e08365d13f5ef49f722060f7` 重新运行 `npm run verify:app` 通过：App unit `33` 文件 `346/346`、production 专项 `1/1`、App Smoke `24/24`，production build、性能预算和移动边界审计全部通过。首载 JS `297,256` 字节（gzip `82,287`）、CSS `34,677` 字节（gzip `7,884`），四个非首屏动态 chunk 均保持拆分。
- 2026-07-26 03:38 +0800，本机 API 36 / Android 16 ARM64 模拟器 Smoke 通过：使用 debug APK SHA-256 `00a8dced5e3fcc9800097d1a4c58f7cdd4250f902cc84d6531b4a0010da2d8cd`，清数据冷启动 `1212ms`，真实 ADB 滑动后棋盘发生变化，HOME 热恢复保留棋盘，force-stop 后冷启动 `606ms` 先返回首页，继续游戏恢复同一棋盘，`externalResources=[]`。报告保存于 worktree 忽略产物 `artifacts/android-emulator-smoke-api-36.json`；运行后已关闭 API 36 模拟器，未改动已有 API 29 AVD。该证据补齐本机最新 Android 模拟器矩阵，但不代替 GitHub 托管 runner 首次实跑、90/120Hz 真机或正式签名候选门禁。
- 2026-07-26 09:36 +0800，后端当前提交重跑 `npm run typecheck && npm run test:node` 通过：Node `33` 文件 `280/280`。前端完整 `verify:release` 首跑暴露两项测试契约回归：production 专项被通用 `MODE=test` 误收集，以及旧 Web 榜单 fixture 缺少已必需的后端权威 `rank`。前端提交 `9b673932` 只修正测试入口与 fixture：通用单测排除 production 专项，专项仍以 `--mode production` 单独执行；TOP 10 与本人行提供 `rank: 1..11`，不恢复前端位置推算。定向榜单 `1/1`、production `1/1`、通用单测 `335` 文件 `2265/2265`通过；最终 `verify:release` 的审计、CI Smoke、Web production build 和 release-ready 全部通过。
- 前端分支 `agent/android-app-foundation` 已推送至远端，当前提交 `9b673932`；后端分支 `agent/android-app-backend` 已推送至远端，当前提交 `8ef54fd`。未推送/提交 `output/playwright/stage5-visual/`，未创建 PR、未合并默认分支、未触发生产部署。手动调度 Android workflow 因该 workflow 尚未存在于默认分支而返回 404，不是构建失败；GitHub 托管 API 29/API 36 与 upgrade Smoke 仍需在安全集成后首次实跑。
